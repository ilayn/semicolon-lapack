/* AUTO-GENERATED FILE — DO NOT EDIT
 * Fortran-ABI compatibility shim for semicolon-lapack
 * Precision: double
 * Source header: semicolon_lapack_double.h
 * Generated by scripts/generate_fortran_shim.py
 */

#include <stdlib.h>
#include "semicolon_lapack_double.h"


f64 dladiv2_(f64* a, f64* b, f64* c, f64* d, f64* r, f64* t) {
    f64 _ret = dladiv2(*a, *b, *c, *d, *r, *t);
    return _ret;
}

f64 dlamch_(char* cmach) {
    f64 _ret = dlamch(cmach);
    return _ret;
}

f64 dlangb_(char* norm, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, f64* work) {
    f64 _ret = dlangb(norm, *n, *kl, *ku, AB, *ldab, work);
    return _ret;
}

f64 dlange_(char* norm, INT* m, INT* n, f64* A, INT* lda, f64* work) {
    f64 _ret = dlange(norm, *m, *n, A, *lda, work);
    return _ret;
}

f64 dlangt_(char* norm, INT* n, f64* DL, f64* D, f64* DU) {
    f64 _ret = dlangt(norm, *n, DL, D, DU);
    return _ret;
}

f64 dlanhs_(char* norm, INT* n, f64* A, INT* lda, f64* work) {
    f64 _ret = dlanhs(norm, *n, A, *lda, work);
    return _ret;
}

f64 dlansb_(char* norm, char* uplo, INT* n, INT* k, f64* AB, INT* ldab, f64* work) {
    f64 _ret = dlansb(norm, uplo, *n, *k, AB, *ldab, work);
    return _ret;
}

f64 dlansf_(char* norm, char* transr, char* uplo, INT* n, f64* A, f64* work) {
    f64 _ret = dlansf(norm, transr, uplo, *n, A, work);
    return _ret;
}

f64 dlansp_(char* norm, char* uplo, INT* n, f64* AP, f64* work) {
    f64 _ret = dlansp(norm, uplo, *n, AP, work);
    return _ret;
}

f64 dlanst_(char* norm, INT* n, f64* D, f64* E) {
    f64 _ret = dlanst(norm, *n, D, E);
    return _ret;
}

f64 dlansy_(char* norm, char* uplo, INT* n, f64* A, INT* lda, f64* work) {
    f64 _ret = dlansy(norm, uplo, *n, A, *lda, work);
    return _ret;
}

f64 dlantb_(char* norm, char* uplo, char* diag, INT* n, INT* k, f64* AB, INT* ldab, f64* work) {
    f64 _ret = dlantb(norm, uplo, diag, *n, *k, AB, *ldab, work);
    return _ret;
}

f64 dlantp_(char* norm, char* uplo, char* diag, INT* n, f64* AP, f64* work) {
    f64 _ret = dlantp(norm, uplo, diag, *n, AP, work);
    return _ret;
}

f64 dlantr_(char* norm, char* uplo, char* diag, INT* m, INT* n, f64* A, INT* lda, f64* work) {
    f64 _ret = dlantr(norm, uplo, diag, *m, *n, A, *lda, work);
    return _ret;
}

f64 dlapy2_(f64* x, f64* y) {
    f64 _ret = dlapy2(*x, *y);
    return _ret;
}

f64 dlapy3_(f64* x, f64* y, f64* z) {
    f64 _ret = dlapy3(*x, *y, *z);
    return _ret;
}

f64 dlarmm_(f64* anorm, f64* bnorm, f64* cnorm) {
    f64 _ret = dlarmm(*anorm, *bnorm, *cnorm);
    return _ret;
}

INT disnan_(f64* din) {
    INT _ret = disnan(*din);
    return _ret;
}

INT dlaisnan_(f64* din1, f64* din2) {
    INT _ret = dlaisnan(*din1, *din2);
    return _ret;
}

INT dlaneg_(INT* n, f64* D, f64* lld, f64* sigma, f64* pivmin, INT* r) {
    INT _ret = dlaneg(*n, D, lld, *sigma, *pivmin, *r);
    return _ret;
}

INT iladlc_(INT* m, INT* n, f64* A, INT* lda) {
    INT _ret = iladlc(*m, *n, A, *lda);
    return _ret;
}

INT iladlr_(INT* m, INT* n, f64* A, INT* lda) {
    INT _ret = iladlr(*m, *n, A, *lda);
    return _ret;
}

void dbbcsd_(char* jobu1, char* jobu2, char* jobv1t, char* jobv2t, char* trans, INT* m, INT* p, INT* q, f64* theta, f64* phi, f64* U1, INT* ldu1, f64* U2, INT* ldu2, f64* V1T, INT* ldv1t, f64* V2T, INT* ldv2t, f64* B11D, f64* B11E, f64* B12D, f64* B12E, f64* B21D, f64* B21E, f64* B22D, f64* B22E, f64* work, INT* lwork, INT* info) {
    dbbcsd(jobu1, jobu2, jobv1t, jobv2t, trans, *m, *p, *q, theta, phi, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, V2T, *ldv2t, B11D, B11E, B12D, B12E, B21D, B21E, B22D, B22E, work, *lwork, info);
}

void dbdsdc_(char* uplo, char* compq, INT* n, f64* D, f64* E, f64* U, INT* ldu, f64* VT, INT* ldvt, f64* Q, INT* IQ, f64* work, INT* IWORK, INT* info) {
    dbdsdc(uplo, compq, *n, D, E, U, *ldu, VT, *ldvt, Q, IQ, work, IWORK, info);
}

void dbdsqr_(char* uplo, INT* n, INT* ncvt, INT* nru, INT* ncc, f64* D, f64* E, f64* VT, INT* ldvt, f64* U, INT* ldu, f64* C, INT* ldc, f64* work, INT* info) {
    dbdsqr(uplo, *n, *ncvt, *nru, *ncc, D, E, VT, *ldvt, U, *ldu, C, *ldc, work, info);
}

void dbdsvdx_(char* uplo, char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, INT* ns, f64* S, f64* Z, INT* ldz, f64* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dbdsvdx(uplo, jobz, range, *n, D, E, *vl, *vu, _il, _iu, ns, S, Z, *ldz, work, iwork, info);
}

void ddisna_(char* job, INT* m, INT* n, f64* D, f64* SEP, INT* info) {
    ddisna(job, *m, *n, D, SEP, info);
}

void dgbbrd_(char* vect, INT* m, INT* n, INT* ncc, INT* kl, INT* ku, f64* AB, INT* ldab, f64* D, f64* E, f64* Q, INT* ldq, f64* PT, INT* ldpt, f64* C, INT* ldc, f64* work, INT* info) {
    dgbbrd(vect, *m, *n, *ncc, *kl, *ku, AB, *ldab, D, E, Q, *ldq, PT, *ldpt, C, *ldc, work, info);
}

void dgbcon_(char* norm, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, INT* ipiv, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgbcon(norm, *n, *kl, *ku, AB, *ldab, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void dgbequ_(INT* m, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    dgbequ(*m, *n, *kl, *ku, AB, *ldab, R, C, rowcnd, colcnd, amax, info);
}

void dgbequb_(INT* m, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    dgbequb(*m, *n, *kl, *ku, AB, *ldab, R, C, rowcnd, colcnd, amax, info);
}

void dgbrfs_(char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, f64* AB, INT* ldab, f64* AFB, INT* ldafb, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgbrfs(trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void dgbsv_(INT* n, INT* kl, INT* ku, INT* nrhs, f64* AB, INT* ldab, INT* ipiv, f64* B, INT* ldb, INT* info) {
    dgbsv(*n, *kl, *ku, *nrhs, AB, *ldab, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgbsvx_(char* fact, char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, f64* AB, INT* ldab, f64* AFB, INT* ldafb, INT* ipiv, char* equed, f64* R, f64* C, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        dgbsvx(fact, trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, _ipiv0, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        dgbsvx(fact, trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, ipiv, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dgbtf2_(INT* m, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, INT* ipiv, INT* info) {
    dgbtf2(*m, *n, *kl, *ku, AB, *ldab, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgbtrf_(INT* m, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, INT* ipiv, INT* info) {
    dgbtrf(*m, *n, *kl, *ku, AB, *ldab, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgbtrs_(char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, f64* AB, INT* ldab, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgbtrs(trans, *n, *kl, *ku, *nrhs, AB, *ldab, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dgebak_(char* job, char* side, INT* n, INT* ilo, INT* ihi, f64* scale, INT* m, f64* V, INT* ldv, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    if (job[0] == 'B' || job[0] == 'b' || job[0] == 'P' || job[0] == 'p') {
        for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] -= 1.0;
        for (INT _i = *ihi; _i < *n; _i++) scale[_i] -= 1.0;
    }
    dgebak(job, side, *n, _ilo, _ihi, scale, *m, V, *ldv, info);
    if (job[0] == 'B' || job[0] == 'b' || job[0] == 'P' || job[0] == 'p') {
        for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] += 1.0;
        for (INT _i = *ihi; _i < *n; _i++) scale[_i] += 1.0;
    }
}

void dgebal_(char* job, INT* n, f64* A, INT* lda, INT* ilo, INT* ihi, f64* scale, INT* info) {
    dgebal(job, *n, A, *lda, ilo, ihi, scale, info);
    (*ilo)++;
    (*ihi)++;
    for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] += 1.0;
    for (INT _i = *ihi; _i < *n; _i++) scale[_i] += 1.0;
}

void dgebd2_(INT* m, INT* n, f64* A, INT* lda, f64* D, f64* E, f64* tauq, f64* taup, f64* work, INT* info) {
    dgebd2(*m, *n, A, *lda, D, E, tauq, taup, work, info);
}

void dgebrd_(INT* m, INT* n, f64* A, INT* lda, f64* D, f64* E, f64* tauq, f64* taup, f64* work, INT* lwork, INT* info) {
    dgebrd(*m, *n, A, *lda, D, E, tauq, taup, work, *lwork, info);
}

void dgecon_(char* norm, INT* n, f64* A, INT* lda, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    dgecon(norm, *n, A, *lda, *anorm, rcond, work, iwork, info);
}

void dgeequ_(INT* m, INT* n, f64* A, INT* lda, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    dgeequ(*m, *n, A, *lda, R, C, rowcnd, colcnd, amax, info);
}

void dgeequb_(INT* m, INT* n, f64* A, INT* lda, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    dgeequb(*m, *n, A, *lda, R, C, rowcnd, colcnd, amax, info);
}

void dgees_(char* jobvs, char* sort, dselect2_t select, INT* n, f64* A, INT* lda, INT* sdim, f64* wr, f64* wi, f64* VS, INT* ldvs, f64* work, INT* lwork, INT* bwork, INT* info) {
    dgees(jobvs, sort, select, *n, A, *lda, sdim, wr, wi, VS, *ldvs, work, *lwork, bwork, info);
}

void dgeesx_(char* jobvs, char* sort, dselect2_t select, char* sense, INT* n, f64* A, INT* lda, INT* sdim, f64* wr, f64* wi, f64* VS, INT* ldvs, f64* rconde, f64* rcondv, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* bwork, INT* info) {
    dgeesx(jobvs, sort, select, sense, *n, A, *lda, sdim, wr, wi, VS, *ldvs, rconde, rcondv, work, *lwork, iwork, *liwork, bwork, info);
}

void dgeev_(char* jobvl, char* jobvr, INT* n, f64* A, INT* lda, f64* wr, f64* wi, f64* VL, INT* ldvl, f64* VR, INT* ldvr, f64* work, INT* lwork, INT* info) {
    dgeev(jobvl, jobvr, *n, A, *lda, wr, wi, VL, *ldvl, VR, *ldvr, work, *lwork, info);
}

void dgeevx_(char* balanc, char* jobvl, char* jobvr, char* sense, INT* n, f64* A, INT* lda, f64* wr, f64* wi, f64* VL, INT* ldvl, f64* VR, INT* ldvr, INT* ilo, INT* ihi, f64* scale, f64* abnrm, f64* rconde, f64* rcondv, f64* work, INT* lwork, INT* iwork, INT* info) {
    dgeevx(balanc, jobvl, jobvr, sense, *n, A, *lda, wr, wi, VL, *ldvl, VR, *ldvr, ilo, ihi, scale, abnrm, rconde, rcondv, work, *lwork, iwork, info);
    (*ilo)++;
    (*ihi)++;
}

void dgehd2_(INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dgehd2(*n, _ilo, _ihi, A, *lda, tau, work, info);
}

void dgehrd_(INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dgehrd(*n, _ilo, _ihi, A, *lda, tau, work, *lwork, info);
}

void dgejsv_(char* joba, char* jobu, char* jobv, char* jobr, char* jobt, char* jobp, INT* m, INT* n, f64* A, INT* lda, f64* SVA, f64* U, INT* ldu, f64* V, INT* ldv, f64* work, INT* lwork, INT* iwork, INT* info) {
    dgejsv(joba, jobu, jobv, jobr, jobt, jobp, *m, *n, A, *lda, SVA, U, *ldu, V, *ldv, work, *lwork, iwork, info);
}

void dgelq_(INT* m, INT* n, f64* A, INT* lda, f64* T, INT* tsize, f64* work, INT* lwork, INT* info) {
    dgelq(*m, *n, A, *lda, T, *tsize, work, *lwork, info);
}

void dgelq2_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dgelq2(*m, *n, A, *lda, tau, work, info);
}

void dgelqf_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dgelqf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void dgelqt_(INT* m, INT* n, INT* mb, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* info) {
    dgelqt(*m, *n, *mb, A, *lda, T, *ldt, work, info);
}

void dgelqt3_(INT* m, INT* n, f64* A, INT* lda, f64* T, INT* ldt, INT* info) {
    dgelqt3(*m, *n, A, *lda, T, *ldt, info);
}

void dgels_(char* trans, INT* m, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dgels(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void dgelsd_(INT* m, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* S, f64* rcond, INT* rank, f64* work, INT* lwork, INT* iwork, INT* info) {
    dgelsd(*m, *n, *nrhs, A, *lda, B, *ldb, S, *rcond, rank, work, *lwork, iwork, info);
}

void dgelss_(INT* m, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* S, f64* rcond, INT* rank, f64* work, INT* lwork, INT* info) {
    dgelss(*m, *n, *nrhs, A, *lda, B, *ldb, S, *rcond, rank, work, *lwork, info);
}

void dgelst_(char* trans, INT* m, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dgelst(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void dgelsy_(INT* m, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, INT* jpvt, f64* rcond, INT* rank, f64* work, INT* lwork, INT* info) {
    INT* _jpvt0 = NULL;
    if (*lwork != -1) {
        if (jpvt) {
            INT _jpvt_sz = *n;
            _jpvt0 = (INT*)malloc(_jpvt_sz * sizeof(INT));
            for (INT _i = 0; _i < _jpvt_sz; _i++) _jpvt0[_i] = (jpvt[_i] > 0) ? jpvt[_i] - 1 : jpvt[_i];
        }
    }
    if (*lwork != -1) {
        dgelsy(*m, *n, *nrhs, A, *lda, B, *ldb, _jpvt0, *rcond, rank, work, *lwork, info);
    } else {
        dgelsy(*m, *n, *nrhs, A, *lda, B, *ldb, jpvt, *rcond, rank, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_jpvt0);
    }
}

void dgemlq_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* T, INT* tsize, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dgemlq(side, trans, *m, *n, *k, A, *lda, T, *tsize, C, *ldc, work, *lwork, info);
}

void dgemlqt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, f64* V, INT* ldv, f64* T, INT* ldt, f64* C, INT* ldc, f64* work, INT* info) {
    dgemlqt(side, trans, *m, *n, *k, *mb, V, *ldv, T, *ldt, C, *ldc, work, info);
}

void dgemqr_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* T, INT* tsize, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dgemqr(side, trans, *m, *n, *k, A, *lda, T, *tsize, C, *ldc, work, *lwork, info);
}

void dgemqrt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* nb, f64* V, INT* ldv, f64* T, INT* ldt, f64* C, INT* ldc, f64* work, INT* info) {
    dgemqrt(side, trans, *m, *n, *k, *nb, V, *ldv, T, *ldt, C, *ldc, work, info);
}

void dgeql2_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dgeql2(*m, *n, A, *lda, tau, work, info);
}

void dgeqlf_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dgeqlf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void dgeqp3_(INT* m, INT* n, f64* A, INT* lda, INT* jpvt, f64* tau, f64* work, INT* lwork, INT* info) {
    dgeqp3(*m, *n, A, *lda, jpvt, tau, work, *lwork, info);
    if (*lwork != -1) {
        if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
    }
}

void dgeqp3rk_(INT* m, INT* n, INT* nrhs, INT* kmax, f64* abstol, f64* reltol, f64* A, INT* lda, INT* K, f64* maxc2nrmk, f64* relmaxc2nrmk, INT* jpiv, f64* tau, f64* work, INT* lwork, INT* iwork, INT* info) {
    dgeqp3rk(*m, *n, *nrhs, *kmax, *abstol, *reltol, A, *lda, K, maxc2nrmk, relmaxc2nrmk, jpiv, tau, work, *lwork, iwork, info);
    if (*lwork != -1) {
        if (jpiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpiv[_i] >= 0) jpiv[_i]++; } }
    }
}

void dgeqr_(INT* m, INT* n, f64* A, INT* lda, f64* T, INT* tsize, f64* work, INT* lwork, INT* info) {
    dgeqr(*m, *n, A, *lda, T, *tsize, work, *lwork, info);
}

void dgeqr2_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dgeqr2(*m, *n, A, *lda, tau, work, info);
}

void dgeqr2p_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dgeqr2p(*m, *n, A, *lda, tau, work, info);
}

void dgeqrf_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dgeqrf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void dgeqrfp_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dgeqrfp(*m, *n, A, *lda, tau, work, *lwork, info);
}

void dgeqrt_(INT* m, INT* n, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* info) {
    dgeqrt(*m, *n, *nb, A, *lda, T, *ldt, work, info);
}

void dgeqrt2_(INT* m, INT* n, f64* A, INT* lda, f64* T, INT* ldt, INT* info) {
    dgeqrt2(*m, *n, A, *lda, T, *ldt, info);
}

void dgeqrt3_(INT* m, INT* n, f64* A, INT* lda, f64* T, INT* ldt, INT* info) {
    dgeqrt3(*m, *n, A, *lda, T, *ldt, info);
}

void dgerfs_(char* trans, INT* n, INT* nrhs, f64* A, INT* lda, f64* AF, INT* ldaf, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgerfs(trans, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void dgerq2_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dgerq2(*m, *n, A, *lda, tau, work, info);
}

void dgerqf_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dgerqf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void dgesc2_(INT* n, f64* A, INT* lda, f64* rhs, INT* ipiv, INT* jpiv, f64* scale) {
    INT* _ipiv0 = NULL;
    INT* _jpiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (jpiv) {
        INT _jpiv_sz = *n;
        _jpiv0 = (INT*)malloc(_jpiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _jpiv_sz; _i++) _jpiv0[_i] = (jpiv[_i] > 0) ? jpiv[_i] - 1 : jpiv[_i];
    }
    dgesc2(*n, A, *lda, rhs, _ipiv0, _jpiv0, scale);
    free(_ipiv0);
    free(_jpiv0);
}

void dgesdd_(char* jobz, INT* m, INT* n, f64* A, INT* lda, f64* S, f64* U, INT* ldu, f64* VT, INT* ldvt, f64* work, INT* lwork, INT* IWORK, INT* info) {
    dgesdd(jobz, *m, *n, A, *lda, S, U, *ldu, VT, *ldvt, work, *lwork, IWORK, info);
}

void dgesv_(INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, INT* info) {
    dgesv(*n, *nrhs, A, *lda, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgesvd_(char* jobu, char* jobvt, INT* m, INT* n, f64* A, INT* lda, f64* S, f64* U, INT* ldu, f64* VT, INT* ldvt, f64* work, INT* lwork, INT* info) {
    dgesvd(jobu, jobvt, *m, *n, A, *lda, S, U, *ldu, VT, *ldvt, work, *lwork, info);
}

void dgesvdq_(char* joba, char* jobp, char* jobr, char* jobu, char* jobv, INT* m, INT* n, f64* A, INT* lda, f64* S, f64* U, INT* ldu, f64* V, INT* ldv, INT* numrank, INT* iwork, INT* liwork, f64* work, INT* lwork, f64* rwork, INT* lrwork, INT* info) {
    dgesvdq(joba, jobp, jobr, jobu, jobv, *m, *n, A, *lda, S, U, *ldu, V, *ldv, numrank, iwork, *liwork, work, *lwork, rwork, *lrwork, info);
}

void dgesvdx_(char* jobu, char* jobvt, char* range, INT* m, INT* n, f64* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, INT* ns, f64* S, f64* U, INT* ldu, f64* VT, INT* ldvt, f64* work, INT* lwork, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dgesvdx(jobu, jobvt, range, *m, *n, A, *lda, *vl, *vu, _il, _iu, ns, S, U, *ldu, VT, *ldvt, work, *lwork, iwork, info);
}

void dgesvj_(char* joba, char* jobu, char* jobv, INT* m, INT* n, f64* A, INT* lda, f64* SVA, INT* mv, f64* V, INT* ldv, f64* work, INT* lwork, INT* info) {
    dgesvj(joba, jobu, jobv, *m, *n, A, *lda, SVA, *mv, V, *ldv, work, *lwork, info);
}

void dgesvx_(char* fact, char* trans, INT* n, INT* nrhs, f64* A, INT* lda, f64* AF, INT* ldaf, INT* ipiv, char* equed, f64* R, f64* C, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        dgesvx(fact, trans, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        dgesvx(fact, trans, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dgetc2_(INT* n, f64* A, INT* lda, INT* ipiv, INT* jpiv, INT* info) {
    dgetc2(*n, A, *lda, ipiv, jpiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    if (jpiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpiv[_i] >= 0) jpiv[_i]++; } }
}

void dgetf2_(INT* m, INT* n, f64* A, INT* lda, INT* ipiv, INT* info) {
    dgetf2(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgetrf_(INT* m, INT* n, f64* A, INT* lda, INT* ipiv, INT* info) {
    dgetrf(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgetrf2_(INT* m, INT* n, f64* A, INT* lda, INT* ipiv, INT* info) {
    dgetrf2(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgetri_(INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        dgetri(*n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        dgetri(*n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void dgetrs_(char* trans, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgetrs(trans, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dgetsls_(char* trans, INT* m, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dgetsls(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void dgetsqrhrt_(INT* m, INT* n, INT* mb1, INT* nb1, INT* nb2, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* lwork, INT* info) {
    dgetsqrhrt(*m, *n, *mb1, *nb1, *nb2, A, *lda, T, *ldt, work, *lwork, info);
}

void dggbak_(char* job, char* side, INT* n, INT* ilo, INT* ihi, f64* lscale, f64* rscale, INT* m, f64* V, INT* ldv, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dggbak(job, side, *n, _ilo, _ihi, lscale, rscale, *m, V, *ldv, info);
}

void dggbal_(char* job, INT* n, f64* A, INT* lda, f64* B, INT* ldb, INT* ilo, INT* ihi, f64* lscale, f64* rscale, f64* work, INT* info) {
    dggbal(job, *n, A, *lda, B, *ldb, ilo, ihi, lscale, rscale, work, info);
    (*ilo)++;
    (*ihi)++;
}

void dgges_(char* jobvsl, char* jobvsr, char* sort, dselect3_t selctg, INT* n, f64* A, INT* lda, f64* B, INT* ldb, INT* sdim, f64* alphar, f64* alphai, f64* beta, f64* VSL, INT* ldvsl, f64* VSR, INT* ldvsr, f64* work, INT* lwork, INT* bwork, INT* info) {
    dgges(jobvsl, jobvsr, sort, selctg, *n, A, *lda, B, *ldb, sdim, alphar, alphai, beta, VSL, *ldvsl, VSR, *ldvsr, work, *lwork, bwork, info);
}

void dgges3_(char* jobvsl, char* jobvsr, char* sort, dselect3_t selctg, INT* n, f64* A, INT* lda, f64* B, INT* ldb, INT* sdim, f64* alphar, f64* alphai, f64* beta, f64* VSL, INT* ldvsl, f64* VSR, INT* ldvsr, f64* work, INT* lwork, INT* bwork, INT* info) {
    dgges3(jobvsl, jobvsr, sort, selctg, *n, A, *lda, B, *ldb, sdim, alphar, alphai, beta, VSL, *ldvsl, VSR, *ldvsr, work, *lwork, bwork, info);
}

void dggesx_(char* jobvsl, char* jobvsr, char* sort, dselect3_t selctg, char* sense, INT* n, f64* A, INT* lda, f64* B, INT* ldb, INT* sdim, f64* alphar, f64* alphai, f64* beta, f64* VSL, INT* ldvsl, f64* VSR, INT* ldvsr, f64* rconde, f64* rcondv, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* bwork, INT* info) {
    dggesx(jobvsl, jobvsr, sort, selctg, sense, *n, A, *lda, B, *ldb, sdim, alphar, alphai, beta, VSL, *ldvsl, VSR, *ldvsr, rconde, rcondv, work, *lwork, iwork, *liwork, bwork, info);
}

void dggev_(char* jobvl, char* jobvr, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* alphar, f64* alphai, f64* beta, f64* VL, INT* ldvl, f64* VR, INT* ldvr, f64* work, INT* lwork, INT* info) {
    dggev(jobvl, jobvr, *n, A, *lda, B, *ldb, alphar, alphai, beta, VL, *ldvl, VR, *ldvr, work, *lwork, info);
}

void dggev3_(char* jobvl, char* jobvr, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* alphar, f64* alphai, f64* beta, f64* VL, INT* ldvl, f64* VR, INT* ldvr, f64* work, INT* lwork, INT* info) {
    dggev3(jobvl, jobvr, *n, A, *lda, B, *ldb, alphar, alphai, beta, VL, *ldvl, VR, *ldvr, work, *lwork, info);
}

void dggevx_(char* balanc, char* jobvl, char* jobvr, char* sense, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* alphar, f64* alphai, f64* beta, f64* VL, INT* ldvl, f64* VR, INT* ldvr, INT* ilo, INT* ihi, f64* lscale, f64* rscale, f64* abnrm, f64* bbnrm, f64* rconde, f64* rcondv, f64* work, INT* lwork, INT* iwork, INT* bwork, INT* info) {
    dggevx(balanc, jobvl, jobvr, sense, *n, A, *lda, B, *ldb, alphar, alphai, beta, VL, *ldvl, VR, *ldvr, ilo, ihi, lscale, rscale, abnrm, bbnrm, rconde, rcondv, work, *lwork, iwork, bwork, info);
    (*ilo)++;
    (*ihi)++;
}

void dggglm_(INT* n, INT* m, INT* p, f64* A, INT* lda, f64* B, INT* ldb, f64* D, f64* X, f64* Y, f64* work, INT* lwork, INT* info) {
    dggglm(*n, *m, *p, A, *lda, B, *ldb, D, X, Y, work, *lwork, info);
}

void dgghd3_(char* compq, char* compz, INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* B, INT* ldb, f64* Q, INT* ldq, f64* Z, INT* ldz, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dgghd3(compq, compz, *n, _ilo, _ihi, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, work, *lwork, info);
}

void dgghrd_(char* compq, char* compz, INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* B, INT* ldb, f64* Q, INT* ldq, f64* Z, INT* ldz, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dgghrd(compq, compz, *n, _ilo, _ihi, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, info);
}

void dgglse_(INT* m, INT* n, INT* p, f64* A, INT* lda, f64* B, INT* ldb, f64* C, f64* D, f64* X, f64* work, INT* lwork, INT* info) {
    dgglse(*m, *n, *p, A, *lda, B, *ldb, C, D, X, work, *lwork, info);
}

void dggqrf_(INT* n, INT* m, INT* p, f64* A, INT* lda, f64* taua, f64* B, INT* ldb, f64* taub, f64* work, INT* lwork, INT* info) {
    dggqrf(*n, *m, *p, A, *lda, taua, B, *ldb, taub, work, *lwork, info);
}

void dggrqf_(INT* m, INT* p, INT* n, f64* A, INT* lda, f64* taua, f64* B, INT* ldb, f64* taub, f64* work, INT* lwork, INT* info) {
    dggrqf(*m, *p, *n, A, *lda, taua, B, *ldb, taub, work, *lwork, info);
}

void dggsvd3_(char* jobu, char* jobv, char* jobq, INT* m, INT* n, INT* p, INT* k, INT* l, f64* A, INT* lda, f64* B, INT* ldb, f64* alpha, f64* beta, f64* U, INT* ldu, f64* V, INT* ldv, f64* Q, INT* ldq, f64* work, INT* lwork, INT* iwork, INT* info) {
    dggsvd3(jobu, jobv, jobq, *m, *n, *p, k, l, A, *lda, B, *ldb, alpha, beta, U, *ldu, V, *ldv, Q, *ldq, work, *lwork, iwork, info);
}

void dggsvp3_(char* jobu, char* jobv, char* jobq, INT* m, INT* p, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* tola, f64* tolb, INT* k, INT* l, f64* U, INT* ldu, f64* V, INT* ldv, f64* Q, INT* ldq, INT* iwork, f64* tau, f64* work, INT* lwork, INT* info) {
    dggsvp3(jobu, jobv, jobq, *m, *p, *n, A, *lda, B, *ldb, *tola, *tolb, k, l, U, *ldu, V, *ldv, Q, *ldq, iwork, tau, work, *lwork, info);
}

void dgsvj0_(char* jobv, INT* m, INT* n, f64* A, INT* lda, f64* D, f64* SVA, INT* mv, f64* V, INT* ldv, f64* eps, f64* sfmin, f64* tol, INT* nsweep, f64* work, INT* lwork, INT* info) {
    dgsvj0(jobv, *m, *n, A, *lda, D, SVA, *mv, V, *ldv, *eps, *sfmin, *tol, *nsweep, work, *lwork, info);
}

void dgsvj1_(char* jobv, INT* m, INT* n, INT* n1, f64* A, INT* lda, f64* D, f64* SVA, INT* mv, f64* V, INT* ldv, f64* eps, f64* sfmin, f64* tol, INT* nsweep, f64* work, INT* lwork, INT* info) {
    dgsvj1(jobv, *m, *n, *n1, A, *lda, D, SVA, *mv, V, *ldv, *eps, *sfmin, *tol, *nsweep, work, *lwork, info);
}

void dgtcon_(char* norm, INT* n, f64* DL, f64* D, f64* DU, f64* DU2, INT* ipiv, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgtcon(norm, *n, DL, D, DU, DU2, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void dgtrfs_(char* trans, INT* n, INT* nrhs, f64* DL, f64* D, f64* DU, f64* DLF, f64* DF, f64* DUF, f64* DU2, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgtrfs(trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void dgtsv_(INT* n, INT* nrhs, f64* DL, f64* D, f64* DU, f64* B, INT* ldb, INT* info) {
    dgtsv(*n, *nrhs, DL, D, DU, B, *ldb, info);
}

void dgtsvx_(char* fact, char* trans, INT* n, INT* nrhs, f64* DL, f64* D, f64* DU, f64* DLF, f64* DF, f64* DUF, f64* DU2, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        dgtsvx(fact, trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        dgtsvx(fact, trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dgttrf_(INT* n, f64* DL, f64* D, f64* DU, f64* DU2, INT* ipiv, INT* info) {
    dgttrf(*n, DL, D, DU, DU2, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dgttrs_(char* trans, INT* n, INT* nrhs, f64* DL, f64* D, f64* DU, f64* DU2, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgttrs(trans, *n, *nrhs, DL, D, DU, DU2, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dgtts2_(INT* itrans, INT* n, INT* nrhs, f64* DL, f64* D, f64* DU, f64* DU2, INT* ipiv, f64* B, INT* ldb) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dgtts2(*itrans, *n, *nrhs, DL, D, DU, DU2, _ipiv0, B, *ldb);
    free(_ipiv0);
}

void dhgeqz_(char* job, char* compq, char* compz, INT* n, INT* ilo, INT* ihi, f64* H, INT* ldh, f64* T, INT* ldt, f64* alphar, f64* alphai, f64* beta, f64* Q, INT* ldq, f64* Z, INT* ldz, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dhgeqz(job, compq, compz, *n, _ilo, _ihi, H, *ldh, T, *ldt, alphar, alphai, beta, Q, *ldq, Z, *ldz, work, *lwork, info);
}

void dhsein_(char* side, char* eigsrc, char* initv, INT* select, INT* n, f64* H, INT* ldh, f64* wr, f64* wi, f64* VL, INT* ldvl, f64* VR, INT* ldvr, INT* mm, INT* m, f64* work, INT* ifaill, INT* ifailr, INT* info) {
    dhsein(side, eigsrc, initv, select, *n, H, *ldh, wr, wi, VL, *ldvl, VR, *ldvr, *mm, m, work, ifaill, ifailr, info);
    if (ifaill) { INT _sz = *mm; for (INT _i = 0; _i < _sz; _i++) ifaill[_i]++; }
    if (ifailr) { INT _sz = *mm; for (INT _i = 0; _i < _sz; _i++) ifailr[_i]++; }
}

void dhseqr_(char* job, char* compz, INT* n, INT* ilo, INT* ihi, f64* H, INT* ldh, f64* wr, f64* wi, f64* Z, INT* ldz, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dhseqr(job, compz, *n, _ilo, _ihi, H, *ldh, wr, wi, Z, *ldz, work, *lwork, info);
}

void dlabad_(f64* small, f64* large) {
    dlabad(small, large);
}

void dlabrd_(INT* m, INT* n, INT* nb, f64* A, INT* lda, f64* D, f64* E, f64* tauq, f64* taup, f64* X, INT* ldx, f64* Y, INT* ldy) {
    dlabrd(*m, *n, *nb, A, *lda, D, E, tauq, taup, X, *ldx, Y, *ldy);
}

void dlacn2_(INT* n, f64* V, f64* X, INT* isgn, f64* est, INT* kase, INT* isave) {
    dlacn2(*n, V, X, isgn, est, kase, isave);
}

void dlacon_(INT* n, f64* V, f64* X, INT* ISGN, f64* est, INT* kase) {
    dlacon(*n, V, X, ISGN, est, kase);
}

void dlacpy_(char* uplo, INT* m, INT* n, f64* A, INT* lda, f64* B, INT* ldb) {
    dlacpy(uplo, *m, *n, A, *lda, B, *ldb);
}

void dladiv_(f64* a, f64* b, f64* c, f64* d, f64* p, f64* q) {
    dladiv(*a, *b, *c, *d, p, q);
}

void dladiv1_(f64* a, f64* b, f64* c, f64* d, f64* p, f64* q) {
    dladiv1(*a, *b, *c, *d, p, q);
}

void dlae2_(f64* a, f64* b, f64* c, f64* rt1, f64* rt2) {
    dlae2(*a, *b, *c, rt1, rt2);
}

void dlaebz_(INT* ijob, INT* nitmax, INT* n, INT* mmax, INT* minp, INT* nbmin, f64* abstol, f64* reltol, f64* pivmin, f64* D, f64* E, f64* E2, INT* nval, f64* AB, f64* C, INT* mout, INT* NAB, f64* work, INT* iwork, INT* info) {
    dlaebz(*ijob, *nitmax, *n, *mmax, *minp, *nbmin, *abstol, *reltol, *pivmin, D, E, E2, nval, AB, C, mout, NAB, work, iwork, info);
}

void dlaed0_(INT* icompq, INT* qsiz, INT* n, f64* D, f64* E, f64* Q, INT* ldq, f64* qstore, INT* ldqs, f64* work, INT* iwork, INT* info) {
    dlaed0(*icompq, *qsiz, *n, D, E, Q, *ldq, qstore, *ldqs, work, iwork, info);
}

void dlaed1_(INT* n, f64* D, f64* Q, INT* ldq, INT* indxq, f64* rho, INT* cutpnt, f64* work, INT* iwork, INT* info) {
    dlaed1(*n, D, Q, *ldq, indxq, *rho, *cutpnt, work, iwork, info);
}

void dlaed2_(INT* K, INT* n, INT* n1, f64* D, f64* Q, INT* ldq, INT* indxq, f64* rho, f64* Z, f64* dlambda, f64* W, f64* Q2, INT* indx, INT* indxc, INT* indxp, INT* coltyp, INT* info) {
    dlaed2(K, *n, *n1, D, Q, *ldq, indxq, rho, Z, dlambda, W, Q2, indx, indxc, indxp, coltyp, info);
}

void dlaed3_(INT* k, INT* n, INT* n1, f64* D, f64* Q, INT* ldq, f64* rho, f64* dlambda, f64* Q2, INT* indx, INT* ctot, f64* W, f64* S, INT* info) {
    dlaed3(*k, *n, *n1, D, Q, *ldq, *rho, dlambda, Q2, indx, ctot, W, S, info);
}

void dlaed4_(INT* n, INT* i, f64* D, f64* Z, f64* delta, f64* rho, f64* dlam, INT* info) {
    dlaed4(*n, *i, D, Z, delta, *rho, dlam, info);
}

void dlaed5_(INT* i, f64* D, f64* Z, f64* delta, f64* rho, f64* dlam) {
    dlaed5(*i, D, Z, delta, *rho, dlam);
}

void dlaed6_(INT* kniter, INT* orgati, f64* rho, f64* D, f64* Z, f64* finit, f64* tau, INT* info) {
    dlaed6(*kniter, *orgati, *rho, D, Z, *finit, tau, info);
}

void dlaed7_(INT* icompq, INT* n, INT* qsiz, INT* tlvls, INT* curlvl, INT* curpbm, f64* D, f64* Q, INT* ldq, INT* indxq, f64* rho, INT* cutpnt, f64* qstore, INT* qptr, INT* prmptr, INT* perm, INT* givptr, INT* givcol, f64* givnum, f64* work, INT* iwork, INT* info) {
    dlaed7(*icompq, *n, *qsiz, *tlvls, *curlvl, *curpbm, D, Q, *ldq, indxq, *rho, *cutpnt, qstore, qptr, prmptr, perm, givptr, givcol, givnum, work, iwork, info);
}

void dlaed8_(INT* icompq, INT* K, INT* n, INT* qsiz, f64* D, f64* Q, INT* ldq, INT* indxq, f64* rho, INT* cutpnt, f64* Z, f64* dlambda, f64* Q2, INT* ldq2, f64* W, INT* perm, INT* givptr, INT* givcol, f64* givnum, INT* indxp, INT* indx, INT* info) {
    dlaed8(*icompq, K, *n, *qsiz, D, Q, *ldq, indxq, rho, *cutpnt, Z, dlambda, Q2, *ldq2, W, perm, givptr, givcol, givnum, indxp, indx, info);
}

void dlaed9_(INT* k, INT* kstart, INT* kstop, INT* n, f64* D, f64* Q, INT* ldq, f64* rho, f64* dlambda, f64* W, f64* S, INT* lds, INT* info) {
    dlaed9(*k, *kstart, *kstop, *n, D, Q, *ldq, *rho, dlambda, W, S, *lds, info);
}

void dlaeda_(INT* n, INT* tlvls, INT* curlvl, INT* curpbm, INT* prmptr, INT* perm, INT* givptr, INT* givcol, f64* givnum, f64* Q, INT* qptr, f64* Z, f64* ztemp, INT* info) {
    dlaeda(*n, *tlvls, *curlvl, *curpbm, prmptr, perm, givptr, givcol, givnum, Q, qptr, Z, ztemp, info);
}

void dlaein_(INT* rightv, INT* noinit, INT* n, f64* H, INT* ldh, f64* wr, f64* wi, f64* vr, f64* vi, f64* B, INT* ldb, f64* work, f64* eps3, f64* smlnum, f64* bignum, INT* info) {
    dlaein(*rightv, *noinit, *n, H, *ldh, *wr, *wi, vr, vi, B, *ldb, work, *eps3, *smlnum, *bignum, info);
}

void dlaev2_(f64* a, f64* b, f64* c, f64* rt1, f64* rt2, f64* cs1, f64* sn1) {
    dlaev2(*a, *b, *c, rt1, rt2, cs1, sn1);
}

void dlaexc_(INT* wantq, INT* n, f64* T, INT* ldt, f64* Q, INT* ldq, INT* j1, INT* n1, INT* n2, f64* work, INT* info) {
    dlaexc(*wantq, *n, T, *ldt, Q, *ldq, *j1, *n1, *n2, work, info);
}

void dlag2_(f64* A, INT* lda, f64* B, INT* ldb, f64* safmin, f64* scale1, f64* scale2, f64* wr1, f64* wr2, f64* wi) {
    dlag2(A, *lda, B, *ldb, *safmin, scale1, scale2, wr1, wr2, wi);
}

void dlag2s_(INT* m, INT* n, f64* A, INT* lda, float* SA, INT* ldsa, INT* info) {
    dlag2s(*m, *n, A, *lda, SA, *ldsa, info);
}

void dlags2_(INT* upper, f64* a1, f64* a2, f64* a3, f64* b1, f64* b2, f64* b3, f64* csu, f64* snu, f64* csv, f64* snv, f64* csq, f64* snq) {
    dlags2(*upper, *a1, *a2, *a3, *b1, *b2, *b3, csu, snu, csv, snv, csq, snq);
}

void dlagtf_(INT* n, f64* A, f64* lambda, f64* B, f64* C, f64* tol, f64* D, INT* in, INT* info) {
    dlagtf(*n, A, *lambda, B, C, *tol, D, in, info);
}

void dlagtm_(char* trans, INT* n, INT* nrhs, f64* alpha, f64* DL, f64* D, f64* DU, f64* X, INT* ldx, f64* beta, f64* B, INT* ldb) {
    dlagtm(trans, *n, *nrhs, *alpha, DL, D, DU, X, *ldx, *beta, B, *ldb);
}

void dlagts_(INT* job, INT* n, f64* A, f64* B, f64* C, f64* D, INT* in, f64* Y, f64* tol, INT* info) {
    dlagts(*job, *n, A, B, C, D, in, Y, tol, info);
}

void dlagv2_(f64* A, INT* lda, f64* B, INT* ldb, f64* alphar, f64* alphai, f64* beta, f64* csl, f64* snl, f64* csr, f64* snr) {
    dlagv2(A, *lda, B, *ldb, alphar, alphai, beta, csl, snl, csr, snr);
}

void dlahqr_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, f64* H, INT* ldh, f64* wr, f64* wi, INT* iloz, INT* ihiz, f64* Z, INT* ldz, INT* info) {
    dlahqr(*wantt, *wantz, *n, *ilo, *ihi, H, *ldh, wr, wi, *iloz, *ihiz, Z, *ldz, info);
}

void dlahr2_(INT* n, INT* k, INT* nb, f64* A, INT* lda, f64* tau, f64* T, INT* ldt, f64* Y, INT* ldy) {
    dlahr2(*n, *k, *nb, A, *lda, tau, T, *ldt, Y, *ldy);
}

void dlaic1_(INT* job, INT* j, f64* x, f64* sest, f64* w, f64* gamma, f64* sestpr, f64* s, f64* c) {
    dlaic1(*job, *j, x, *sest, w, *gamma, sestpr, s, c);
}

void dlaln2_(INT* ltrans, INT* na, INT* nw, f64* smin, f64* ca, f64* A, INT* lda, f64* d1, f64* d2, f64* B, INT* ldb, f64* wr, f64* wi, f64* X, INT* ldx, f64* scale, f64* xnorm, INT* info) {
    dlaln2(*ltrans, *na, *nw, *smin, *ca, A, *lda, *d1, *d2, B, *ldb, *wr, *wi, X, *ldx, scale, xnorm, info);
}

void dlals0_(INT* icompq, INT* nl, INT* nr, INT* sqre, INT* nrhs, f64* B, INT* ldb, f64* BX, INT* ldbx, INT* perm, INT* givptr, INT* givcol, INT* ldgcol, f64* givnum, INT* ldgnum, f64* poles, f64* difl, f64* difr, f64* Z, INT* k, f64* c, f64* s, f64* work, INT* info) {
    dlals0(*icompq, *nl, *nr, *sqre, *nrhs, B, *ldb, BX, *ldbx, perm, *givptr, givcol, *ldgcol, givnum, *ldgnum, poles, difl, difr, Z, *k, *c, *s, work, info);
}

void dlalsa_(INT* icompq, INT* smlsiz, INT* n, INT* nrhs, f64* B, INT* ldb, f64* BX, INT* ldbx, f64* U, INT* ldu, f64* VT, INT* K, f64* difl, f64* difr, f64* Z, f64* poles, INT* givptr, INT* givcol, INT* ldgcol, INT* perm, f64* givnum, f64* C, f64* S, f64* work, INT* iwork, INT* info) {
    dlalsa(*icompq, *smlsiz, *n, *nrhs, B, *ldb, BX, *ldbx, U, *ldu, VT, K, difl, difr, Z, poles, givptr, givcol, *ldgcol, perm, givnum, C, S, work, iwork, info);
}

void dlalsd_(char* uplo, INT* smlsiz, INT* n, INT* nrhs, f64* D, f64* E, f64* B, INT* ldb, f64* rcond, INT* rank, f64* work, INT* iwork, INT* info) {
    dlalsd(uplo, *smlsiz, *n, *nrhs, D, E, B, *ldb, *rcond, rank, work, iwork, info);
}

void dlamrg_(INT* n1, INT* n2, f64* A, INT* dtrd1, INT* dtrd2, INT* index) {
    dlamrg(*n1, *n2, A, *dtrd1, *dtrd2, index);
}

void dlamswlq_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dlamswlq(side, trans, *m, *n, *k, *mb, *nb, A, *lda, T, *ldt, C, *ldc, work, *lwork, info);
}

void dlamtsqr_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dlamtsqr(side, trans, *m, *n, *k, *mb, *nb, A, *lda, T, *ldt, C, *ldc, work, *lwork, info);
}

void dlanv2_(f64* a, f64* b, f64* c, f64* d, f64* rt1r, f64* rt1i, f64* rt2r, f64* rt2i, f64* cs, f64* sn) {
    dlanv2(a, b, c, d, rt1r, rt1i, rt2r, rt2i, cs, sn);
}

void dlaorhr_col_getrfnp_(INT* m, INT* n, f64* A, INT* lda, f64* D, INT* info) {
    dlaorhr_col_getrfnp(*m, *n, A, *lda, D, info);
}

void dlaorhr_col_getrfnp2_(INT* m, INT* n, f64* A, INT* lda, f64* D, INT* info) {
    dlaorhr_col_getrfnp2(*m, *n, A, *lda, D, info);
}

void dlapll_(INT* n, f64* x, INT* incx, f64* y, INT* incy, f64* ssmin) {
    dlapll(*n, x, *incx, y, *incy, ssmin);
}

void dlapmr_(INT* forwrd, INT* m, INT* n, f64* X, INT* ldx, INT* K) {
    dlapmr(*forwrd, *m, *n, X, *ldx, K);
}

void dlapmt_(INT* forwrd, INT* m, INT* n, f64* X, INT* ldx, INT* K) {
    dlapmt(*forwrd, *m, *n, X, *ldx, K);
}

void dlaqgb_(INT* m, INT* n, INT* kl, INT* ku, f64* AB, INT* ldab, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, char* equed) {
    dlaqgb(*m, *n, *kl, *ku, AB, *ldab, R, C, *rowcnd, *colcnd, *amax, equed);
}

void dlaqge_(INT* m, INT* n, f64* A, INT* lda, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, char* equed) {
    dlaqge(*m, *n, A, *lda, R, C, *rowcnd, *colcnd, *amax, equed);
}

void dlaqp2_(INT* m, INT* n, INT* offset, f64* A, INT* lda, INT* jpvt, f64* tau, f64* vn1, f64* vn2, f64* work) {
    dlaqp2(*m, *n, *offset, A, *lda, jpvt, tau, vn1, vn2, work);
    if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
}

void dlaqp2rk_(INT* m, INT* n, INT* nrhs, INT* ioffset, INT* kmax, f64* abstol, f64* reltol, INT* kp1, f64* maxc2nrm, f64* A, INT* lda, INT* K, f64* maxc2nrmk, f64* relmaxc2nrmk, INT* jpiv, f64* tau, f64* vn1, f64* vn2, f64* work, INT* info) {
    dlaqp2rk(*m, *n, *nrhs, *ioffset, *kmax, *abstol, *reltol, *kp1, *maxc2nrm, A, *lda, K, maxc2nrmk, relmaxc2nrmk, jpiv, tau, vn1, vn2, work, info);
}

void dlaqp3rk_(INT* m, INT* n, INT* nrhs, INT* ioffset, INT* nb, f64* abstol, f64* reltol, INT* kp1, f64* maxc2nrm, f64* A, INT* lda, INT* done, INT* KB, f64* maxc2nrmk, f64* relmaxc2nrmk, INT* jpiv, f64* tau, f64* vn1, f64* vn2, f64* auxv, f64* F, INT* ldf, INT* iwork, INT* info) {
    dlaqp3rk(*m, *n, *nrhs, *ioffset, nb, *abstol, *reltol, *kp1, *maxc2nrm, A, *lda, done, KB, maxc2nrmk, relmaxc2nrmk, jpiv, tau, vn1, vn2, auxv, F, *ldf, iwork, info);
}

void dlaqps_(INT* m, INT* n, INT* offset, INT* nb, INT* kb, f64* A, INT* lda, INT* jpvt, f64* tau, f64* vn1, f64* vn2, f64* auxv, f64* F, INT* ldf) {
    dlaqps(*m, *n, *offset, *nb, kb, A, *lda, jpvt, tau, vn1, vn2, auxv, F, *ldf);
    if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
}

void dlaqr0_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, f64* H, INT* ldh, f64* wr, f64* wi, INT* iloz, INT* ihiz, f64* Z, INT* ldz, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dlaqr0(*wantt, *wantz, *n, _ilo, _ihi, H, *ldh, wr, wi, *iloz, *ihiz, Z, *ldz, work, *lwork, info);
}

void dlaqr1_(INT* n, f64* H, INT* ldh, f64* sr1, f64* si1, f64* sr2, f64* si2, f64* v) {
    dlaqr1(*n, H, *ldh, *sr1, *si1, *sr2, *si2, v);
}

void dlaqr2_(INT* wantt, INT* wantz, INT* n, INT* ktop, INT* kbot, INT* nw, f64* H, INT* ldh, INT* iloz, INT* ihiz, f64* Z, INT* ldz, INT* ns, INT* nd, f64* sr, f64* si, f64* V, INT* ldv, INT* nh, f64* T, INT* ldt, INT* nv, f64* WV, INT* ldwv, f64* work, INT* lwork) {
    dlaqr2(*wantt, *wantz, *n, *ktop, *kbot, *nw, H, *ldh, *iloz, *ihiz, Z, *ldz, ns, nd, sr, si, V, *ldv, *nh, T, *ldt, *nv, WV, *ldwv, work, *lwork);
}

void dlaqr3_(INT* wantt, INT* wantz, INT* n, INT* ktop, INT* kbot, INT* nw, f64* H, INT* ldh, INT* iloz, INT* ihiz, f64* Z, INT* ldz, INT* ns, INT* nd, f64* sr, f64* si, f64* V, INT* ldv, INT* nh, f64* T, INT* ldt, INT* nv, f64* WV, INT* ldwv, f64* work, INT* lwork) {
    dlaqr3(*wantt, *wantz, *n, *ktop, *kbot, *nw, H, *ldh, *iloz, *ihiz, Z, *ldz, ns, nd, sr, si, V, *ldv, *nh, T, *ldt, *nv, WV, *ldwv, work, *lwork);
}

void dlaqr4_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, f64* H, INT* ldh, f64* wr, f64* wi, INT* iloz, INT* ihiz, f64* Z, INT* ldz, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dlaqr4(*wantt, *wantz, *n, _ilo, _ihi, H, *ldh, wr, wi, *iloz, *ihiz, Z, *ldz, work, *lwork, info);
}

void dlaqr5_(INT* wantt, INT* wantz, INT* kacc22, INT* n, INT* ktop, INT* kbot, INT* nshfts, f64* sr, f64* si, f64* H, INT* ldh, INT* iloz, INT* ihiz, f64* Z, INT* ldz, f64* V, INT* ldv, f64* U, INT* ldu, INT* nv, f64* WV, INT* ldwv, INT* nh, f64* WH, INT* ldwh) {
    dlaqr5(*wantt, *wantz, *kacc22, *n, *ktop, *kbot, *nshfts, sr, si, H, *ldh, *iloz, *ihiz, Z, *ldz, V, *ldv, U, *ldu, *nv, WV, *ldwv, *nh, WH, *ldwh);
}

void dlaqsb_(char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* S, f64* scond, f64* amax, char* equed) {
    dlaqsb(uplo, *n, *kd, AB, *ldab, S, *scond, *amax, equed);
}

void dlaqsp_(char* uplo, INT* n, f64* AP, f64* S, f64* scond, f64* amax, char* equed) {
    dlaqsp(uplo, *n, AP, S, *scond, *amax, equed);
}

void dlaqsy_(char* uplo, INT* n, f64* A, INT* lda, f64* S, f64* scond, f64* amax, char* equed) {
    dlaqsy(uplo, *n, A, *lda, S, *scond, *amax, equed);
}

void dlaqtr_(INT* ltran, INT* lreal, INT* n, f64* T, INT* ldt, f64* B, f64* w, f64* scale, f64* X, f64* work, INT* info) {
    dlaqtr(*ltran, *lreal, *n, T, *ldt, B, *w, scale, X, work, info);
}

void dlaqz0_(char* wants, char* wantq, char* wantz, INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* B, INT* ldb, f64* alphar, f64* alphai, f64* beta, f64* Q, INT* ldq, f64* Z, INT* ldz, f64* work, INT* lwork, INT* rec, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dlaqz0(wants, wantq, wantz, *n, _ilo, _ihi, A, *lda, B, *ldb, alphar, alphai, beta, Q, *ldq, Z, *ldz, work, *lwork, *rec, info);
}

void dlaqz1_(f64* A, INT* lda, f64* B, INT* ldb, f64* sr1, f64* sr2, f64* si, f64* beta1, f64* beta2, f64* v) {
    dlaqz1(A, *lda, B, *ldb, *sr1, *sr2, *si, *beta1, *beta2, v);
}

void dlaqz2_(INT* ilq, INT* ilz, INT* k, INT* istartm, INT* istopm, INT* ihi, f64* A, INT* lda, f64* B, INT* ldb, INT* nq, INT* qstart, f64* Q, INT* ldq, INT* nz, INT* zstart, f64* Z, INT* ldz) {
    dlaqz2(*ilq, *ilz, *k, *istartm, *istopm, *ihi, A, *lda, B, *ldb, *nq, *qstart, Q, *ldq, *nz, *zstart, Z, *ldz);
}

void dlaqz3_(INT* ilschur, INT* ilq, INT* ilz, INT* n, INT* ilo, INT* ihi, INT* nw, f64* A, INT* lda, f64* B, INT* ldb, f64* Q, INT* ldq, f64* Z, INT* ldz, INT* ns, INT* nd, f64* alphar, f64* alphai, f64* beta, f64* QC, INT* ldqc, f64* ZC, INT* ldzc, f64* work, INT* lwork, INT* rec, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dlaqz3(*ilschur, *ilq, *ilz, *n, _ilo, _ihi, *nw, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, ns, nd, alphar, alphai, beta, QC, *ldqc, ZC, *ldzc, work, *lwork, *rec, info);
}

void dlaqz4_(INT* ilschur, INT* ilq, INT* ilz, INT* n, INT* ilo, INT* ihi, INT* nshifts, INT* nblock_desired, f64* sr, f64* si, f64* ss, f64* A, INT* lda, f64* B, INT* ldb, f64* Q, INT* ldq, f64* Z, INT* ldz, f64* QC, INT* ldqc, f64* ZC, INT* ldzc, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dlaqz4(*ilschur, *ilq, *ilz, *n, _ilo, _ihi, *nshifts, *nblock_desired, sr, si, ss, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, QC, *ldqc, ZC, *ldzc, work, *lwork, info);
}

void dlar1v_(INT* n, INT* b1, INT* bn, f64* lambda, f64* D, f64* L, f64* LD, f64* LLD, f64* pivmin, f64* gaptol, f64* Z, INT* wantnc, INT* negcnt, f64* ztz, f64* mingma, INT* r, INT* isuppz, f64* nrminv, f64* resid, f64* rqcorr, f64* work) {
    dlar1v(*n, *b1, *bn, *lambda, D, L, LD, LLD, *pivmin, *gaptol, Z, *wantnc, negcnt, ztz, mingma, r, isuppz, nrminv, resid, rqcorr, work);
}

void dlar2v_(INT* n, f64* X, f64* Y, f64* Z, INT* incx, f64* C, f64* S, INT* incc) {
    dlar2v(*n, X, Y, Z, *incx, C, S, *incc);
}

void dlarf_(char* side, INT* m, INT* n, f64* v, INT* incv, f64* tau, f64* C, INT* ldc, f64* work) {
    dlarf(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void dlarf1f_(char* side, INT* m, INT* n, f64* v, INT* incv, f64* tau, f64* C, INT* ldc, f64* work) {
    dlarf1f(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void dlarf1l_(char* side, INT* m, INT* n, f64* v, INT* incv, f64* tau, f64* C, INT* ldc, f64* work) {
    dlarf1l(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void dlarfb_gett_(char* ident, INT* m, INT* n, INT* k, f64* T, INT* ldt, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* ldwork) {
    dlarfb_gett(ident, *m, *n, *k, T, *ldt, A, *lda, B, *ldb, work, *ldwork);
}

void dlarfb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, f64* V, INT* ldv, f64* T, INT* ldt, f64* C, INT* ldc, f64* work, INT* ldwork) {
    dlarfb(side, trans, direct, storev, *m, *n, *k, V, *ldv, T, *ldt, C, *ldc, work, *ldwork);
}

void dlarfg_(INT* n, f64* alpha, f64* x, INT* incx, f64* tau) {
    dlarfg(*n, alpha, x, *incx, tau);
}

void dlarfgp_(INT* n, f64* alpha, f64* x, INT* incx, f64* tau) {
    dlarfgp(*n, alpha, x, *incx, tau);
}

void dlarft_(char* direct, char* storev, INT* n, INT* k, f64* V, INT* ldv, f64* tau, f64* T, INT* ldt) {
    dlarft(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void dlarfx_(char* side, INT* m, INT* n, f64* v, f64* tau, f64* C, INT* ldc, f64* work) {
    dlarfx(side, *m, *n, v, *tau, C, *ldc, work);
}

void dlarfy_(char* uplo, INT* n, f64* V, INT* incv, f64* tau, f64* C, INT* ldc, f64* work) {
    dlarfy(uplo, *n, V, *incv, *tau, C, *ldc, work);
}

void dlargv_(INT* n, f64* X, INT* incx, f64* Y, INT* incy, f64* C, INT* incc) {
    dlargv(*n, X, *incx, Y, *incy, C, *incc);
}

void dlarnv_(INT* idist, INT* iseed, INT* n, f64* X) {
    dlarnv(*idist, iseed, *n, X);
}

void dlarra_(INT* n, f64* D, f64* E, f64* E2, f64* spltol, f64* tnrm, INT* nsplit, INT* isplit, INT* info) {
    dlarra(*n, D, E, E2, *spltol, *tnrm, nsplit, isplit, info);
}

void dlarrb_(INT* n, f64* D, f64* lld, INT* ifirst, INT* ilast, f64* rtol1, f64* rtol2, INT* offset, f64* W, f64* wgap, f64* werr, f64* work, INT* iwork, f64* pivmin, f64* spdiam, INT* twist, INT* info) {
    dlarrb(*n, D, lld, *ifirst, *ilast, *rtol1, *rtol2, *offset, W, wgap, werr, work, iwork, *pivmin, *spdiam, *twist, info);
}

void dlarrc_(char* jobt, INT* n, f64* vl, f64* vu, f64* D, f64* E, f64* pivmin, INT* eigcnt, INT* lcnt, INT* rcnt, INT* info) {
    dlarrc(jobt, *n, *vl, *vu, D, E, *pivmin, eigcnt, lcnt, rcnt, info);
}

void dlarrd_(char* range, char* order, INT* n, f64* vl, f64* vu, INT* il, INT* iu, f64* gers, f64* reltol, f64* D, f64* E, f64* E2, f64* pivmin, INT* nsplit, INT* isplit, INT* m, f64* W, f64* werr, f64* wl, f64* wu, INT* iblock, INT* indexw, f64* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dlarrd(range, order, *n, *vl, *vu, _il, _iu, gers, *reltol, D, E, E2, *pivmin, *nsplit, isplit, m, W, werr, wl, wu, iblock, indexw, work, iwork, info);
    if (iblock) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) iblock[_i]++; }
    if (isplit) { INT _sz = *nsplit; for (INT _i = 0; _i < _sz; _i++) isplit[_i]++; }
    if (indexw) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) indexw[_i]++; }
}

void dlarre_(char* range, INT* n, f64* vl, f64* vu, INT* il, INT* iu, f64* D, f64* E, f64* E2, f64* rtol1, f64* rtol2, f64* spltol, INT* nsplit, INT* isplit, INT* m, f64* W, f64* werr, f64* wgap, INT* iblock, INT* indexw, f64* gers, f64* pivmin, f64* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dlarre(range, *n, vl, vu, _il, _iu, D, E, E2, *rtol1, *rtol2, *spltol, nsplit, isplit, m, W, werr, wgap, iblock, indexw, gers, pivmin, work, iwork, info);
    if (iblock) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) iblock[_i]++; }
    if (isplit) { INT _sz = *nsplit; for (INT _i = 0; _i < _sz; _i++) isplit[_i]++; }
    if (indexw) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) indexw[_i]++; }
}

void dlarrf_(INT* n, f64* D, f64* L, f64* LD, INT* clstrt, INT* clend, f64* W, f64* wgap, f64* werr, f64* spdiam, f64* clgapl, f64* clgapr, f64* pivmin, f64* sigma, f64* dplus, f64* lplus, f64* work, INT* info) {
    dlarrf(*n, D, L, LD, *clstrt, *clend, W, wgap, werr, *spdiam, *clgapl, *clgapr, *pivmin, sigma, dplus, lplus, work, info);
}

void dlarrj_(INT* n, f64* D, f64* E2, INT* ifirst, INT* ilast, f64* rtol, INT* offset, f64* W, f64* werr, f64* work, INT* iwork, f64* pivmin, f64* spdiam, INT* info) {
    dlarrj(*n, D, E2, *ifirst, *ilast, *rtol, *offset, W, werr, work, iwork, *pivmin, *spdiam, info);
}

void dlarrk_(INT* n, INT* iw, f64* gl, f64* gu, f64* D, f64* E2, f64* pivmin, f64* reltol, f64* w, f64* werr, INT* info) {
    dlarrk(*n, *iw, *gl, *gu, D, E2, *pivmin, *reltol, w, werr, info);
}

void dlarrr_(INT* n, f64* D, f64* E, INT* info) {
    dlarrr(*n, D, E, info);
}

void dlarrv_(INT* n, f64* vl, f64* vu, f64* D, f64* L, f64* pivmin, INT* isplit, INT* m, INT* dol, INT* dou, f64* minrgp, f64* rtol1, f64* rtol2, f64* W, f64* werr, f64* wgap, INT* iblock, INT* indexw, f64* gers, f64* Z, INT* ldz, INT* isuppz, f64* work, INT* iwork, INT* info) {
    INT* _isplit0 = NULL;
    INT* _iblock0 = NULL;
    INT* _indexw0 = NULL;
    if (isplit) {
        INT _isplit_sz = *n;
        _isplit0 = (INT*)malloc(_isplit_sz * sizeof(INT));
        for (INT _i = 0; _i < _isplit_sz; _i++) _isplit0[_i] = (isplit[_i] > 0) ? isplit[_i] - 1 : isplit[_i];
    }
    if (iblock) {
        INT _iblock_sz = *m;
        _iblock0 = (INT*)malloc(_iblock_sz * sizeof(INT));
        for (INT _i = 0; _i < _iblock_sz; _i++) _iblock0[_i] = (iblock[_i] > 0) ? iblock[_i] - 1 : iblock[_i];
    }
    if (indexw) {
        INT _indexw_sz = *m;
        _indexw0 = (INT*)malloc(_indexw_sz * sizeof(INT));
        for (INT _i = 0; _i < _indexw_sz; _i++) _indexw0[_i] = (indexw[_i] > 0) ? indexw[_i] - 1 : indexw[_i];
    }
    dlarrv(*n, *vl, *vu, D, L, *pivmin, _isplit0, *m, *dol, *dou, *minrgp, *rtol1, *rtol2, W, werr, wgap, _iblock0, _indexw0, gers, Z, *ldz, isuppz, work, iwork, info);
    if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    free(_isplit0);
    free(_iblock0);
    free(_indexw0);
}

void dlartg_(f64* f, f64* g, f64* c, f64* s, f64* r) {
    dlartg(*f, *g, c, s, r);
}

void dlartgp_(f64* f, f64* g, f64* cs, f64* sn, f64* r) {
    dlartgp(*f, *g, cs, sn, r);
}

void dlartgs_(f64* x, f64* y, f64* sigma, f64* cs, f64* sn) {
    dlartgs(*x, *y, *sigma, cs, sn);
}

void dlartv_(INT* n, f64* X, INT* incx, f64* Y, INT* incy, f64* C, f64* S, INT* incc) {
    dlartv(*n, X, *incx, Y, *incy, C, S, *incc);
}

void dlaruv_(INT* iseed, INT* n, f64* X) {
    dlaruv(iseed, *n, X);
}

void dlarz_(char* side, INT* m, INT* n, INT* l, f64* v, INT* incv, f64* tau, f64* C, INT* ldc, f64* work) {
    dlarz(side, *m, *n, *l, v, *incv, *tau, C, *ldc, work);
}

void dlarzb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, INT* l, f64* V, INT* ldv, f64* T, INT* ldt, f64* C, INT* ldc, f64* work, INT* ldwork) {
    dlarzb(side, trans, direct, storev, *m, *n, *k, *l, V, *ldv, T, *ldt, C, *ldc, work, *ldwork);
}

void dlarzt_(char* direct, char* storev, INT* n, INT* k, f64* V, INT* ldv, f64* tau, f64* T, INT* ldt) {
    dlarzt(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void dlas2_(f64* f, f64* g, f64* h, f64* ssmin, f64* ssmax) {
    dlas2(*f, *g, *h, ssmin, ssmax);
}

void dlascl_(char* type, INT* kl, INT* ku, f64* cfrom, f64* cto, INT* m, INT* n, f64* A, INT* lda, INT* info) {
    dlascl(type, *kl, *ku, *cfrom, *cto, *m, *n, A, *lda, info);
}

void dlasd0_(INT* n, INT* sqre, f64* D, f64* E, f64* U, INT* ldu, f64* VT, INT* ldvt, INT* smlsiz, INT* IWORK, f64* work, INT* info) {
    dlasd0(*n, *sqre, D, E, U, *ldu, VT, *ldvt, *smlsiz, IWORK, work, info);
}

void dlasd1_(INT* nl, INT* nr, INT* sqre, f64* D, f64* alpha, f64* beta, f64* U, INT* ldu, f64* VT, INT* ldvt, INT* IDXQ, INT* IWORK, f64* work, INT* info) {
    dlasd1(*nl, *nr, *sqre, D, alpha, beta, U, *ldu, VT, *ldvt, IDXQ, IWORK, work, info);
}

void dlasd2_(INT* nl, INT* nr, INT* sqre, INT* k, f64* D, f64* Z, f64* alpha, f64* beta, f64* U, INT* ldu, f64* VT, INT* ldvt, f64* DSIGMA, f64* U2, INT* ldu2, f64* VT2, INT* ldvt2, INT* IDXP, INT* IDX, INT* IDXC, INT* IDXQ, INT* COLTYP, INT* info) {
    dlasd2(*nl, *nr, *sqre, k, D, Z, *alpha, *beta, U, *ldu, VT, *ldvt, DSIGMA, U2, *ldu2, VT2, *ldvt2, IDXP, IDX, IDXC, IDXQ, COLTYP, info);
}

void dlasd3_(INT* nl, INT* nr, INT* sqre, INT* k, f64* D, f64* Q, INT* ldq, f64* DSIGMA, f64* U, INT* ldu, f64* U2, INT* ldu2, f64* VT, INT* ldvt, f64* VT2, INT* ldvt2, INT* IDXC, INT* CTOT, f64* Z, INT* info) {
    dlasd3(*nl, *nr, *sqre, *k, D, Q, *ldq, DSIGMA, U, *ldu, U2, *ldu2, VT, *ldvt, VT2, *ldvt2, IDXC, CTOT, Z, info);
}

void dlasd4_(INT* n, INT* i, f64* D, f64* Z, f64* delta, f64* rho, f64* sigma, f64* work, INT* info) {
    INT _i = *i - 1;
    dlasd4(*n, _i, D, Z, delta, *rho, sigma, work, info);
}

void dlasd5_(INT* i, f64* D, f64* Z, f64* delta, f64* rho, f64* dsigma, f64* work) {
    INT _i = *i - 1;
    dlasd5(_i, D, Z, delta, *rho, dsigma, work);
}

void dlasd6_(INT* icompq, INT* nl, INT* nr, INT* sqre, f64* D, f64* VF, f64* VL, f64* alpha, f64* beta, INT* IDXQ, INT* PERM, INT* givptr, INT* GIVCOL, INT* ldgcol, f64* GIVNUM, INT* ldgnum, f64* POLES, f64* DIFL, f64* DIFR, f64* Z, INT* k, f64* c, f64* s, f64* work, INT* IWORK, INT* info) {
    dlasd6(*icompq, *nl, *nr, *sqre, D, VF, VL, alpha, beta, IDXQ, PERM, givptr, GIVCOL, *ldgcol, GIVNUM, *ldgnum, POLES, DIFL, DIFR, Z, k, c, s, work, IWORK, info);
}

void dlasd7_(INT* icompq, INT* nl, INT* nr, INT* sqre, INT* k, f64* D, f64* Z, f64* ZW, f64* VF, f64* VFW, f64* VL, f64* VLW, f64* alpha, f64* beta, f64* DSIGMA, INT* IDX, INT* IDXP, INT* IDXQ, INT* PERM, INT* givptr, INT* GIVCOL, INT* ldgcol, f64* GIVNUM, INT* ldgnum, f64* c, f64* s, INT* info) {
    dlasd7(*icompq, *nl, *nr, *sqre, k, D, Z, ZW, VF, VFW, VL, VLW, *alpha, *beta, DSIGMA, IDX, IDXP, IDXQ, PERM, givptr, GIVCOL, *ldgcol, GIVNUM, *ldgnum, c, s, info);
}

void dlasd8_(INT* icompq, INT* k, f64* D, f64* Z, f64* VF, f64* VL, f64* DIFL, f64* DIFR, INT* lddifr, f64* DSIGMA, f64* work, INT* info) {
    dlasd8(*icompq, *k, D, Z, VF, VL, DIFL, DIFR, *lddifr, DSIGMA, work, info);
}

void dlasda_(INT* icompq, INT* smlsiz, INT* n, INT* sqre, f64* D, f64* E, f64* U, INT* ldu, f64* VT, INT* K, f64* DIFL, f64* DIFR, f64* Z, f64* POLES, INT* GIVPTR, INT* GIVCOL, INT* ldgcol, INT* PERM, f64* GIVNUM, f64* C, f64* S, f64* work, INT* IWORK, INT* info) {
    dlasda(*icompq, *smlsiz, *n, *sqre, D, E, U, *ldu, VT, K, DIFL, DIFR, Z, POLES, GIVPTR, GIVCOL, *ldgcol, PERM, GIVNUM, C, S, work, IWORK, info);
}

void dlasdq_(char* uplo, INT* sqre, INT* n, INT* ncvt, INT* nru, INT* ncc, f64* D, f64* E, f64* VT, INT* ldvt, f64* U, INT* ldu, f64* C, INT* ldc, f64* work, INT* info) {
    dlasdq(uplo, *sqre, *n, *ncvt, *nru, *ncc, D, E, VT, *ldvt, U, *ldu, C, *ldc, work, info);
}

void dlasdt_(INT* n, INT* lvl, INT* nd, INT* inode, INT* ndiml, INT* ndimr, INT* msub) {
    dlasdt(*n, lvl, nd, inode, ndiml, ndimr, *msub);
}

void dlaset_(char* uplo, INT* m, INT* n, f64* alpha, f64* beta, f64* A, INT* lda) {
    dlaset(uplo, *m, *n, *alpha, *beta, A, *lda);
}

void dlasq1_(INT* n, f64* D, f64* E, f64* work, INT* info) {
    dlasq1(*n, D, E, work, info);
}

void dlasq2_(INT* n, f64* Z, INT* info) {
    dlasq2(*n, Z, info);
}

void dlasq3_(INT* i0, INT* n0, f64* Z, INT* pp, f64* dmin, f64* sigma, f64* desig, INT* nfail, INT* iter, INT* ndiv, INT* ieee, INT* ttype, f64* dmin1, f64* dmin2, f64* dn, f64* dn1, f64* dn2, f64* g, f64* tau) {
    dlasq3(*i0, n0, Z, pp, dmin, sigma, desig, nfail, iter, ndiv, *ieee, ttype, dmin1, dmin2, dn, dn1, dn2, g, tau);
}

void dlasq4_(INT* i0, INT* n0, f64* Z, INT* pp, INT* n0in, f64* dmin, f64* dmin1, f64* dmin2, f64* dn, f64* dn1, f64* dn2, f64* tau, INT* ttype, f64* g) {
    dlasq4(*i0, *n0, Z, *pp, *n0in, *dmin, *dmin1, *dmin2, *dn, *dn1, *dn2, tau, ttype, g);
}

void dlasq5_(INT* i0, INT* n0, f64* Z, INT* pp, f64* tau, f64* sigma, f64* dmin, f64* dmin1, f64* dmin2, f64* dn, f64* dnm1, f64* dnm2, INT* ieee, f64* eps) {
    dlasq5(*i0, *n0, Z, *pp, *tau, *sigma, dmin, dmin1, dmin2, dn, dnm1, dnm2, *ieee, *eps);
}

void dlasq6_(INT* i0, INT* n0, f64* Z, INT* pp, f64* dmin, f64* dmin1, f64* dmin2, f64* dn, f64* dnm1, f64* dnm2) {
    dlasq6(*i0, *n0, Z, *pp, dmin, dmin1, dmin2, dn, dnm1, dnm2);
}

void dlasr_(char* side, char* pivot, char* direct, INT* m, INT* n, f64* C_rot, f64* S_rot, f64* A, INT* lda) {
    dlasr(side, pivot, direct, *m, *n, C_rot, S_rot, A, *lda);
}

void dlasrt_(char* id, INT* n, f64* D, INT* info) {
    dlasrt(id, *n, D, info);
}

void dlassq_(INT* n, f64* X, INT* incx, f64* scale, f64* sumsq) {
    dlassq(*n, X, *incx, scale, sumsq);
}

void dlasv2_(f64* f, f64* g, f64* h, f64* ssmin, f64* ssmax, f64* snr, f64* csr, f64* snl, f64* csl) {
    dlasv2(*f, *g, *h, ssmin, ssmax, snr, csr, snl, csl);
}

void dlaswlq_(INT* m, INT* n, INT* mb, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* lwork, INT* info) {
    dlaswlq(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void dlaswp_(INT* n, f64* A, INT* lda, INT* k1, INT* k2, INT* ipiv, INT* incx) {
    INT _k1 = *k1 - 1;
    INT _k2 = *k2 - 1;
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *k2;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dlaswp(*n, A, *lda, _k1, _k2, _ipiv0, *incx);
    free(_ipiv0);
}

void dlasy2_(INT* ltranl, INT* ltranr, INT* isgn, INT* n1, INT* n2, f64* TL, INT* ldtl, f64* TR, INT* ldtr, f64* B, INT* ldb, f64* scale, f64* X, INT* ldx, f64* xnorm, INT* info) {
    dlasy2(*ltranl, *ltranr, *isgn, *n1, *n2, TL, *ldtl, TR, *ldtr, B, *ldb, scale, X, *ldx, xnorm, info);
}

void dlasyf_aa_(char* uplo, INT* j1, INT* m, INT* nb, f64* A, INT* lda, INT* ipiv, f64* H, INT* ldh, f64* work) {
    dlasyf_aa(uplo, *j1, *m, *nb, A, *lda, ipiv, H, *ldh, work);
}

void dlasyf_rk_(char* uplo, INT* n, INT* nb, INT* kb, f64* A, INT* lda, f64* E, INT* ipiv, f64* W, INT* ldw, INT* info) {
    dlasyf_rk(uplo, *n, *nb, kb, A, *lda, E, ipiv, W, *ldw, info);
}

void dlasyf_rook_(char* uplo, INT* n, INT* nb, INT* kb, f64* A, INT* lda, INT* ipiv, f64* W, INT* ldw, INT* info) {
    dlasyf_rook(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void dlasyf_(char* uplo, INT* n, INT* nb, INT* kb, f64* A, INT* lda, INT* ipiv, f64* W, INT* ldw, INT* info) {
    dlasyf(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void dlat2s_(char* uplo, INT* n, f64* A, INT* lda, float* SA, INT* ldsa, INT* info) {
    dlat2s(uplo, *n, A, *lda, SA, *ldsa, info);
}

void dlatbs_(char* uplo, char* trans, char* diag, char* normin, INT* n, INT* kd, f64* AB, INT* ldab, f64* X, f64* scale, f64* cnorm, INT* info) {
    dlatbs(uplo, trans, diag, normin, *n, *kd, AB, *ldab, X, scale, cnorm, info);
}

void dlatdf_(INT* ijob, INT* n, f64* Z, INT* ldz, f64* rhs, f64* rdsum, f64* rdscal, INT* ipiv, INT* jpiv) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dlatdf(*ijob, *n, Z, *ldz, rhs, rdsum, rdscal, _ipiv0, jpiv);
    free(_ipiv0);
}

void dlatps_(char* uplo, char* trans, char* diag, char* normin, INT* n, f64* AP, f64* X, f64* scale, f64* cnorm, INT* info) {
    dlatps(uplo, trans, diag, normin, *n, AP, X, scale, cnorm, info);
}

void dlatrd_(char* uplo, INT* n, INT* nb, f64* A, INT* lda, f64* E, f64* tau, f64* W, INT* ldw) {
    dlatrd(uplo, *n, *nb, A, *lda, E, tau, W, *ldw);
}

void dlatrs_(char* uplo, char* trans, char* diag, char* normin, INT* n, f64* A, INT* lda, f64* X, f64* scale, f64* cnorm, INT* info) {
    dlatrs(uplo, trans, diag, normin, *n, A, *lda, X, scale, cnorm, info);
}

void dlatrs3_(char* uplo, char* trans, char* diag, char* normin, INT* n, INT* nrhs, f64* A, INT* lda, f64* X, INT* ldx, f64* scale, f64* cnorm, f64* work, INT* lwork, INT* info) {
    dlatrs3(uplo, trans, diag, normin, *n, *nrhs, A, *lda, X, *ldx, scale, cnorm, work, *lwork, info);
}

void dlatrz_(INT* m, INT* n, INT* l, f64* A, INT* lda, f64* tau, f64* work) {
    dlatrz(*m, *n, *l, A, *lda, tau, work);
}

void dlatsqr_(INT* m, INT* n, INT* mb, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* lwork, INT* info) {
    dlatsqr(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void dlauu2_(char* uplo, INT* n, f64* A, INT* lda, INT* info) {
    dlauu2(uplo, *n, A, *lda, info);
}

void dlauum_(char* uplo, INT* n, f64* A, INT* lda, INT* info) {
    dlauum(uplo, *n, A, *lda, info);
}

void dopgtr_(char* uplo, INT* n, f64* AP, f64* tau, f64* Q, INT* ldq, f64* work, INT* info) {
    dopgtr(uplo, *n, AP, tau, Q, *ldq, work, info);
}

void dopmtr_(char* side, char* uplo, char* trans, INT* m, INT* n, f64* AP, f64* tau, f64* C, INT* ldc, f64* work, INT* info) {
    dopmtr(side, uplo, trans, *m, *n, AP, tau, C, *ldc, work, info);
}

void dorbdb_(char* trans, char* signs, INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X12, INT* ldx12, f64* X21, INT* ldx21, f64* X22, INT* ldx22, f64* theta, f64* phi, f64* taup1, f64* taup2, f64* tauq1, f64* tauq2, f64* work, INT* lwork, INT* info) {
    dorbdb(trans, signs, *m, *p, *q, X11, *ldx11, X12, *ldx12, X21, *ldx21, X22, *ldx22, theta, phi, taup1, taup2, tauq1, tauq2, work, *lwork, info);
}

void dorbdb1_(INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X21, INT* ldx21, f64* theta, f64* phi, f64* taup1, f64* taup2, f64* tauq1, f64* work, INT* lwork, INT* info) {
    dorbdb1(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void dorbdb2_(INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X21, INT* ldx21, f64* theta, f64* phi, f64* taup1, f64* taup2, f64* tauq1, f64* work, INT* lwork, INT* info) {
    dorbdb2(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void dorbdb3_(INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X21, INT* ldx21, f64* theta, f64* phi, f64* taup1, f64* taup2, f64* tauq1, f64* work, INT* lwork, INT* info) {
    dorbdb3(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void dorbdb4_(INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X21, INT* ldx21, f64* theta, f64* phi, f64* taup1, f64* taup2, f64* tauq1, f64* phantom, f64* work, INT* lwork, INT* info) {
    dorbdb4(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, phantom, work, *lwork, info);
}

void dorbdb5_(INT* m1, INT* m2, INT* n, f64* X1, INT* incx1, f64* X2, INT* incx2, f64* Q1, INT* ldq1, f64* Q2, INT* ldq2, f64* work, INT* lwork, INT* info) {
    dorbdb5(*m1, *m2, *n, X1, *incx1, X2, *incx2, Q1, *ldq1, Q2, *ldq2, work, *lwork, info);
}

void dorbdb6_(INT* m1, INT* m2, INT* n, f64* X1, INT* incx1, f64* X2, INT* incx2, f64* Q1, INT* ldq1, f64* Q2, INT* ldq2, f64* work, INT* lwork, INT* info) {
    dorbdb6(*m1, *m2, *n, X1, *incx1, X2, *incx2, Q1, *ldq1, Q2, *ldq2, work, *lwork, info);
}

void dorcsd_(char* jobu1, char* jobu2, char* jobv1t, char* jobv2t, char* trans, char* signs, INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X12, INT* ldx12, f64* X21, INT* ldx21, f64* X22, INT* ldx22, f64* theta, f64* U1, INT* ldu1, f64* U2, INT* ldu2, f64* V1T, INT* ldv1t, f64* V2T, INT* ldv2t, f64* work, INT* lwork, INT* iwork, INT* info) {
    dorcsd(jobu1, jobu2, jobv1t, jobv2t, trans, signs, *m, *p, *q, X11, *ldx11, X12, *ldx12, X21, *ldx21, X22, *ldx22, theta, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, V2T, *ldv2t, work, *lwork, iwork, info);
}

void dorcsd2by1_(char* jobu1, char* jobu2, char* jobv1t, INT* m, INT* p, INT* q, f64* X11, INT* ldx11, f64* X21, INT* ldx21, f64* theta, f64* U1, INT* ldu1, f64* U2, INT* ldu2, f64* V1T, INT* ldv1t, f64* work, INT* lwork, INT* iwork, INT* info) {
    dorcsd2by1(jobu1, jobu2, jobv1t, *m, *p, *q, X11, *ldx11, X21, *ldx21, theta, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, work, *lwork, iwork, info);
}

void dorg2l_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dorg2l(*m, *n, *k, A, *lda, tau, work, info);
}

void dorg2r_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dorg2r(*m, *n, *k, A, *lda, tau, work, info);
}

void dorgbr_(char* vect, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dorgbr(vect, *m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void dorghr_(INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dorghr(*n, _ilo, _ihi, A, *lda, tau, work, *lwork, info);
}

void dorgl2_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dorgl2(*m, *n, *k, A, *lda, tau, work, info);
}

void dorglq_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dorglq(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void dorgql_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dorgql(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void dorgqr_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dorgqr(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void dorgr2_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* info) {
    dorgr2(*m, *n, *k, A, *lda, tau, work, info);
}

void dorgrq_(INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dorgrq(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void dorgtr_(char* uplo, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dorgtr(uplo, *n, A, *lda, tau, work, *lwork, info);
}

void dorgtsqr_row_(INT* m, INT* n, INT* mb, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* lwork, INT* info) {
    dorgtsqr_row(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void dorgtsqr_(INT* m, INT* n, INT* mb, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* work, INT* lwork, INT* info) {
    dorgtsqr(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void dorhr_col_(INT* m, INT* n, INT* nb, f64* A, INT* lda, f64* T, INT* ldt, f64* D, INT* info) {
    dorhr_col(*m, *n, *nb, A, *lda, T, *ldt, D, info);
}

void dorm22_(char* side, char* trans, INT* m, INT* n, INT* n1, INT* n2, f64* Q, INT* ldq, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dorm22(side, trans, *m, *n, *n1, *n2, Q, *ldq, C, *ldc, work, *lwork, info);
}

void dorm2l_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* info) {
    dorm2l(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void dorm2r_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* info) {
    dorm2r(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void dormbr_(char* vect, char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormbr(vect, side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dormhr_(char* side, char* trans, INT* m, INT* n, INT* ilo, INT* ihi, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    dormhr(side, trans, *m, *n, _ilo, _ihi, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dorml2_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* info) {
    dorml2(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void dormlq_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormlq(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dormql_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormql(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dormqr_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormqr(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dormr2_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* info) {
    dormr2(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void dormr3_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* info) {
    dormr3(side, trans, *m, *n, *k, *l, A, *lda, tau, C, *ldc, work, info);
}

void dormrq_(char* side, char* trans, INT* m, INT* n, INT* k, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormrq(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dormrz_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormrz(side, trans, *m, *n, *k, *l, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dormtr_(char* side, char* uplo, char* trans, INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* C, INT* ldc, f64* work, INT* lwork, INT* info) {
    dormtr(side, uplo, trans, *m, *n, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void dpbcon_(char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    dpbcon(uplo, *n, *kd, AB, *ldab, *anorm, rcond, work, iwork, info);
}

void dpbequ_(char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* S, f64* scond, f64* amax, INT* info) {
    dpbequ(uplo, *n, *kd, AB, *ldab, S, scond, amax, info);
}

void dpbrfs_(char* uplo, INT* n, INT* kd, INT* nrhs, f64* AB, INT* ldab, f64* AFB, INT* ldafb, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dpbrfs(uplo, *n, *kd, *nrhs, AB, *ldab, AFB, *ldafb, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void dpbstf_(char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, INT* info) {
    dpbstf(uplo, *n, *kd, AB, *ldab, info);
}

void dpbsv_(char* uplo, INT* n, INT* kd, INT* nrhs, f64* AB, INT* ldab, f64* B, INT* ldb, INT* info) {
    dpbsv(uplo, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void dpbsvx_(char* fact, char* uplo, INT* n, INT* kd, INT* nrhs, f64* AB, INT* ldab, f64* AFB, INT* ldafb, char* equed, f64* S, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dpbsvx(fact, uplo, *n, *kd, *nrhs, AB, *ldab, AFB, *ldafb, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
}

void dpbtf2_(char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, INT* info) {
    dpbtf2(uplo, *n, *kd, AB, *ldab, info);
}

void dpbtrf_(char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, INT* info) {
    dpbtrf(uplo, *n, *kd, AB, *ldab, info);
}

void dpbtrs_(char* uplo, INT* n, INT* kd, INT* nrhs, f64* AB, INT* ldab, f64* B, INT* ldb, INT* info) {
    dpbtrs(uplo, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void dpftrf_(char* transr, char* uplo, INT* n, f64* A, INT* info) {
    dpftrf(transr, uplo, *n, A, info);
}

void dpftri_(char* transr, char* uplo, INT* n, f64* A, INT* info) {
    dpftri(transr, uplo, *n, A, info);
}

void dpftrs_(char* transr, char* uplo, INT* n, INT* nrhs, f64* A, f64* B, INT* ldb, INT* info) {
    dpftrs(transr, uplo, *n, *nrhs, A, B, *ldb, info);
}

void dpocon_(char* uplo, INT* n, f64* A, INT* lda, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    dpocon(uplo, *n, A, *lda, *anorm, rcond, work, iwork, info);
}

void dpoequ_(INT* n, f64* A, INT* lda, f64* S, f64* scond, f64* amax, INT* info) {
    dpoequ(*n, A, *lda, S, scond, amax, info);
}

void dpoequb_(INT* n, f64* A, INT* lda, f64* S, f64* scond, f64* amax, INT* info) {
    dpoequb(*n, A, *lda, S, scond, amax, info);
}

void dporfs_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* AF, INT* ldaf, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dporfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void dposv_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, INT* info) {
    dposv(uplo, *n, *nrhs, A, *lda, B, *ldb, info);
}

void dposvx_(char* fact, char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* AF, INT* ldaf, char* equed, f64* S, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dposvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
}

void dpotf2_(char* uplo, INT* n, f64* A, INT* lda, INT* info) {
    dpotf2(uplo, *n, A, *lda, info);
}

void dpotrf_(char* uplo, INT* n, f64* A, INT* lda, INT* info) {
    dpotrf(uplo, *n, A, *lda, info);
}

void dpotrf2_(char* uplo, INT* n, f64* A, INT* lda, INT* info) {
    dpotrf2(uplo, *n, A, *lda, info);
}

void dpotri_(char* uplo, INT* n, f64* A, INT* lda, INT* info) {
    dpotri(uplo, *n, A, *lda, info);
}

void dpotrs_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, INT* info) {
    dpotrs(uplo, *n, *nrhs, A, *lda, B, *ldb, info);
}

void dppcon_(char* uplo, INT* n, f64* AP, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    dppcon(uplo, *n, AP, *anorm, rcond, work, iwork, info);
}

void dppequ_(char* uplo, INT* n, f64* AP, f64* S, f64* scond, f64* amax, INT* info) {
    dppequ(uplo, *n, AP, S, scond, amax, info);
}

void dpprfs_(char* uplo, INT* n, INT* nrhs, f64* AP, f64* AFP, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dpprfs(uplo, *n, *nrhs, AP, AFP, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void dppsv_(char* uplo, INT* n, INT* nrhs, f64* AP, f64* B, INT* ldb, INT* info) {
    dppsv(uplo, *n, *nrhs, AP, B, *ldb, info);
}

void dppsvx_(char* fact, char* uplo, INT* n, INT* nrhs, f64* AP, f64* AFP, char* equed, f64* S, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dppsvx(fact, uplo, *n, *nrhs, AP, AFP, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
}

void dpptrf_(char* uplo, INT* n, f64* AP, INT* info) {
    dpptrf(uplo, *n, AP, info);
}

void dpptri_(char* uplo, INT* n, f64* AP, INT* info) {
    dpptri(uplo, *n, AP, info);
}

void dpptrs_(char* uplo, INT* n, INT* nrhs, f64* AP, f64* B, INT* ldb, INT* info) {
    dpptrs(uplo, *n, *nrhs, AP, B, *ldb, info);
}

void dpstf2_(char* uplo, INT* n, f64* A, INT* lda, INT* piv, INT* rank, f64* tol, f64* work, INT* info) {
    dpstf2(uplo, *n, A, *lda, piv, rank, *tol, work, info);
    if (piv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (piv[_i] >= 0) piv[_i]++; } }
}

void dpstrf_(char* uplo, INT* n, f64* A, INT* lda, INT* piv, INT* rank, f64* tol, f64* work, INT* info) {
    dpstrf(uplo, *n, A, *lda, piv, rank, *tol, work, info);
    if (piv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (piv[_i] >= 0) piv[_i]++; } }
}

void dptcon_(INT* n, f64* D, f64* E, f64* anorm, f64* rcond, f64* work, INT* info) {
    dptcon(*n, D, E, *anorm, rcond, work, info);
}

void dpteqr_(char* compz, INT* n, f64* D, f64* E, f64* Z, INT* ldz, f64* work, INT* info) {
    dpteqr(compz, *n, D, E, Z, *ldz, work, info);
}

void dptrfs_(INT* n, INT* nrhs, f64* D, f64* E, f64* DF, f64* EF, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* info) {
    dptrfs(*n, *nrhs, D, E, DF, EF, B, *ldb, X, *ldx, ferr, berr, work, info);
}

void dptsv_(INT* n, INT* nrhs, f64* D, f64* E, f64* B, INT* ldb, INT* info) {
    dptsv(*n, *nrhs, D, E, B, *ldb, info);
}

void dptsvx_(char* fact, INT* n, INT* nrhs, f64* D, f64* E, f64* DF, f64* EF, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* info) {
    dptsvx(fact, *n, *nrhs, D, E, DF, EF, B, *ldb, X, *ldx, rcond, ferr, berr, work, info);
}

void dpttrf_(INT* n, f64* D, f64* E, INT* info) {
    dpttrf(*n, D, E, info);
}

void dpttrs_(INT* n, INT* nrhs, f64* D, f64* E, f64* B, INT* ldb, INT* info) {
    dpttrs(*n, *nrhs, D, E, B, *ldb, info);
}

void dptts2_(INT* n, INT* nrhs, f64* D, f64* E, f64* B, INT* ldb) {
    dptts2(*n, *nrhs, D, E, B, *ldb);
}

void drscl_(INT* n, f64* sa, f64* sx, INT* incx) {
    drscl(*n, *sa, sx, *incx);
}

void dsb2st_kernels_(char* uplo, INT* wantz, INT* ttype, INT* st, INT* ed, INT* sweep, INT* n, INT* nb, INT* ib, f64* A, INT* lda, f64* V, f64* tau, INT* ldvt, f64* work) {
    dsb2st_kernels(uplo, *wantz, *ttype, *st, *ed, *sweep, *n, *nb, *ib, A, *lda, V, tau, *ldvt, work);
}

void dsbev_2stage_(char* jobz, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* info) {
    dsbev_2stage(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, info);
}

void dsbev_(char* jobz, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* W, f64* Z, INT* ldz, f64* work, INT* info) {
    dsbev(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, info);
}

void dsbevd_2stage_(char* jobz, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dsbevd_2stage(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dsbevd_(char* jobz, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dsbevd(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dsbevx_2stage_(char* jobz, char* range, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* Q, INT* ldq, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsbevx_2stage(jobz, range, uplo, *n, *kd, AB, *ldab, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void dsbevx_(char* jobz, char* range, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* Q, INT* ldq, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsbevx(jobz, range, uplo, *n, *kd, AB, *ldab, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void dsbgst_(char* vect, char* uplo, INT* n, INT* ka, INT* kb, f64* AB, INT* ldab, f64* BB, INT* ldbb, f64* X, INT* ldx, f64* work, INT* info) {
    dsbgst(vect, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, X, *ldx, work, info);
}

void dsbgv_(char* jobz, char* uplo, INT* n, INT* ka, INT* kb, f64* AB, INT* ldab, f64* BB, INT* ldbb, f64* W, f64* Z, INT* ldz, f64* work, INT* info) {
    dsbgv(jobz, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, W, Z, *ldz, work, info);
}

void dsbgvd_(char* jobz, char* uplo, INT* n, INT* ka, INT* kb, f64* AB, INT* ldab, f64* BB, INT* ldbb, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dsbgvd(jobz, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dsbgvx_(char* jobz, char* range, char* uplo, INT* n, INT* ka, INT* kb, f64* AB, INT* ldab, f64* BB, INT* ldbb, f64* Q, INT* ldq, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsbgvx(jobz, range, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void dsbtrd_(char* vect, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* D, f64* E, f64* Q, INT* ldq, f64* work, INT* info) {
    dsbtrd(vect, uplo, *n, *kd, AB, *ldab, D, E, Q, *ldq, work, info);
}

void dsfrk_(char* transr, char* uplo, char* trans, INT* n, INT* k, f64* alpha, f64* A, INT* lda, f64* beta, f64* C) {
    dsfrk(transr, uplo, trans, *n, *k, *alpha, A, *lda, *beta, C);
}

void dsgesv_(INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* work, float* swork, INT* iter, INT* info) {
    dsgesv(*n, *nrhs, A, *lda, ipiv, B, *ldb, X, *ldx, work, swork, iter, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dspcon_(char* uplo, INT* n, f64* AP, INT* ipiv, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dspcon(uplo, *n, AP, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void dspev_(char* jobz, char* uplo, INT* n, f64* AP, f64* W, f64* Z, INT* ldz, f64* work, INT* info) {
    dspev(jobz, uplo, *n, AP, W, Z, *ldz, work, info);
}

void dspevd_(char* jobz, char* uplo, INT* n, f64* AP, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dspevd(jobz, uplo, *n, AP, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dspevx_(char* jobz, char* range, char* uplo, INT* n, f64* AP, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dspevx(jobz, range, uplo, *n, AP, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void dspgst_(INT* itype, char* uplo, INT* n, f64* AP, f64* BP, INT* info) {
    dspgst(*itype, uplo, *n, AP, BP, info);
}

void dspgv_(INT* itype, char* jobz, char* uplo, INT* n, f64* AP, f64* BP, f64* W, f64* Z, INT* ldz, f64* work, INT* info) {
    dspgv(*itype, jobz, uplo, *n, AP, BP, W, Z, *ldz, work, info);
}

void dspgvd_(INT* itype, char* jobz, char* uplo, INT* n, f64* AP, f64* BP, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dspgvd(*itype, jobz, uplo, *n, AP, BP, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dspgvx_(INT* itype, char* jobz, char* range, char* uplo, INT* n, f64* AP, f64* BP, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dspgvx(*itype, jobz, range, uplo, *n, AP, BP, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void dsposv_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* X, INT* ldx, f64* work, float* swork, INT* iter, INT* info) {
    dsposv(uplo, *n, *nrhs, A, *lda, B, *ldb, X, *ldx, work, swork, iter, info);
}

void dsprfs_(char* uplo, INT* n, INT* nrhs, f64* AP, f64* AFP, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsprfs(uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void dspsv_(char* uplo, INT* n, INT* nrhs, f64* AP, INT* ipiv, f64* B, INT* ldb, INT* info) {
    dspsv(uplo, *n, *nrhs, AP, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dspsvx_(char* fact, char* uplo, INT* n, INT* nrhs, f64* AP, f64* AFP, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        dspsvx(fact, uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        dspsvx(fact, uplo, *n, *nrhs, AP, AFP, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsptrd_(char* uplo, INT* n, f64* AP, f64* D, f64* E, f64* tau, INT* info) {
    dsptrd(uplo, *n, AP, D, E, tau, info);
}

void dsptrf_(char* uplo, INT* n, f64* AP, INT* ipiv, INT* info) {
    dsptrf(uplo, *n, AP, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dsptri_(char* uplo, INT* n, f64* AP, INT* ipiv, f64* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsptri(uplo, *n, AP, _ipiv0, work, info);
    free(_ipiv0);
}

void dsptrs_(char* uplo, INT* n, INT* nrhs, f64* AP, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsptrs(uplo, *n, *nrhs, AP, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dstebz_(char* range, char* order, INT* n, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, f64* D, f64* E, INT* m, INT* nsplit, f64* W, INT* iblock, INT* isplit, f64* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dstebz(range, order, *n, *vl, *vu, _il, _iu, *abstol, D, E, m, nsplit, W, iblock, isplit, work, iwork, info);
    if (iblock) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) iblock[_i]++; }
    if (isplit) { INT _sz = *nsplit; for (INT _i = 0; _i < _sz; _i++) isplit[_i]++; }
}

void dstedc_(char* compz, INT* n, f64* D, f64* E, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dstedc(compz, *n, D, E, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dstegr_(char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, INT* isuppz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dstegr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void dstein_(INT* n, f64* D, f64* E, INT* m, f64* W, INT* iblock, INT* isplit, f64* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT* _iblock0 = NULL;
    INT* _isplit0 = NULL;
    if (iblock) {
        INT _iblock_sz = *m;
        _iblock0 = (INT*)malloc(_iblock_sz * sizeof(INT));
        for (INT _i = 0; _i < _iblock_sz; _i++) _iblock0[_i] = (iblock[_i] > 0) ? iblock[_i] - 1 : iblock[_i];
    }
    if (isplit) {
        INT _isplit_sz = *n;
        _isplit0 = (INT*)malloc(_isplit_sz * sizeof(INT));
        for (INT _i = 0; _i < _isplit_sz; _i++) _isplit0[_i] = (isplit[_i] > 0) ? isplit[_i] - 1 : isplit[_i];
    }
    dstein(*n, D, E, *m, W, _iblock0, _isplit0, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    free(_iblock0);
    free(_isplit0);
}

void dstemr_(char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, INT* m, f64* W, f64* Z, INT* ldz, INT* nzc, INT* isuppz, INT* tryrac, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dstemr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, m, W, Z, *ldz, *nzc, isuppz, tryrac, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void dsteqr_(char* compz, INT* n, f64* D, f64* E, f64* Z, INT* ldz, f64* work, INT* info) {
    dsteqr(compz, *n, D, E, Z, *ldz, work, info);
}

void dsterf_(INT* n, f64* D, f64* E, INT* info) {
    dsterf(*n, D, E, info);
}

void dstev_(char* jobz, INT* n, f64* D, f64* E, f64* Z, INT* ldz, f64* work, INT* info) {
    dstev(jobz, *n, D, E, Z, *ldz, work, info);
}

void dstevd_(char* jobz, INT* n, f64* D, f64* E, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dstevd(jobz, *n, D, E, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void dstevr_(char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, INT* isuppz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dstevr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void dstevx_(char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dstevx(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void dsycon_3_(char* uplo, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsycon_3(uplo, *n, A, *lda, E, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void dsycon_rook_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsycon_rook(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void dsycon_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* anorm, f64* rcond, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsycon(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void dsyconv_(char* uplo, char* way, INT* n, f64* A, INT* lda, INT* ipiv, f64* E, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsyconv(uplo, way, *n, A, *lda, _ipiv0, E, info);
    free(_ipiv0);
}

void dsyconvf_rook_(char* uplo, char* way, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsyconvf_rook(uplo, way, *n, A, *lda, E, _ipiv0, info);
    free(_ipiv0);
}

void dsyconvf_(char* uplo, char* way, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsyconvf(uplo, way, *n, A, *lda, E, _ipiv0, info);
    free(_ipiv0);
}

void dsyequb_(char* uplo, INT* n, f64* A, INT* lda, f64* S, f64* scond, f64* amax, f64* work, INT* info) {
    dsyequb(uplo, *n, A, *lda, S, scond, amax, work, info);
}

void dsyev_2stage_(char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* W, f64* work, INT* lwork, INT* info) {
    dsyev_2stage(jobz, uplo, *n, A, *lda, W, work, *lwork, info);
}

void dsyev_(char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* W, f64* work, INT* lwork, INT* info) {
    dsyev(jobz, uplo, *n, A, *lda, W, work, *lwork, info);
}

void dsyevd_2stage_(char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* W, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dsyevd_2stage(jobz, uplo, *n, A, *lda, W, work, *lwork, iwork, *liwork, info);
}

void dsyevd_(char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* W, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dsyevd(jobz, uplo, *n, A, *lda, W, work, *lwork, iwork, *liwork, info);
}

void dsyevr_2stage_(char* jobz, char* range, char* uplo, INT* n, f64* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, INT* isuppz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsyevr_2stage(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz && ((*jobz == 'V' || *jobz == 'v') && (*range == 'A' || *range == 'a'))) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void dsyevr_(char* jobz, char* range, char* uplo, INT* n, f64* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, INT* isuppz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsyevr(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz && ((*jobz == 'V' || *jobz == 'v') && (*range == 'A' || *range == 'a'))) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void dsyevx_2stage_(char* jobz, char* range, char* uplo, INT* n, f64* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsyevx_2stage(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void dsyevx_(char* jobz, char* range, char* uplo, INT* n, f64* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsyevx(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void dsygs2_(INT* itype, char* uplo, INT* n, f64* A, INT* lda, f64* B, INT* ldb, INT* info) {
    dsygs2(*itype, uplo, *n, A, *lda, B, *ldb, info);
}

void dsygst_(INT* itype, char* uplo, INT* n, f64* A, INT* lda, f64* B, INT* ldb, INT* info) {
    dsygst(*itype, uplo, *n, A, *lda, B, *ldb, info);
}

void dsygv_2stage_(INT* itype, char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* W, f64* work, INT* lwork, INT* info) {
    dsygv_2stage(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, info);
}

void dsygv_(INT* itype, char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* W, f64* work, INT* lwork, INT* info) {
    dsygv(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, info);
}

void dsygvd_(INT* itype, char* jobz, char* uplo, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* W, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dsygvd(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, iwork, *liwork, info);
}

void dsygvx_(INT* itype, char* jobz, char* range, char* uplo, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, f64* Z, INT* ldz, f64* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    dsygvx(*itype, jobz, range, uplo, *n, A, *lda, B, *ldb, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void dsyrfs_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* AF, INT* ldaf, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsyrfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void dsysv_aa_2stage_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* TB, INT* ltb, INT* ipiv, INT* ipiv2, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dsysv_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, ipiv, ipiv2, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void dsysv_aa_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dsysv_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsysv_rk_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* E, INT* ipiv, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dsysv_rk(uplo, *n, *nrhs, A, *lda, E, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsysv_rook_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dsysv_rook(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsysv_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    dsysv(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsysvx_(char* fact, char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* AF, INT* ldaf, INT* ipiv, f64* B, INT* ldb, f64* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, f64* work, INT* lwork, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        dsysvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, iwork, info);
        free(_ipiv0);
    } else {
        dsysvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsyswapr_(char* uplo, INT* n, f64* A, INT* lda, INT* i1, INT* i2) {
    dsyswapr(uplo, *n, A, *lda, *i1, *i2);
}

void dsytd2_(char* uplo, INT* n, f64* A, INT* lda, f64* D, f64* E, f64* tau, INT* info) {
    dsytd2(uplo, *n, A, *lda, D, E, tau, info);
}

void dsytf2_rk_(char* uplo, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, INT* info) {
    dsytf2_rk(uplo, *n, A, *lda, E, ipiv, info);
}

void dsytf2_rook_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, INT* info) {
    dsytf2_rook(uplo, *n, A, *lda, ipiv, info);
}

void dsytf2_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, INT* info) {
    dsytf2(uplo, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void dsytrd_2stage_(char* vect, char* uplo, INT* n, f64* A, INT* lda, f64* D, f64* E, f64* tau, f64* hous2, INT* lhous2, f64* work, INT* lwork, INT* info) {
    dsytrd_2stage(vect, uplo, *n, A, *lda, D, E, tau, hous2, *lhous2, work, *lwork, info);
}

void dsytrd_sb2st_(char* stage1, char* vect, char* uplo, INT* n, INT* kd, f64* AB, INT* ldab, f64* D, f64* E, f64* hous, INT* lhous, f64* work, INT* lwork, INT* info) {
    dsytrd_sb2st(stage1, vect, uplo, *n, *kd, AB, *ldab, D, E, hous, *lhous, work, *lwork, info);
}

void dsytrd_sy2sb_(char* uplo, INT* n, INT* kd, f64* A, INT* lda, f64* AB, INT* ldab, f64* tau, f64* work, INT* lwork, INT* info) {
    dsytrd_sy2sb(uplo, *n, *kd, A, *lda, AB, *ldab, tau, work, *lwork, info);
}

void dsytrd_(char* uplo, INT* n, f64* A, INT* lda, f64* D, f64* E, f64* tau, f64* work, INT* lwork, INT* info) {
    dsytrd(uplo, *n, A, *lda, D, E, tau, work, *lwork, info);
}

void dsytrf_aa_2stage_(char* uplo, INT* n, f64* A, INT* lda, f64* TB, INT* ltb, INT* ipiv, INT* ipiv2, f64* work, INT* lwork, INT* info) {
    dsytrf_aa_2stage(uplo, *n, A, *lda, TB, *ltb, ipiv, ipiv2, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void dsytrf_aa_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* lwork, INT* info) {
    dsytrf_aa(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsytrf_rk_(char* uplo, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, f64* work, INT* lwork, INT* info) {
    dsytrf_rk(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsytrf_rook_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* lwork, INT* info) {
    dsytrf_rook(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsytrf_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* lwork, INT* info) {
    dsytrf(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void dsytri_3_(char* uplo, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, f64* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        dsytri_3(uplo, *n, A, *lda, E, _ipiv0, work, *lwork, info);
    } else {
        dsytri_3(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void dsytri_3x_(char* uplo, INT* n, f64* A, INT* lda, f64* E, INT* ipiv, f64* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytri_3x(uplo, *n, A, *lda, E, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void dsytri_rook_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytri_rook(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void dsytri_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytri(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void dsytri2_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        dsytri2(uplo, *n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        dsytri2(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void dsytri2x_(char* uplo, INT* n, f64* A, INT* lda, INT* ipiv, f64* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytri2x(uplo, *n, A, *lda, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void dsytrs_3_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* E, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytrs_3(uplo, *n, *nrhs, A, *lda, E, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dsytrs_aa_2stage_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, f64* TB, INT* ltb, INT* ipiv, INT* ipiv2, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    INT* _ipiv20 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (ipiv2) {
        INT _ipiv2_sz = *n;
        _ipiv20 = (INT*)malloc(_ipiv2_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv2_sz; _i++) _ipiv20[_i] = (ipiv2[_i] > 0) ? ipiv2[_i] - 1 : ipiv2[_i];
    }
    dsytrs_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, _ipiv0, _ipiv20, B, *ldb, info);
    free(_ipiv0);
    free(_ipiv20);
}

void dsytrs_aa_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, f64* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        dsytrs_aa(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, *lwork, info);
    } else {
        dsytrs_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void dsytrs_rook_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytrs_rook(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dsytrs_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytrs(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void dsytrs2_(char* uplo, INT* n, INT* nrhs, f64* A, INT* lda, INT* ipiv, f64* B, INT* ldb, f64* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    dsytrs2(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, info);
    free(_ipiv0);
}

void dtbcon_(char* norm, char* uplo, char* diag, INT* n, INT* kd, f64* AB, INT* ldab, f64* rcond, f64* work, INT* iwork, INT* info) {
    dtbcon(norm, uplo, diag, *n, *kd, AB, *ldab, rcond, work, iwork, info);
}

void dtbrfs_(char* uplo, char* trans, char* diag, INT* n, INT* kd, INT* nrhs, f64* AB, INT* ldab, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dtbrfs(uplo, trans, diag, *n, *kd, *nrhs, AB, *ldab, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void dtbtrs_(char* uplo, char* trans, char* diag, INT* n, INT* kd, INT* nrhs, f64* AB, INT* ldab, f64* B, INT* ldb, INT* info) {
    dtbtrs(uplo, trans, diag, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void dtfsm_(char* transr, char* side, char* uplo, char* trans, char* diag, INT* m, INT* n, f64* alpha, f64* A, f64* B, INT* ldb) {
    dtfsm(transr, side, uplo, trans, diag, *m, *n, *alpha, A, B, *ldb);
}

void dtftri_(char* transr, char* uplo, char* diag, INT* n, f64* A, INT* info) {
    dtftri(transr, uplo, diag, *n, A, info);
}

void dtfttp_(char* transr, char* uplo, INT* n, f64* ARF, f64* AP, INT* info) {
    dtfttp(transr, uplo, *n, ARF, AP, info);
}

void dtfttr_(char* transr, char* uplo, INT* n, f64* ARF, f64* A, INT* lda, INT* info) {
    dtfttr(transr, uplo, *n, ARF, A, *lda, info);
}

void dtgevc_(char* side, char* howmny, INT* select, INT* n, f64* S, INT* lds, f64* P, INT* ldp, f64* VL, INT* ldvl, f64* VR, INT* ldvr, INT* mm, INT* m, f64* work, INT* info) {
    dtgevc(side, howmny, select, *n, S, *lds, P, *ldp, VL, *ldvl, VR, *ldvr, *mm, m, work, info);
}

void dtgex2_(INT* wantq, INT* wantz, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* Q, INT* ldq, f64* Z, INT* ldz, INT* j1, INT* n1, INT* n2, f64* work, INT* lwork, INT* info) {
    dtgex2(*wantq, *wantz, *n, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, *j1, *n1, *n2, work, *lwork, info);
}

void dtgexc_(INT* wantq, INT* wantz, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* Q, INT* ldq, f64* Z, INT* ldz, INT* ifst, INT* ilst, f64* work, INT* lwork, INT* info) {
    INT _ifst = *ifst - 1;
    INT _ilst = *ilst - 1;
    dtgexc(*wantq, *wantz, *n, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, &_ifst, &_ilst, work, *lwork, info);
    *ifst = _ifst + 1;
    *ilst = _ilst + 1;
}

void dtgsen_(INT* ijob, INT* wantq, INT* wantz, INT* select, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* alphar, f64* alphai, f64* beta, f64* Q, INT* ldq, f64* Z, INT* ldz, INT* m, f64* pl, f64* pr, f64* dif, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dtgsen(*ijob, *wantq, *wantz, select, *n, A, *lda, B, *ldb, alphar, alphai, beta, Q, *ldq, Z, *ldz, m, pl, pr, dif, work, *lwork, iwork, *liwork, info);
}

void dtgsja_(char* jobu, char* jobv, char* jobq, INT* m, INT* p, INT* n, INT* k, INT* l, f64* A, INT* lda, f64* B, INT* ldb, f64* tola, f64* tolb, f64* alpha, f64* beta, f64* U, INT* ldu, f64* V, INT* ldv, f64* Q, INT* ldq, f64* work, INT* ncycle, INT* info) {
    dtgsja(jobu, jobv, jobq, *m, *p, *n, *k, *l, A, *lda, B, *ldb, *tola, *tolb, alpha, beta, U, *ldu, V, *ldv, Q, *ldq, work, ncycle, info);
}

void dtgsna_(char* job, char* howmny, INT* select, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* VL, INT* ldvl, f64* VR, INT* ldvr, f64* S, f64* dif, INT* mm, INT* m, f64* work, INT* lwork, INT* iwork, INT* info) {
    dtgsna(job, howmny, select, *n, A, *lda, B, *ldb, VL, *ldvl, VR, *ldvr, S, dif, *mm, m, work, *lwork, iwork, info);
}

void dtgsy2_(char* trans, INT* ijob, INT* m, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* C, INT* ldc, f64* D, INT* ldd, f64* E, INT* lde, f64* F, INT* ldf, f64* scale, f64* rdsum, f64* rdscal, INT* iwork, INT* pq, INT* info) {
    dtgsy2(trans, *ijob, *m, *n, A, *lda, B, *ldb, C, *ldc, D, *ldd, E, *lde, F, *ldf, scale, rdsum, rdscal, iwork, pq, info);
}

void dtgsyl_(char* trans, INT* ijob, INT* m, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* C, INT* ldc, f64* D, INT* ldd, f64* E, INT* lde, f64* F, INT* ldf, f64* scale, f64* dif, f64* work, INT* lwork, INT* iwork, INT* info) {
    dtgsyl(trans, *ijob, *m, *n, A, *lda, B, *ldb, C, *ldc, D, *ldd, E, *lde, F, *ldf, scale, dif, work, *lwork, iwork, info);
}

void dtpcon_(char* norm, char* uplo, char* diag, INT* n, f64* AP, f64* rcond, f64* work, INT* iwork, INT* info) {
    dtpcon(norm, uplo, diag, *n, AP, rcond, work, iwork, info);
}

void dtplqt_(INT* m, INT* n, INT* l, INT* mb, f64* A, INT* lda, f64* B, INT* ldb, f64* T, INT* ldt, f64* work, INT* info) {
    dtplqt(*m, *n, *l, *mb, A, *lda, B, *ldb, T, *ldt, work, info);
}

void dtplqt2_(INT* m, INT* n, INT* l, f64* A, INT* lda, f64* B, INT* ldb, f64* T, INT* ldt, INT* info) {
    dtplqt2(*m, *n, *l, A, *lda, B, *ldb, T, *ldt, info);
}

void dtpmlqt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, INT* mb, f64* V, INT* ldv, f64* T, INT* ldt, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* info) {
    dtpmlqt(side, trans, *m, *n, *k, *l, *mb, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, info);
}

void dtpmqrt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, INT* nb, f64* V, INT* ldv, f64* T, INT* ldt, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* info) {
    dtpmqrt(side, trans, *m, *n, *k, *l, *nb, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, info);
}

void dtpqrt_(INT* m, INT* n, INT* l, INT* nb, f64* A, INT* lda, f64* B, INT* ldb, f64* T, INT* ldt, f64* work, INT* info) {
    dtpqrt(*m, *n, *l, *nb, A, *lda, B, *ldb, T, *ldt, work, info);
}

void dtpqrt2_(INT* m, INT* n, INT* l, f64* A, INT* lda, f64* B, INT* ldb, f64* T, INT* ldt, INT* info) {
    dtpqrt2(*m, *n, *l, A, *lda, B, *ldb, T, *ldt, info);
}

void dtprfb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, INT* l, f64* V, INT* ldv, f64* T, INT* ldt, f64* A, INT* lda, f64* B, INT* ldb, f64* work, INT* ldwork) {
    dtprfb(side, trans, direct, storev, *m, *n, *k, *l, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, *ldwork);
}

void dtprfs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f64* AP, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dtprfs(uplo, trans, diag, *n, *nrhs, AP, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void dtptri_(char* uplo, char* diag, INT* n, f64* AP, INT* info) {
    dtptri(uplo, diag, *n, AP, info);
}

void dtptrs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f64* AP, f64* B, INT* ldb, INT* info) {
    dtptrs(uplo, trans, diag, *n, *nrhs, AP, B, *ldb, info);
}

void dtpttf_(char* transr, char* uplo, INT* n, f64* AP, f64* ARF, INT* info) {
    dtpttf(transr, uplo, *n, AP, ARF, info);
}

void dtpttr_(char* uplo, INT* n, f64* AP, f64* A, INT* lda, INT* info) {
    dtpttr(uplo, *n, AP, A, *lda, info);
}

void dtrcon_(char* norm, char* uplo, char* diag, INT* n, f64* A, INT* lda, f64* rcond, f64* work, INT* iwork, INT* info) {
    dtrcon(norm, uplo, diag, *n, A, *lda, rcond, work, iwork, info);
}

void dtrevc_(char* side, char* howmny, INT* select, INT* n, f64* T, INT* ldt, f64* VL, INT* ldvl, f64* VR, INT* ldvr, INT* mm, INT* m, f64* work, INT* info) {
    dtrevc(side, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, *mm, m, work, info);
}

void dtrevc3_(char* side, char* howmny, INT* select, INT* n, f64* T, INT* ldt, f64* VL, INT* ldvl, f64* VR, INT* ldvr, INT* mm, INT* m, f64* work, INT* lwork, INT* info) {
    dtrevc3(side, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, *mm, m, work, *lwork, info);
}

void dtrexc_(char* compq, INT* n, f64* T, INT* ldt, f64* Q, INT* ldq, INT* ifst, INT* ilst, f64* work, INT* info) {
    INT _ifst = *ifst - 1;
    INT _ilst = *ilst - 1;
    dtrexc(compq, *n, T, *ldt, Q, *ldq, &_ifst, &_ilst, work, info);
    *ifst = _ifst + 1;
    *ilst = _ilst + 1;
}

void dtrrfs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, f64* X, INT* ldx, f64* ferr, f64* berr, f64* work, INT* iwork, INT* info) {
    dtrrfs(uplo, trans, diag, *n, *nrhs, A, *lda, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void dtrsen_(char* job, char* compq, INT* select, INT* n, f64* T, INT* ldt, f64* Q, INT* ldq, f64* wr, f64* wi, INT* m, f64* s, f64* sep, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    dtrsen(job, compq, select, *n, T, *ldt, Q, *ldq, wr, wi, m, s, sep, work, *lwork, iwork, *liwork, info);
}

void dtrsna_(char* job, char* howmny, INT* select, INT* n, f64* T, INT* ldt, f64* VL, INT* ldvl, f64* VR, INT* ldvr, f64* S, f64* sep, INT* mm, INT* m, f64* work, INT* ldwork, INT* iwork, INT* info) {
    dtrsna(job, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, S, sep, *mm, m, work, *ldwork, iwork, info);
}

void dtrsyl_(char* trana, char* tranb, INT* isgn, INT* m, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* C, INT* ldc, f64* scale, INT* info) {
    dtrsyl(trana, tranb, *isgn, *m, *n, A, *lda, B, *ldb, C, *ldc, scale, info);
}

void dtrsyl3_(char* trana, char* tranb, INT* isgn, INT* m, INT* n, f64* A, INT* lda, f64* B, INT* ldb, f64* C, INT* ldc, f64* scale, INT* iwork, INT* liwork, f64* swork, INT* ldswork, INT* info) {
    dtrsyl3(trana, tranb, *isgn, *m, *n, A, *lda, B, *ldb, C, *ldc, scale, iwork, *liwork, swork, *ldswork, info);
}

void dtrti2_(char* uplo, char* diag, INT* n, f64* A, INT* lda, INT* info) {
    dtrti2(uplo, diag, *n, A, *lda, info);
}

void dtrtri_(char* uplo, char* diag, INT* n, f64* A, INT* lda, INT* info) {
    dtrtri(uplo, diag, *n, A, *lda, info);
}

void dtrtrs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f64* A, INT* lda, f64* B, INT* ldb, INT* info) {
    dtrtrs(uplo, trans, diag, *n, *nrhs, A, *lda, B, *ldb, info);
}

void dtrttf_(char* transr, char* uplo, INT* n, f64* A, INT* lda, f64* ARF, INT* info) {
    dtrttf(transr, uplo, *n, A, *lda, ARF, info);
}

void dtrttp_(char* uplo, INT* n, f64* A, INT* lda, f64* AP, INT* info) {
    dtrttp(uplo, *n, A, *lda, AP, info);
}

void dtzrzf_(INT* m, INT* n, f64* A, INT* lda, f64* tau, f64* work, INT* lwork, INT* info) {
    dtzrzf(*m, *n, A, *lda, tau, work, *lwork, info);
}
