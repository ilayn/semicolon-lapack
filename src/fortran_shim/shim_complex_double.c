/* AUTO-GENERATED FILE — DO NOT EDIT
 * Fortran-ABI compatibility shim for semicolon-lapack
 * Precision: complex_double
 * Source header: semicolon_lapack_complex_double.h
 * Generated by scripts/generate_fortran_shim.py
 */

#include <stdlib.h>
#include "semicolon_lapack_complex_double.h"


f64 dzsum1_(INT* n, c128* CX, INT* incx) {
    f64 _ret = dzsum1(*n, CX, *incx);
    return _ret;
}

f64 zla_gbrpvgrw_(INT* n, INT* kl, INT* ku, INT* ncols, c128* AB, INT* ldab, c128* AFB, INT* ldafb) {
    f64 _ret = zla_gbrpvgrw(*n, *kl, *ku, *ncols, AB, *ldab, AFB, *ldafb);
    return _ret;
}

f64 zla_gerpvgrw_(INT* n, INT* ncols, c128* A, INT* lda, c128* AF, INT* ldaf) {
    f64 _ret = zla_gerpvgrw(*n, *ncols, A, *lda, AF, *ldaf);
    return _ret;
}

c128 zladiv_(c128* X, c128* Y) {
    c128 _ret = zladiv(*X, *Y);
    return _ret;
}

f64 zlangb_(char* norm, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, f64* work) {
    f64 _ret = zlangb(norm, *n, *kl, *ku, AB, *ldab, work);
    return _ret;
}

f64 zlange_(char* norm, INT* m, INT* n, c128* A, INT* lda, f64* work) {
    f64 _ret = zlange(norm, *m, *n, A, *lda, work);
    return _ret;
}

f64 zlangt_(char* norm, INT* n, c128* DL, c128* D, c128* DU) {
    f64 _ret = zlangt(norm, *n, DL, D, DU);
    return _ret;
}

f64 zlanhb_(char* norm, char* uplo, INT* n, INT* k, c128* AB, INT* ldab, f64* work) {
    f64 _ret = zlanhb(norm, uplo, *n, *k, AB, *ldab, work);
    return _ret;
}

f64 zlanhe_(char* norm, char* uplo, INT* n, c128* A, INT* lda, f64* work) {
    f64 _ret = zlanhe(norm, uplo, *n, A, *lda, work);
    return _ret;
}

f64 zlanhf_(char* norm, char* transr, char* uplo, INT* n, c128* A, f64* work) {
    f64 _ret = zlanhf(norm, transr, uplo, *n, A, work);
    return _ret;
}

f64 zlanhp_(char* norm, char* uplo, INT* n, c128* AP, f64* work) {
    f64 _ret = zlanhp(norm, uplo, *n, AP, work);
    return _ret;
}

f64 zlanhs_(char* norm, INT* n, c128* A, INT* lda, f64* work) {
    f64 _ret = zlanhs(norm, *n, A, *lda, work);
    return _ret;
}

f64 zlanht_(char* norm, INT* n, f64* D, c128* E) {
    f64 _ret = zlanht(norm, *n, D, E);
    return _ret;
}

f64 zlansb_(char* norm, char* uplo, INT* n, INT* k, c128* AB, INT* ldab, f64* work) {
    f64 _ret = zlansb(norm, uplo, *n, *k, AB, *ldab, work);
    return _ret;
}

f64 zlansp_(char* norm, char* uplo, INT* n, c128* AP, f64* work) {
    f64 _ret = zlansp(norm, uplo, *n, AP, work);
    return _ret;
}

f64 zlansy_(char* norm, char* uplo, INT* n, c128* A, INT* lda, f64* work) {
    f64 _ret = zlansy(norm, uplo, *n, A, *lda, work);
    return _ret;
}

f64 zlantb_(char* norm, char* uplo, char* diag, INT* n, INT* k, c128* AB, INT* ldab, f64* work) {
    f64 _ret = zlantb(norm, uplo, diag, *n, *k, AB, *ldab, work);
    return _ret;
}

f64 zlantp_(char* norm, char* uplo, char* diag, INT* n, c128* AP, f64* work) {
    f64 _ret = zlantp(norm, uplo, diag, *n, AP, work);
    return _ret;
}

f64 zlantr_(char* norm, char* uplo, char* diag, INT* m, INT* n, c128* A, INT* lda, f64* work) {
    f64 _ret = zlantr(norm, uplo, diag, *m, *n, A, *lda, work);
    return _ret;
}

INT izmax1_(INT* n, c128* ZX, INT* incx) {
    INT _ret = izmax1(*n, ZX, *incx);
    return _ret;
}

void zbbcsd_(char* jobu1, char* jobu2, char* jobv1t, char* jobv2t, char* trans, INT* m, INT* p, INT* q, f64* theta, f64* phi, c128* U1, INT* ldu1, c128* U2, INT* ldu2, c128* V1T, INT* ldv1t, c128* V2T, INT* ldv2t, f64* B11D, f64* B11E, f64* B12D, f64* B12E, f64* B21D, f64* B21E, f64* B22D, f64* B22E, f64* rwork, INT* lrwork, INT* info) {
    zbbcsd(jobu1, jobu2, jobv1t, jobv2t, trans, *m, *p, *q, theta, phi, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, V2T, *ldv2t, B11D, B11E, B12D, B12E, B21D, B21E, B22D, B22E, rwork, *lrwork, info);
}

void zbdsqr_(char* uplo, INT* n, INT* ncvt, INT* nru, INT* ncc, f64* D, f64* E, c128* VT, INT* ldvt, c128* U, INT* ldu, c128* C, INT* ldc, f64* rwork, INT* info) {
    zbdsqr(uplo, *n, *ncvt, *nru, *ncc, D, E, VT, *ldvt, U, *ldu, C, *ldc, rwork, info);
}

void zcgesv_(INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, c128* work, c64* swork, f64* rwork, INT* iter, INT* info) {
    zcgesv(*n, *nrhs, A, *lda, ipiv, B, *ldb, X, *ldx, work, swork, rwork, iter, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zcposv_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, c128* X, INT* ldx, c128* work, c64* swork, f64* rwork, INT* iter, INT* info) {
    zcposv(uplo, *n, *nrhs, A, *lda, B, *ldb, X, *ldx, work, swork, rwork, iter, info);
}

void zdrscl_(INT* n, f64* sa, c128* sx, INT* incx) {
    zdrscl(*n, *sa, sx, *incx);
}

void zgbcon_(char* norm, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, INT* ipiv, f64* anorm, f64* rcond, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgbcon(norm, *n, *kl, *ku, AB, *ldab, _ipiv0, *anorm, rcond, work, rwork, info);
    free(_ipiv0);
}

void zgbequ_(INT* m, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    zgbequ(*m, *n, *kl, *ku, AB, *ldab, R, C, rowcnd, colcnd, amax, info);
}

void zgbequb_(INT* m, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    zgbequb(*m, *n, *kl, *ku, AB, *ldab, R, C, rowcnd, colcnd, amax, info);
}

void zgbrfs_(char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, c128* AB, INT* ldab, c128* AFB, INT* ldafb, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgbrfs(trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zgbsv_(INT* n, INT* kl, INT* ku, INT* nrhs, c128* AB, INT* ldab, INT* ipiv, c128* B, INT* ldb, INT* info) {
    zgbsv(*n, *kl, *ku, *nrhs, AB, *ldab, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgbsvx_(char* fact, char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, c128* AB, INT* ldab, c128* AFB, INT* ldafb, INT* ipiv, char* equed, f64* R, f64* C, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zgbsvx(fact, trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, _ipiv0, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        free(_ipiv0);
    } else {
        zgbsvx(fact, trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, ipiv, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zgbbrd_(char* vect, INT* m, INT* n, INT* ncc, INT* kl, INT* ku, c128* AB, INT* ldab, f64* D, f64* E, c128* Q, INT* ldq, c128* PT, INT* ldpt, c128* C, INT* ldc, c128* work, f64* rwork, INT* info) {
    zgbbrd(vect, *m, *n, *ncc, *kl, *ku, AB, *ldab, D, E, Q, *ldq, PT, *ldpt, C, *ldc, work, rwork, info);
}

void zgbtf2_(INT* m, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, INT* ipiv, INT* info) {
    zgbtf2(*m, *n, *kl, *ku, AB, *ldab, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgbtrf_(INT* m, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, INT* ipiv, INT* info) {
    zgbtrf(*m, *n, *kl, *ku, AB, *ldab, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgbtrs_(char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, c128* AB, INT* ldab, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgbtrs(trans, *n, *kl, *ku, *nrhs, AB, *ldab, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zgebak_(char* job, char* side, INT* n, INT* ilo, INT* ihi, f64* scale, INT* m, c128* V, INT* ldv, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    if (job[0] == 'B' || job[0] == 'b' || job[0] == 'P' || job[0] == 'p') {
        for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] -= 1.0;
        for (INT _i = *ihi; _i < *n; _i++) scale[_i] -= 1.0;
    }
    zgebak(job, side, *n, _ilo, _ihi, scale, *m, V, *ldv, info);
    if (job[0] == 'B' || job[0] == 'b' || job[0] == 'P' || job[0] == 'p') {
        for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] += 1.0;
        for (INT _i = *ihi; _i < *n; _i++) scale[_i] += 1.0;
    }
}

void zgebal_(char* job, INT* n, c128* A, INT* lda, INT* ilo, INT* ihi, f64* scale, INT* info) {
    zgebal(job, *n, A, *lda, ilo, ihi, scale, info);
    (*ilo)++;
    (*ihi)++;
    for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] += 1.0;
    for (INT _i = *ihi; _i < *n; _i++) scale[_i] += 1.0;
}

void zgebd2_(INT* m, INT* n, c128* A, INT* lda, f64* D, f64* E, c128* tauq, c128* taup, c128* work, INT* info) {
    zgebd2(*m, *n, A, *lda, D, E, tauq, taup, work, info);
}

void zgebrd_(INT* m, INT* n, c128* A, INT* lda, f64* D, f64* E, c128* tauq, c128* taup, c128* work, INT* lwork, INT* info) {
    zgebrd(*m, *n, A, *lda, D, E, tauq, taup, work, *lwork, info);
}

void zgecon_(char* norm, INT* n, c128* A, INT* lda, f64* anorm, f64* rcond, c128* work, f64* rwork, INT* info) {
    zgecon(norm, *n, A, *lda, *anorm, rcond, work, rwork, info);
}

void zgeequ_(INT* m, INT* n, c128* A, INT* lda, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    zgeequ(*m, *n, A, *lda, R, C, rowcnd, colcnd, amax, info);
}

void zgeequb_(INT* m, INT* n, c128* A, INT* lda, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, INT* info) {
    zgeequb(*m, *n, A, *lda, R, C, rowcnd, colcnd, amax, info);
}

void zgees_(char* jobvs, char* sort, zselect1_t select, INT* n, c128* A, INT* lda, INT* sdim, c128* W, c128* VS, INT* ldvs, c128* work, INT* lwork, f64* rwork, INT* bwork, INT* info) {
    zgees(jobvs, sort, select, *n, A, *lda, sdim, W, VS, *ldvs, work, *lwork, rwork, bwork, info);
}

void zgeesx_(char* jobvs, char* sort, zselect1_t select, char* sense, INT* n, c128* A, INT* lda, INT* sdim, c128* W, c128* VS, INT* ldvs, f64* rconde, f64* rcondv, c128* work, INT* lwork, f64* rwork, INT* bwork, INT* info) {
    zgeesx(jobvs, sort, select, sense, *n, A, *lda, sdim, W, VS, *ldvs, rconde, rcondv, work, *lwork, rwork, bwork, info);
}

void zgeev_(char* jobvl, char* jobvr, INT* n, c128* A, INT* lda, c128* W, c128* VL, INT* ldvl, c128* VR, INT* ldvr, c128* work, INT* lwork, f64* rwork, INT* info) {
    zgeev(jobvl, jobvr, *n, A, *lda, W, VL, *ldvl, VR, *ldvr, work, *lwork, rwork, info);
}

void zgeevx_(char* balanc, char* jobvl, char* jobvr, char* sense, INT* n, c128* A, INT* lda, c128* W, c128* VL, INT* ldvl, c128* VR, INT* ldvr, INT* ilo, INT* ihi, f64* scale, f64* abnrm, f64* rconde, f64* rcondv, c128* work, INT* lwork, f64* rwork, INT* info) {
    zgeevx(balanc, jobvl, jobvr, sense, *n, A, *lda, W, VL, *ldvl, VR, *ldvr, ilo, ihi, scale, abnrm, rconde, rcondv, work, *lwork, rwork, info);
    (*ilo)++;
    (*ihi)++;
}

void zgehd2_(INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zgehd2(*n, _ilo, _ihi, A, *lda, tau, work, info);
}

void zgehrd_(INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zgehrd(*n, _ilo, _ihi, A, *lda, tau, work, *lwork, info);
}

void zgelq_(INT* m, INT* n, c128* A, INT* lda, c128* T, INT* tsize, c128* work, INT* lwork, INT* info) {
    zgelq(*m, *n, A, *lda, T, *tsize, work, *lwork, info);
}

void zgelq2_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zgelq2(*m, *n, A, *lda, tau, work, info);
}

void zgelqf_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zgelqf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void zgelqt_(INT* m, INT* n, INT* mb, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* info) {
    zgelqt(*m, *n, *mb, A, *lda, T, *ldt, work, info);
}

void zgelqt3_(INT* m, INT* n, c128* A, INT* lda, c128* T, INT* ldt, INT* info) {
    zgelqt3(*m, *n, A, *lda, T, *ldt, info);
}

void zgels_(char* trans, INT* m, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zgels(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void zgelsd_(INT* m, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, f64* S, f64* rcond, INT* rank, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* info) {
    zgelsd(*m, *n, *nrhs, A, *lda, B, *ldb, S, *rcond, rank, work, *lwork, rwork, iwork, info);
}

void zgelst_(char* trans, INT* m, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zgelst(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void zgelss_(INT* m, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, f64* S, f64* rcond, INT* rank, c128* work, INT* lwork, f64* rwork, INT* info) {
    zgelss(*m, *n, *nrhs, A, *lda, B, *ldb, S, *rcond, rank, work, *lwork, rwork, info);
}

void zgelsy_(INT* m, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, INT* jpvt, f64* rcond, INT* rank, c128* work, INT* lwork, f64* rwork, INT* info) {
    INT* _jpvt0 = NULL;
    if (*lwork != -1) {
        if (jpvt) {
            INT _jpvt_sz = *n;
            _jpvt0 = (INT*)malloc(_jpvt_sz * sizeof(INT));
            for (INT _i = 0; _i < _jpvt_sz; _i++) _jpvt0[_i] = (jpvt[_i] > 0) ? jpvt[_i] - 1 : jpvt[_i];
        }
    }
    if (*lwork != -1) {
        zgelsy(*m, *n, *nrhs, A, *lda, B, *ldb, _jpvt0, *rcond, rank, work, *lwork, rwork, info);
    } else {
        zgelsy(*m, *n, *nrhs, A, *lda, B, *ldb, jpvt, *rcond, rank, work, *lwork, rwork, info);
    }
    if (*lwork != -1) {
        free(_jpvt0);
    }
}

void zgemlq_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* T, INT* tsize, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zgemlq(side, trans, *m, *n, *k, A, *lda, T, *tsize, C, *ldc, work, *lwork, info);
}

void zgemlqt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, c128* V, INT* ldv, c128* T, INT* ldt, c128* C, INT* ldc, c128* work, INT* info) {
    zgemlqt(side, trans, *m, *n, *k, *mb, V, *ldv, T, *ldt, C, *ldc, work, info);
}

void zgemqr_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* T, INT* tsize, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zgemqr(side, trans, *m, *n, *k, A, *lda, T, *tsize, C, *ldc, work, *lwork, info);
}

void zgemqrt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* nb, c128* V, INT* ldv, c128* T, INT* ldt, c128* C, INT* ldc, c128* work, INT* info) {
    zgemqrt(side, trans, *m, *n, *k, *nb, V, *ldv, T, *ldt, C, *ldc, work, info);
}

void zgeql2_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zgeql2(*m, *n, A, *lda, tau, work, info);
}

void zgeqlf_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zgeqlf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void zgeqp3_(INT* m, INT* n, c128* A, INT* lda, INT* jpvt, c128* tau, c128* work, INT* lwork, f64* rwork, INT* info) {
    zgeqp3(*m, *n, A, *lda, jpvt, tau, work, *lwork, rwork, info);
    if (*lwork != -1) {
        if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
    }
}

void zgeqp3rk_(INT* m, INT* n, INT* nrhs, INT* kmax, f64* abstol, f64* reltol, c128* A, INT* lda, INT* K, f64* maxc2nrmk, f64* relmaxc2nrmk, INT* jpiv, c128* tau, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* info) {
    zgeqp3rk(*m, *n, *nrhs, *kmax, *abstol, *reltol, A, *lda, K, maxc2nrmk, relmaxc2nrmk, jpiv, tau, work, *lwork, rwork, iwork, info);
    if (*lwork != -1) {
        if (jpiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpiv[_i] >= 0) jpiv[_i]++; } }
    }
}

void zgeqr_(INT* m, INT* n, c128* A, INT* lda, c128* T, INT* tsize, c128* work, INT* lwork, INT* info) {
    zgeqr(*m, *n, A, *lda, T, *tsize, work, *lwork, info);
}

void zgeqr2_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zgeqr2(*m, *n, A, *lda, tau, work, info);
}

void zgeqr2p_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zgeqr2p(*m, *n, A, *lda, tau, work, info);
}

void zgeqrf_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zgeqrf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void zgeqrfp_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zgeqrfp(*m, *n, A, *lda, tau, work, *lwork, info);
}

void zgeqrt_(INT* m, INT* n, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* info) {
    zgeqrt(*m, *n, *nb, A, *lda, T, *ldt, work, info);
}

void zgeqrt2_(INT* m, INT* n, c128* A, INT* lda, c128* T, INT* ldt, INT* info) {
    zgeqrt2(*m, *n, A, *lda, T, *ldt, info);
}

void zgeqrt3_(INT* m, INT* n, c128* A, INT* lda, c128* T, INT* ldt, INT* info) {
    zgeqrt3(*m, *n, A, *lda, T, *ldt, info);
}

void zgerfs_(char* trans, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgerfs(trans, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zgerq2_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zgerq2(*m, *n, A, *lda, tau, work, info);
}

void zgerqf_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zgerqf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void zgesc2_(INT* n, c128* A, INT* lda, c128* rhs, INT* ipiv, INT* jpiv, f64* scale) {
    INT* _ipiv0 = NULL;
    INT* _jpiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (jpiv) {
        INT _jpiv_sz = *n;
        _jpiv0 = (INT*)malloc(_jpiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _jpiv_sz; _i++) _jpiv0[_i] = (jpiv[_i] > 0) ? jpiv[_i] - 1 : jpiv[_i];
    }
    zgesc2(*n, A, *lda, rhs, _ipiv0, _jpiv0, scale);
    free(_ipiv0);
    free(_jpiv0);
}

void zgesv_(INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, INT* info) {
    zgesv(*n, *nrhs, A, *lda, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgejsv_(char* joba, char* jobu, char* jobv, char* jobr, char* jobt, char* jobp, INT* m, INT* n, c128* A, INT* lda, f64* SVA, c128* U, INT* ldu, c128* V, INT* ldv, c128* cwork, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* info) {
    zgejsv(joba, jobu, jobv, jobr, jobt, jobp, *m, *n, A, *lda, SVA, U, *ldu, V, *ldv, cwork, *lwork, rwork, *lrwork, iwork, info);
}

void zgesvj_(char* joba, char* jobu, char* jobv, INT* m, INT* n, c128* A, INT* lda, f64* SVA, INT* mv, c128* V, INT* ldv, c128* cwork, INT* lwork, f64* rwork, INT* lrwork, INT* info) {
    zgesvj(joba, jobu, jobv, *m, *n, A, *lda, SVA, *mv, V, *ldv, cwork, *lwork, rwork, *lrwork, info);
}

void zgesdd_(char* jobz, INT* m, INT* n, c128* A, INT* lda, f64* S, c128* U, INT* ldu, c128* VT, INT* ldvt, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* info) {
    zgesdd(jobz, *m, *n, A, *lda, S, U, *ldu, VT, *ldvt, work, *lwork, rwork, iwork, info);
}

void zgesvd_(char* jobu, char* jobvt, INT* m, INT* n, c128* A, INT* lda, f64* S, c128* U, INT* ldu, c128* VT, INT* ldvt, c128* work, INT* lwork, f64* rwork, INT* info) {
    zgesvd(jobu, jobvt, *m, *n, A, *lda, S, U, *ldu, VT, *ldvt, work, *lwork, rwork, info);
}

void zgesvdq_(char* joba, char* jobp, char* jobr, char* jobu, char* jobv, INT* m, INT* n, c128* A, INT* lda, f64* S, c128* U, INT* ldu, c128* V, INT* ldv, INT* numrank, INT* iwork, INT* liwork, c128* cwork, INT* lcwork, f64* rwork, INT* lrwork, INT* info) {
    zgesvdq(joba, jobp, jobr, jobu, jobv, *m, *n, A, *lda, S, U, *ldu, V, *ldv, numrank, iwork, *liwork, cwork, *lcwork, rwork, *lrwork, info);
}

void zgesvdx_(char* jobu, char* jobvt, char* range, INT* m, INT* n, c128* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, INT* ns, f64* S, c128* U, INT* ldu, c128* VT, INT* ldvt, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zgesvdx(jobu, jobvt, range, *m, *n, A, *lda, *vl, *vu, _il, _iu, ns, S, U, *ldu, VT, *ldvt, work, *lwork, rwork, iwork, info);
}

void zgesvx_(char* fact, char* trans, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, INT* ipiv, char* equed, f64* R, f64* C, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zgesvx(fact, trans, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        free(_ipiv0);
    } else {
        zgesvx(fact, trans, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zgetc2_(INT* n, c128* A, INT* lda, INT* ipiv, INT* jpiv, INT* info) {
    zgetc2(*n, A, *lda, ipiv, jpiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    if (jpiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpiv[_i] >= 0) jpiv[_i]++; } }
}

void zgetf2_(INT* m, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zgetf2(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgetrf_(INT* m, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zgetrf(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgetrf2_(INT* m, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zgetrf2(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgetri_(INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zgetri(*n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        zgetri(*n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zgetrs_(char* trans, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgetrs(trans, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zgetsls_(char* trans, INT* m, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zgetsls(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void zgetsqrhrt_(INT* m, INT* n, INT* mb1, INT* nb1, INT* nb2, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* lwork, INT* info) {
    zgetsqrhrt(*m, *n, *mb1, *nb1, *nb2, A, *lda, T, *ldt, work, *lwork, info);
}

void zggbak_(char* job, char* side, INT* n, INT* ilo, INT* ihi, f64* lscale, f64* rscale, INT* m, c128* V, INT* ldv, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zggbak(job, side, *n, _ilo, _ihi, lscale, rscale, *m, V, *ldv, info);
}

void zggbal_(char* job, INT* n, c128* A, INT* lda, c128* B, INT* ldb, INT* ilo, INT* ihi, f64* lscale, f64* rscale, f64* work, INT* info) {
    zggbal(job, *n, A, *lda, B, *ldb, ilo, ihi, lscale, rscale, work, info);
    (*ilo)++;
    (*ihi)++;
}

void zggglm_(INT* n, INT* m, INT* p, c128* A, INT* lda, c128* B, INT* ldb, c128* D, c128* X, c128* Y, c128* work, INT* lwork, INT* info) {
    zggglm(*n, *m, *p, A, *lda, B, *ldb, D, X, Y, work, *lwork, info);
}

void zgges_(char* jobvsl, char* jobvsr, char* sort, zselect2_t selctg, INT* n, c128* A, INT* lda, c128* B, INT* ldb, INT* sdim, c128* alpha, c128* beta, c128* VSL, INT* ldvsl, c128* VSR, INT* ldvsr, c128* work, INT* lwork, f64* rwork, INT* bwork, INT* info) {
    zgges(jobvsl, jobvsr, sort, selctg, *n, A, *lda, B, *ldb, sdim, alpha, beta, VSL, *ldvsl, VSR, *ldvsr, work, *lwork, rwork, bwork, info);
}

void zgges3_(char* jobvsl, char* jobvsr, char* sort, zselect2_t selctg, INT* n, c128* A, INT* lda, c128* B, INT* ldb, INT* sdim, c128* alpha, c128* beta, c128* VSL, INT* ldvsl, c128* VSR, INT* ldvsr, c128* work, INT* lwork, f64* rwork, INT* bwork, INT* info) {
    zgges3(jobvsl, jobvsr, sort, selctg, *n, A, *lda, B, *ldb, sdim, alpha, beta, VSL, *ldvsl, VSR, *ldvsr, work, *lwork, rwork, bwork, info);
}

void zggesx_(char* jobvsl, char* jobvsr, char* sort, zselect2_t selctg, char* sense, INT* n, c128* A, INT* lda, c128* B, INT* ldb, INT* sdim, c128* alpha, c128* beta, c128* VSL, INT* ldvsl, c128* VSR, INT* ldvsr, f64* rconde, f64* rcondv, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* liwork, INT* bwork, INT* info) {
    zggesx(jobvsl, jobvsr, sort, selctg, sense, *n, A, *lda, B, *ldb, sdim, alpha, beta, VSL, *ldvsl, VSR, *ldvsr, rconde, rcondv, work, *lwork, rwork, iwork, *liwork, bwork, info);
}

void zggev_(char* jobvl, char* jobvr, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* alpha, c128* beta, c128* VL, INT* ldvl, c128* VR, INT* ldvr, c128* work, INT* lwork, f64* rwork, INT* info) {
    zggev(jobvl, jobvr, *n, A, *lda, B, *ldb, alpha, beta, VL, *ldvl, VR, *ldvr, work, *lwork, rwork, info);
}

void zggev3_(char* jobvl, char* jobvr, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* alpha, c128* beta, c128* VL, INT* ldvl, c128* VR, INT* ldvr, c128* work, INT* lwork, f64* rwork, INT* info) {
    zggev3(jobvl, jobvr, *n, A, *lda, B, *ldb, alpha, beta, VL, *ldvl, VR, *ldvr, work, *lwork, rwork, info);
}

void zggevx_(char* balanc, char* jobvl, char* jobvr, char* sense, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* alpha, c128* beta, c128* VL, INT* ldvl, c128* VR, INT* ldvr, INT* ilo, INT* ihi, f64* lscale, f64* rscale, f64* abnrm, f64* bbnrm, f64* rconde, f64* rcondv, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* bwork, INT* info) {
    zggevx(balanc, jobvl, jobvr, sense, *n, A, *lda, B, *ldb, alpha, beta, VL, *ldvl, VR, *ldvr, ilo, ihi, lscale, rscale, abnrm, bbnrm, rconde, rcondv, work, *lwork, rwork, iwork, bwork, info);
    (*ilo)++;
    (*ihi)++;
}

void zgghd3_(char* compq, char* compz, INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* B, INT* ldb, c128* Q, INT* ldq, c128* Z, INT* ldz, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zgghd3(compq, compz, *n, _ilo, _ihi, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, work, *lwork, info);
}

void zgghrd_(char* compq, char* compz, INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* B, INT* ldb, c128* Q, INT* ldq, c128* Z, INT* ldz, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zgghrd(compq, compz, *n, _ilo, _ihi, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, info);
}

void zggsvd3_(char* jobu, char* jobv, char* jobq, INT* m, INT* n, INT* p, INT* k, INT* l, c128* A, INT* lda, c128* B, INT* ldb, f64* alpha, f64* beta, c128* U, INT* ldu, c128* V, INT* ldv, c128* Q, INT* ldq, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* info) {
    zggsvd3(jobu, jobv, jobq, *m, *n, *p, k, l, A, *lda, B, *ldb, alpha, beta, U, *ldu, V, *ldv, Q, *ldq, work, *lwork, rwork, iwork, info);
}

void zggsvp3_(char* jobu, char* jobv, char* jobq, INT* m, INT* p, INT* n, c128* A, INT* lda, c128* B, INT* ldb, f64* tola, f64* tolb, INT* k, INT* l, c128* U, INT* ldu, c128* V, INT* ldv, c128* Q, INT* ldq, INT* iwork, f64* rwork, c128* tau, c128* work, INT* lwork, INT* info) {
    zggsvp3(jobu, jobv, jobq, *m, *p, *n, A, *lda, B, *ldb, *tola, *tolb, k, l, U, *ldu, V, *ldv, Q, *ldq, iwork, rwork, tau, work, *lwork, info);
}

void zgglse_(INT* m, INT* n, INT* p, c128* A, INT* lda, c128* B, INT* ldb, c128* C, c128* D, c128* X, c128* work, INT* lwork, INT* info) {
    zgglse(*m, *n, *p, A, *lda, B, *ldb, C, D, X, work, *lwork, info);
}

void zggqrf_(INT* n, INT* m, INT* p, c128* A, INT* lda, c128* taua, c128* B, INT* ldb, c128* taub, c128* work, INT* lwork, INT* info) {
    zggqrf(*n, *m, *p, A, *lda, taua, B, *ldb, taub, work, *lwork, info);
}

void zggrqf_(INT* m, INT* p, INT* n, c128* A, INT* lda, c128* taua, c128* B, INT* ldb, c128* taub, c128* work, INT* lwork, INT* info) {
    zggrqf(*m, *p, *n, A, *lda, taua, B, *ldb, taub, work, *lwork, info);
}

void zgsvj0_(char* jobv, INT* m, INT* n, c128* A, INT* lda, c128* D, f64* SVA, INT* mv, c128* V, INT* ldv, f64* eps, f64* sfmin, f64* tol, INT* nsweep, c128* work, INT* lwork, INT* info) {
    zgsvj0(jobv, *m, *n, A, *lda, D, SVA, *mv, V, *ldv, *eps, *sfmin, *tol, *nsweep, work, *lwork, info);
}

void zgsvj1_(char* jobv, INT* m, INT* n, INT* n1, c128* A, INT* lda, c128* D, f64* SVA, INT* mv, c128* V, INT* ldv, f64* eps, f64* sfmin, f64* tol, INT* nsweep, c128* work, INT* lwork, INT* info) {
    zgsvj1(jobv, *m, *n, *n1, A, *lda, D, SVA, *mv, V, *ldv, *eps, *sfmin, *tol, *nsweep, work, *lwork, info);
}

void zgtcon_(char* norm, INT* n, c128* DL, c128* D, c128* DU, c128* DU2, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgtcon(norm, *n, DL, D, DU, DU2, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zgtrfs_(char* trans, INT* n, INT* nrhs, c128* DL, c128* D, c128* DU, c128* DLF, c128* DF, c128* DUF, c128* DU2, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgtrfs(trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zgtsv_(INT* n, INT* nrhs, c128* DL, c128* D, c128* DU, c128* B, INT* ldb, INT* info) {
    zgtsv(*n, *nrhs, DL, D, DU, B, *ldb, info);
}

void zgtsvx_(char* fact, char* trans, INT* n, INT* nrhs, c128* DL, c128* D, c128* DU, c128* DLF, c128* DF, c128* DUF, c128* DU2, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zgtsvx(fact, trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        free(_ipiv0);
    } else {
        zgtsvx(fact, trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zgttrf_(INT* n, c128* DL, c128* D, c128* DU, c128* DU2, INT* ipiv, INT* info) {
    zgttrf(*n, DL, D, DU, DU2, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zgttrs_(char* trans, INT* n, INT* nrhs, c128* DL, c128* D, c128* DU, c128* DU2, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgttrs(trans, *n, *nrhs, DL, D, DU, DU2, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zgtts2_(INT* itrans, INT* n, INT* nrhs, c128* DL, c128* D, c128* DU, c128* DU2, INT* ipiv, c128* B, INT* ldb) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zgtts2(*itrans, *n, *nrhs, DL, D, DU, DU2, _ipiv0, B, *ldb);
    free(_ipiv0);
}

void zhb2st_kernels_(char* uplo, INT* wantz, INT* ttype, INT* st, INT* ed, INT* sweep, INT* n, INT* nb, INT* ib, c128* A, INT* lda, c128* V, c128* TAU, INT* ldvt, c128* WORK) {
    zhb2st_kernels(uplo, *wantz, *ttype, *st, *ed, *sweep, *n, *nb, *ib, A, *lda, V, TAU, *ldvt, WORK);
}

void zhbev_(char* jobz, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* info) {
    zhbev(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, rwork, info);
}

void zhbev_2stage_(char* jobz, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* info) {
    zhbev_2stage(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, rwork, info);
}

void zhbevd_(char* jobz, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zhbevd(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zhbevd_2stage_(char* jobz, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zhbevd_2stage(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zhbevx_(char* jobz, char* range, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, c128* Q, INT* ldq, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zhbevx(jobz, range, uplo, *n, *kd, AB, *ldab, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, rwork, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void zhbevx_2stage_(char* jobz, char* range, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, c128* Q, INT* ldq, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zhbevx_2stage(jobz, range, uplo, *n, *kd, AB, *ldab, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, rwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void zhbgst_(char* vect, char* uplo, INT* n, INT* ka, INT* kb, c128* AB, INT* ldab, c128* BB, INT* ldbb, c128* X, INT* ldx, c128* work, f64* rwork, INT* info) {
    zhbgst(vect, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, X, *ldx, work, rwork, info);
}

void zhbgv_(char* jobz, char* uplo, INT* n, INT* ka, INT* kb, c128* AB, INT* ldab, c128* BB, INT* ldbb, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* info) {
    zhbgv(jobz, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, W, Z, *ldz, work, rwork, info);
}

void zhbgvd_(char* jobz, char* uplo, INT* n, INT* ka, INT* kb, c128* AB, INT* ldab, c128* BB, INT* ldbb, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zhbgvd(jobz, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, W, Z, *ldz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zhbgvx_(char* jobz, char* range, char* uplo, INT* n, INT* ka, INT* kb, c128* AB, INT* ldab, c128* BB, INT* ldbb, c128* Q, INT* ldq, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zhbgvx(jobz, range, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, rwork, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void zhbtrd_(char* vect, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* D, f64* E, c128* Q, INT* ldq, c128* work, INT* info) {
    zhbtrd(vect, uplo, *n, *kd, AB, *ldab, D, E, Q, *ldq, work, info);
}

void zhfrk_(char* transr, char* uplo, char* trans, INT* n, INT* k, f64* alpha, c128* A, INT* lda, f64* beta, c128* C) {
    zhfrk(transr, uplo, trans, *n, *k, *alpha, A, *lda, *beta, C);
}

void zhecon_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhecon(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zhecon_3_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhecon_3(uplo, *n, A, *lda, E, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zhecon_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhecon_rook(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zheequb_(char* uplo, INT* n, c128* A, INT* lda, f64* S, f64* scond, f64* amax, c128* work, INT* info) {
    zheequb(uplo, *n, A, *lda, S, scond, amax, work, info);
}

void zheev_(char* jobz, char* uplo, INT* n, c128* A, INT* lda, f64* W, c128* work, INT* lwork, f64* rwork, INT* info) {
    zheev(jobz, uplo, *n, A, *lda, W, work, *lwork, rwork, info);
}

void zheev_2stage_(char* jobz, char* uplo, INT* n, c128* A, INT* lda, f64* W, c128* work, INT* lwork, f64* rwork, INT* info) {
    zheev_2stage(jobz, uplo, *n, A, *lda, W, work, *lwork, rwork, info);
}

void zheevd_(char* jobz, char* uplo, INT* n, c128* A, INT* lda, f64* W, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zheevd(jobz, uplo, *n, A, *lda, W, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zheevd_2stage_(char* jobz, char* uplo, INT* n, c128* A, INT* lda, f64* W, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zheevd_2stage(jobz, uplo, *n, A, *lda, W, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zheevr_(char* jobz, char* range, char* uplo, INT* n, c128* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, INT* isuppz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zheevr(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz && ((*jobz == 'V' || *jobz == 'v') && (*range == 'A' || *range == 'a'))) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void zheevr_2stage_(char* jobz, char* range, char* uplo, INT* n, c128* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, INT* isuppz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zheevr_2stage(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz && ((*jobz == 'V' || *jobz == 'v') && (*range == 'A' || *range == 'a'))) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void zheevx_(char* jobz, char* range, char* uplo, INT* n, c128* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zheevx(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, rwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void zheevx_2stage_(char* jobz, char* range, char* uplo, INT* n, c128* A, INT* lda, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zheevx_2stage(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, rwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void zhegs2_(INT* itype, char* uplo, INT* n, c128* A, INT* lda, c128* B, INT* ldb, INT* info) {
    zhegs2(*itype, uplo, *n, A, *lda, B, *ldb, info);
}

void zhegst_(INT* itype, char* uplo, INT* n, c128* A, INT* lda, c128* B, INT* ldb, INT* info) {
    zhegst(*itype, uplo, *n, A, *lda, B, *ldb, info);
}

void zhegv_(INT* itype, char* jobz, char* uplo, INT* n, c128* A, INT* lda, c128* B, INT* ldb, f64* W, c128* work, INT* lwork, f64* rwork, INT* info) {
    zhegv(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, rwork, info);
}

void zhegv_2stage_(INT* itype, char* jobz, char* uplo, INT* n, c128* A, INT* lda, c128* B, INT* ldb, f64* W, c128* work, INT* lwork, f64* rwork, INT* info) {
    zhegv_2stage(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, rwork, info);
}

void zhegvd_(INT* itype, char* jobz, char* uplo, INT* n, c128* A, INT* lda, c128* B, INT* ldb, f64* W, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zhegvd(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zhegvx_(INT* itype, char* jobz, char* range, char* uplo, INT* n, c128* A, INT* lda, c128* B, INT* ldb, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    zhegvx(*itype, jobz, range, uplo, *n, A, *lda, B, *ldb, *vl, *vu, *il, *iu, *abstol, m, W, Z, *ldz, work, *lwork, rwork, iwork, ifail, info);
}

void zherfs_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zherfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zhesv_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zhesv(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhesv_aa_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zhesv_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhesv_aa_2stage_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* TB, INT* ltb, INT* ipiv, INT* ipiv2, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zhesv_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, ipiv, ipiv2, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void zhesv_rk_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* E, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zhesv_rk(uplo, *n, *nrhs, A, *lda, E, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhesv_rook_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zhesv_rook(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhesvx_(char* fact, char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, INT* lwork, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zhesvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, rwork, info);
        free(_ipiv0);
    } else {
        zhesvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zheswapr_(char* uplo, INT* n, c128* A, INT* lda, INT* i1, INT* i2) {
    zheswapr(uplo, *n, A, *lda, *i1, *i2);
}

void zhetd2_(char* uplo, INT* n, c128* A, INT* lda, f64* D, f64* E, c128* tau, INT* info) {
    zhetd2(uplo, *n, A, *lda, D, E, tau, info);
}

void zhetrd_(char* uplo, INT* n, c128* A, INT* lda, f64* D, f64* E, c128* tau, c128* work, INT* lwork, INT* info) {
    zhetrd(uplo, *n, A, *lda, D, E, tau, work, *lwork, info);
}

void zhetrd_2stage_(char* vect, char* uplo, INT* n, c128* A, INT* lda, f64* D, f64* E, c128* tau, c128* hous2, INT* lhous2, c128* work, INT* lwork, INT* info) {
    zhetrd_2stage(vect, uplo, *n, A, *lda, D, E, tau, hous2, *lhous2, work, *lwork, info);
}

void zhetrd_hb2st_(char* stage1, char* vect, char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* D, f64* E, c128* hous, INT* lhous, c128* work, INT* lwork, INT* info) {
    zhetrd_hb2st(stage1, vect, uplo, *n, *kd, AB, *ldab, D, E, hous, *lhous, work, *lwork, info);
}

void zhetrd_he2hb_(char* uplo, INT* n, INT* kd, c128* A, INT* lda, c128* AB, INT* ldab, c128* tau, c128* work, INT* lwork, INT* info) {
    zhetrd_he2hb(uplo, *n, *kd, A, *lda, AB, *ldab, tau, work, *lwork, info);
}

void zhetf2_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zhetf2(uplo, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zhetf2_rk_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, INT* info) {
    zhetf2_rk(uplo, *n, A, *lda, E, ipiv, info);
}

void zhetf2_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zhetf2_rook(uplo, *n, A, *lda, ipiv, info);
}

void zhetrf_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zhetrf(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhetrf_aa_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zhetrf_aa(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhetrf_aa_2stage_(char* uplo, INT* n, c128* A, INT* lda, c128* TB, INT* ltb, INT* ipiv, INT* ipiv2, c128* work, INT* lwork, INT* info) {
    zhetrf_aa_2stage(uplo, *n, A, *lda, TB, *ltb, ipiv, ipiv2, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void zhetrf_rk_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zhetrf_rk(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhetrf_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zhetrf_rook(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhetri_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetri(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void zhetri2_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zhetri2(uplo, *n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        zhetri2(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zhetri2x_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetri2x(uplo, *n, A, *lda, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void zhetri_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetri_rook(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void zhetri_3_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zhetri_3(uplo, *n, A, *lda, E, _ipiv0, work, *lwork, info);
    } else {
        zhetri_3(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zhetri_3x_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, c128* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetri_3x(uplo, *n, A, *lda, E, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void zhetrs_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetrs(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zhetrs2_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetrs2(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, info);
    free(_ipiv0);
}

void zhetrs_3_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* E, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetrs_3(uplo, *n, *nrhs, A, *lda, E, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zhetrs_aa_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zhetrs_aa(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, *lwork, info);
    } else {
        zhetrs_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zhetrs_aa_2stage_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* TB, INT* ltb, INT* ipiv, INT* ipiv2, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    INT* _ipiv20 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (ipiv2) {
        INT _ipiv2_sz = *n;
        _ipiv20 = (INT*)malloc(_ipiv2_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv2_sz; _i++) _ipiv20[_i] = (ipiv2[_i] > 0) ? ipiv2[_i] - 1 : ipiv2[_i];
    }
    zhetrs_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, _ipiv0, _ipiv20, B, *ldb, info);
    free(_ipiv0);
    free(_ipiv20);
}

void zhetrs_rook_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhetrs_rook(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zhgeqz_(char* job, char* compq, char* compz, INT* n, INT* ilo, INT* ihi, c128* H, INT* ldh, c128* T, INT* ldt, c128* alpha, c128* beta, c128* Q, INT* ldq, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zhgeqz(job, compq, compz, *n, _ilo, _ihi, H, *ldh, T, *ldt, alpha, beta, Q, *ldq, Z, *ldz, work, *lwork, rwork, info);
}

void zhpcon_(char* uplo, INT* n, c128* AP, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhpcon(uplo, *n, AP, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zhpev_(char* jobz, char* uplo, INT* n, c128* AP, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* info) {
    zhpev(jobz, uplo, *n, AP, W, Z, *ldz, work, rwork, info);
}

void zhpevd_(char* jobz, char* uplo, INT* n, c128* AP, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zhpevd(jobz, uplo, *n, AP, W, Z, *ldz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zhpevx_(char* jobz, char* range, char* uplo, INT* n, c128* AP, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zhpevx(jobz, range, uplo, *n, AP, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, rwork, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void zhpgst_(INT* itype, char* uplo, INT* n, c128* AP, c128* BP, INT* info) {
    zhpgst(*itype, uplo, *n, AP, BP, info);
}

void zhpgv_(INT* itype, char* jobz, char* uplo, INT* n, c128* AP, c128* BP, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* info) {
    zhpgv(*itype, jobz, uplo, *n, AP, BP, W, Z, *ldz, work, rwork, info);
}

void zhpgvd_(INT* itype, char* jobz, char* uplo, INT* n, c128* AP, c128* BP, f64* W, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zhpgvd(*itype, jobz, uplo, *n, AP, BP, W, Z, *ldz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zhpgvx_(INT* itype, char* jobz, char* range, char* uplo, INT* n, c128* AP, c128* BP, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, c128* work, f64* rwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zhpgvx(*itype, jobz, range, uplo, *n, AP, BP, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, rwork, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void zhprfs_(char* uplo, INT* n, INT* nrhs, c128* AP, c128* AFP, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhprfs(uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zhpsv_(char* uplo, INT* n, INT* nrhs, c128* AP, INT* ipiv, c128* B, INT* ldb, INT* info) {
    zhpsv(uplo, *n, *nrhs, AP, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zhpsvx_(char* fact, char* uplo, INT* n, INT* nrhs, c128* AP, c128* AFP, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zhpsvx(fact, uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        free(_ipiv0);
    } else {
        zhpsvx(fact, uplo, *n, *nrhs, AP, AFP, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zhptrd_(char* uplo, INT* n, c128* AP, f64* d, f64* e, c128* tau, INT* info) {
    zhptrd(uplo, *n, AP, d, e, tau, info);
}

void zhptrf_(char* uplo, INT* n, c128* AP, INT* ipiv, INT* info) {
    zhptrf(uplo, *n, AP, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zhptri_(char* uplo, INT* n, c128* AP, INT* ipiv, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhptri(uplo, *n, AP, _ipiv0, work, info);
    free(_ipiv0);
}

void zhptrs_(char* uplo, INT* n, INT* nrhs, c128* AP, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zhptrs(uplo, *n, *nrhs, AP, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zhsein_(char* side, char* eigsrc, char* initv, INT* select, INT* n, c128* H, INT* ldh, c128* W, c128* VL, INT* ldvl, c128* VR, INT* ldvr, INT* mm, INT* m, c128* work, f64* rwork, INT* ifaill, INT* ifailr, INT* info) {
    zhsein(side, eigsrc, initv, select, *n, H, *ldh, W, VL, *ldvl, VR, *ldvr, *mm, m, work, rwork, ifaill, ifailr, info);
    if (ifaill) { INT _sz = *mm; for (INT _i = 0; _i < _sz; _i++) ifaill[_i]++; }
    if (ifailr) { INT _sz = *mm; for (INT _i = 0; _i < _sz; _i++) ifailr[_i]++; }
}

void zhseqr_(char* job, char* compz, INT* n, INT* ilo, INT* ihi, c128* H, INT* ldh, c128* W, c128* Z, INT* ldz, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zhseqr(job, compz, *n, _ilo, _ihi, H, *ldh, W, Z, *ldz, work, *lwork, info);
}

void zla_gbamv_(INT* trans, INT* m, INT* n, INT* kl, INT* ku, f64* alpha, c128* AB, INT* ldab, c128* X, INT* incx, f64* beta, f64* Y, INT* incy) {
    zla_gbamv(*trans, *m, *n, *kl, *ku, *alpha, AB, *ldab, X, *incx, *beta, Y, *incy);
}

void zla_geamv_(INT* trans, INT* m, INT* n, f64* alpha, c128* A, INT* lda, c128* X, INT* incx, f64* beta, f64* Y, INT* incy) {
    zla_geamv(*trans, *m, *n, *alpha, A, *lda, X, *incx, *beta, Y, *incy);
}

void zla_lin_berr_(INT* n, INT* nz, INT* nrhs, c128* RES, f64* AYB, f64* BERR) {
    zla_lin_berr(*n, *nz, *nrhs, RES, AYB, BERR);
}

void zlabrd_(INT* m, INT* n, INT* nb, c128* A, INT* lda, f64* D, f64* E, c128* tauq, c128* taup, c128* X, INT* ldx, c128* Y, INT* ldy) {
    zlabrd(*m, *n, *nb, A, *lda, D, E, tauq, taup, X, *ldx, Y, *ldy);
}

void zlacgv_(INT* n, c128* X, INT* incx) {
    zlacgv(*n, X, *incx);
}

void zlacn2_(INT* n, c128* V, c128* X, f64* est, INT* kase, INT* isave) {
    zlacn2(*n, V, X, est, kase, isave);
}

void zlacon_(INT* n, c128* V, c128* X, f64* est, INT* kase) {
    zlacon(*n, V, X, est, kase);
}

void zlacp2_(char* uplo, INT* m, INT* n, f64* A, INT* lda, c128* B, INT* ldb) {
    zlacp2(uplo, *m, *n, A, *lda, B, *ldb);
}

void zlacpy_(char* uplo, INT* m, INT* n, c128* A, INT* lda, c128* B, INT* ldb) {
    zlacpy(uplo, *m, *n, A, *lda, B, *ldb);
}

void zlacrm_(INT* m, INT* n, c128* A, INT* lda, f64* B, INT* ldb, c128* C, INT* ldc, f64* rwork) {
    zlacrm(*m, *n, A, *lda, B, *ldb, C, *ldc, rwork);
}

void zlacrt_(INT* n, c128* cx, INT* incx, c128* cy, INT* incy, c128* c, c128* s) {
    zlacrt(*n, cx, *incx, cy, *incy, *c, *s);
}

void zlaed0_(INT* qsiz, INT* n, f64* D, f64* E, c128* Q, INT* ldq, c128* qstore, INT* ldqs, f64* rwork, INT* iwork, INT* info) {
    zlaed0(*qsiz, *n, D, E, Q, *ldq, qstore, *ldqs, rwork, iwork, info);
}

void zlaed7_(INT* n, INT* cutpnt, INT* qsiz, INT* tlvls, INT* curlvl, INT* curpbm, f64* D, c128* Q, INT* ldq, f64* rho, INT* indxq, f64* qstore, INT* qptr, INT* prmptr, INT* perm, INT* givptr, INT* givcol, f64* givnum, c128* work, f64* rwork, INT* iwork, INT* info) {
    zlaed7(*n, *cutpnt, *qsiz, *tlvls, *curlvl, *curpbm, D, Q, *ldq, *rho, indxq, qstore, qptr, prmptr, perm, givptr, givcol, givnum, work, rwork, iwork, info);
}

void zlaed8_(INT* K, INT* n, INT* qsiz, c128* Q, INT* ldq, f64* D, f64* rho, INT* cutpnt, f64* Z, f64* dlambda, c128* Q2, INT* ldq2, f64* W, INT* indxp, INT* indx, INT* indxq, INT* perm, INT* givptr, INT* givcol, f64* givnum, INT* info) {
    zlaed8(K, *n, *qsiz, Q, *ldq, D, rho, *cutpnt, Z, dlambda, Q2, *ldq2, W, indxp, indx, indxq, perm, givptr, givcol, givnum, info);
}

void zlaein_(INT* rightv, INT* noinit, INT* n, c128* H, INT* ldh, c128* w, c128* V, c128* B, INT* ldb, f64* rwork, f64* eps3, f64* smlnum, INT* info) {
    zlaein(*rightv, *noinit, *n, H, *ldh, *w, V, B, *ldb, rwork, *eps3, *smlnum, info);
}

void zlaesy_(c128* a, c128* b, c128* c, c128* rt1, c128* rt2, c128* evscal, c128* cs1, c128* sn1) {
    zlaesy(*a, *b, *c, rt1, rt2, evscal, cs1, sn1);
}

void zlaev2_(c128* a, c128* b, c128* c, f64* rt1, f64* rt2, f64* cs1, c128* sn1) {
    zlaev2(*a, *b, *c, rt1, rt2, cs1, sn1);
}

void zlag2c_(INT* m, INT* n, c128* A, INT* lda, c64* SA, INT* ldsa, INT* info) {
    zlag2c(*m, *n, A, *lda, SA, *ldsa, info);
}

void zlags2_(INT* upper, f64* a1, c128* a2, f64* a3, f64* b1, c128* b2, f64* b3, f64* csu, c128* snu, f64* csv, c128* snv, f64* csq, c128* snq) {
    zlags2(*upper, *a1, *a2, *a3, *b1, *b2, *b3, csu, snu, csv, snv, csq, snq);
}

void zlagtm_(char* trans, INT* n, INT* nrhs, f64* alpha, c128* DL, c128* D, c128* DU, c128* X, INT* ldx, f64* beta, c128* B, INT* ldb) {
    zlagtm(trans, *n, *nrhs, *alpha, DL, D, DU, X, *ldx, *beta, B, *ldb);
}

void zlals0_(INT* icompq, INT* nl, INT* nr, INT* sqre, INT* nrhs, c128* B, INT* ldb, c128* BX, INT* ldbx, INT* perm, INT* givptr, INT* givcol, INT* ldgcol, f64* givnum, INT* ldgnum, f64* poles, f64* difl, f64* difr, f64* Z, INT* k, f64* c, f64* s, f64* rwork, INT* info) {
    zlals0(*icompq, *nl, *nr, *sqre, *nrhs, B, *ldb, BX, *ldbx, perm, *givptr, givcol, *ldgcol, givnum, *ldgnum, poles, difl, difr, Z, *k, *c, *s, rwork, info);
}

void zlalsa_(INT* icompq, INT* smlsiz, INT* n, INT* nrhs, c128* B, INT* ldb, c128* BX, INT* ldbx, f64* U, INT* ldu, f64* VT, INT* K, f64* difl, f64* difr, f64* Z, f64* poles, INT* givptr, INT* givcol, INT* ldgcol, INT* perm, f64* givnum, f64* C, f64* S, f64* rwork, INT* iwork, INT* info) {
    zlalsa(*icompq, *smlsiz, *n, *nrhs, B, *ldb, BX, *ldbx, U, *ldu, VT, K, difl, difr, Z, poles, givptr, givcol, *ldgcol, perm, givnum, C, S, rwork, iwork, info);
}

void zlalsd_(char* uplo, INT* smlsiz, INT* n, INT* nrhs, f64* D, f64* E, c128* B, INT* ldb, f64* rcond, INT* rank, c128* work, f64* rwork, INT* iwork, INT* info) {
    zlalsd(uplo, *smlsiz, *n, *nrhs, D, E, B, *ldb, *rcond, rank, work, rwork, iwork, info);
}

void zlahr2_(INT* n, INT* k, INT* nb, c128* A, INT* lda, c128* tau, c128* T, INT* ldt, c128* Y, INT* ldy) {
    zlahr2(*n, *k, *nb, A, *lda, tau, T, *ldt, Y, *ldy);
}

void zlahqr_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, c128* H, INT* ldh, c128* W, INT* iloz, INT* ihiz, c128* Z, INT* ldz, INT* info) {
    zlahqr(*wantt, *wantz, *n, *ilo, *ihi, H, *ldh, W, *iloz, *ihiz, Z, *ldz, info);
}

void zlaic1_(INT* job, INT* j, c128* x, f64* sest, c128* w, c128* gamma_, f64* sestpr, c128* s, c128* c) {
    zlaic1(*job, *j, x, *sest, w, *gamma_, sestpr, s, c);
}

void zlamswlq_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zlamswlq(side, trans, *m, *n, *k, *mb, *nb, A, *lda, T, *ldt, C, *ldc, work, *lwork, info);
}

void zlamtsqr_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zlamtsqr(side, trans, *m, *n, *k, *mb, *nb, A, *lda, T, *ldt, C, *ldc, work, *lwork, info);
}

void zlapll_(INT* n, c128* x, INT* incx, c128* y, INT* incy, f64* ssmin) {
    zlapll(*n, x, *incx, y, *incy, ssmin);
}

void zlapmr_(INT* forwrd, INT* m, INT* n, c128* X, INT* ldx, INT* K) {
    zlapmr(*forwrd, *m, *n, X, *ldx, K);
}

void zlapmt_(INT* forwrd, INT* m, INT* n, c128* X, INT* ldx, INT* K) {
    zlapmt(*forwrd, *m, *n, X, *ldx, K);
}

void zlaqgb_(INT* m, INT* n, INT* kl, INT* ku, c128* AB, INT* ldab, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, char* equed) {
    zlaqgb(*m, *n, *kl, *ku, AB, *ldab, R, C, *rowcnd, *colcnd, *amax, equed);
}

void zlaqge_(INT* m, INT* n, c128* A, INT* lda, f64* R, f64* C, f64* rowcnd, f64* colcnd, f64* amax, char* equed) {
    zlaqge(*m, *n, A, *lda, R, C, *rowcnd, *colcnd, *amax, equed);
}

void zlaqhb_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* S, f64* scond, f64* amax, char* equed) {
    zlaqhb(uplo, *n, *kd, AB, *ldab, S, *scond, *amax, equed);
}

void zlaqhe_(char* uplo, INT* n, c128* A, INT* lda, f64* S, f64* scond, f64* amax, char* equed) {
    zlaqhe(uplo, *n, A, *lda, S, *scond, *amax, equed);
}

void zlaqhp_(char* uplo, INT* n, c128* AP, f64* S, f64* scond, f64* amax, char* equed) {
    zlaqhp(uplo, *n, AP, S, *scond, *amax, equed);
}

void zlaqp2_(INT* m, INT* n, INT* offset, c128* A, INT* lda, INT* jpvt, c128* tau, f64* vn1, f64* vn2, c128* work) {
    zlaqp2(*m, *n, *offset, A, *lda, jpvt, tau, vn1, vn2, work);
    if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
}

void zlaqp2rk_(INT* m, INT* n, INT* nrhs, INT* ioffset, INT* kmax, f64* abstol, f64* reltol, INT* kp1, f64* maxc2nrm, c128* A, INT* lda, INT* K, f64* maxc2nrmk, f64* relmaxc2nrmk, INT* jpiv, c128* tau, f64* vn1, f64* vn2, c128* work, INT* info) {
    zlaqp2rk(*m, *n, *nrhs, *ioffset, *kmax, *abstol, *reltol, *kp1, *maxc2nrm, A, *lda, K, maxc2nrmk, relmaxc2nrmk, jpiv, tau, vn1, vn2, work, info);
}

void zlaqp3rk_(INT* m, INT* n, INT* nrhs, INT* ioffset, INT* nb, f64* abstol, f64* reltol, INT* kp1, f64* maxc2nrm, c128* A, INT* lda, INT* done, INT* KB, f64* maxc2nrmk, f64* relmaxc2nrmk, INT* jpiv, c128* tau, f64* vn1, f64* vn2, c128* auxv, c128* F, INT* ldf, INT* iwork, INT* info) {
    zlaqp3rk(*m, *n, *nrhs, *ioffset, nb, *abstol, *reltol, *kp1, *maxc2nrm, A, *lda, done, KB, maxc2nrmk, relmaxc2nrmk, jpiv, tau, vn1, vn2, auxv, F, *ldf, iwork, info);
}

void zlaqps_(INT* m, INT* n, INT* offset, INT* nb, INT* kb, c128* A, INT* lda, INT* jpvt, c128* tau, f64* vn1, f64* vn2, c128* auxv, c128* F, INT* ldf) {
    zlaqps(*m, *n, *offset, *nb, kb, A, *lda, jpvt, tau, vn1, vn2, auxv, F, *ldf);
    if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
}

void zlaqr0_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, c128* H, INT* ldh, c128* W, INT* iloz, INT* ihiz, c128* Z, INT* ldz, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zlaqr0(*wantt, *wantz, *n, _ilo, _ihi, H, *ldh, W, *iloz, *ihiz, Z, *ldz, work, *lwork, info);
}

void zlaqr1_(INT* n, c128* H, INT* ldh, c128* s1, c128* s2, c128* v) {
    zlaqr1(*n, H, *ldh, *s1, *s2, v);
}

void zlaqr2_(INT* wantt, INT* wantz, INT* n, INT* ktop, INT* kbot, INT* nw, c128* H, INT* ldh, INT* iloz, INT* ihiz, c128* Z, INT* ldz, INT* ns, INT* nd, c128* SH, c128* V, INT* ldv, INT* nh, c128* T, INT* ldt, INT* nv, c128* WV, INT* ldwv, c128* work, INT* lwork) {
    zlaqr2(*wantt, *wantz, *n, *ktop, *kbot, *nw, H, *ldh, *iloz, *ihiz, Z, *ldz, ns, nd, SH, V, *ldv, *nh, T, *ldt, *nv, WV, *ldwv, work, *lwork);
}

void zlaqr3_(INT* wantt, INT* wantz, INT* n, INT* ktop, INT* kbot, INT* nw, c128* H, INT* ldh, INT* iloz, INT* ihiz, c128* Z, INT* ldz, INT* ns, INT* nd, c128* SH, c128* V, INT* ldv, INT* nh, c128* T, INT* ldt, INT* nv, c128* WV, INT* ldwv, c128* work, INT* lwork) {
    zlaqr3(*wantt, *wantz, *n, *ktop, *kbot, *nw, H, *ldh, *iloz, *ihiz, Z, *ldz, ns, nd, SH, V, *ldv, *nh, T, *ldt, *nv, WV, *ldwv, work, *lwork);
}

void zlaqr4_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, c128* H, INT* ldh, c128* W, INT* iloz, INT* ihiz, c128* Z, INT* ldz, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zlaqr4(*wantt, *wantz, *n, _ilo, _ihi, H, *ldh, W, *iloz, *ihiz, Z, *ldz, work, *lwork, info);
}

void zlaqr5_(INT* wantt, INT* wantz, INT* kacc22, INT* n, INT* ktop, INT* kbot, INT* nshfts, c128* S, c128* H, INT* ldh, INT* iloz, INT* ihiz, c128* Z, INT* ldz, c128* V, INT* ldv, c128* U, INT* ldu, INT* nv, c128* WV, INT* ldwv, INT* nh, c128* WH, INT* ldwh) {
    zlaqr5(*wantt, *wantz, *kacc22, *n, *ktop, *kbot, *nshfts, S, H, *ldh, *iloz, *ihiz, Z, *ldz, V, *ldv, U, *ldu, *nv, WV, *ldwv, *nh, WH, *ldwh);
}

void zlaqsb_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* S, f64* scond, f64* amax, char* equed) {
    zlaqsb(uplo, *n, *kd, AB, *ldab, S, *scond, *amax, equed);
}

void zlaqsp_(char* uplo, INT* n, c128* AP, f64* S, f64* scond, f64* amax, char* equed) {
    zlaqsp(uplo, *n, AP, S, *scond, *amax, equed);
}

void zlaqsy_(char* uplo, INT* n, c128* A, INT* lda, f64* S, f64* scond, f64* amax, char* equed) {
    zlaqsy(uplo, *n, A, *lda, S, *scond, *amax, equed);
}

void zlaqz0_(char* wants, char* wantq, char* wantz, INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* B, INT* ldb, c128* alpha, c128* beta, c128* Q, INT* ldq, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* rec, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zlaqz0(wants, wantq, wantz, *n, _ilo, _ihi, A, *lda, B, *ldb, alpha, beta, Q, *ldq, Z, *ldz, work, *lwork, rwork, *rec, info);
}

void zlaqz1_(INT* ilq, INT* ilz, INT* k, INT* istartm, INT* istopm, INT* ihi, c128* A, INT* lda, c128* B, INT* ldb, INT* nq, INT* qstart, c128* Q, INT* ldq, INT* nz, INT* zstart, c128* Z, INT* ldz) {
    zlaqz1(*ilq, *ilz, *k, *istartm, *istopm, *ihi, A, *lda, B, *ldb, *nq, *qstart, Q, *ldq, *nz, *zstart, Z, *ldz);
}

void zlaqz2_(INT* ilschur, INT* ilq, INT* ilz, INT* n, INT* ilo, INT* ihi, INT* nw, c128* A, INT* lda, c128* B, INT* ldb, c128* Q, INT* ldq, c128* Z, INT* ldz, INT* ns, INT* nd, c128* alpha, c128* beta, c128* QC, INT* ldqc, c128* ZC, INT* ldzc, c128* work, INT* lwork, f64* rwork, INT* rec, INT* info) {
    zlaqz2(*ilschur, *ilq, *ilz, *n, *ilo, *ihi, *nw, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, ns, nd, alpha, beta, QC, *ldqc, ZC, *ldzc, work, *lwork, rwork, *rec, info);
}

void zlaqz3_(INT* ilschur, INT* ilq, INT* ilz, INT* n, INT* ilo, INT* ihi, INT* nshifts, INT* nblock_desired, c128* alpha, c128* beta, c128* A, INT* lda, c128* B, INT* ldb, c128* Q, INT* ldq, c128* Z, INT* ldz, c128* QC, INT* ldqc, c128* ZC, INT* ldzc, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zlaqz3(*ilschur, *ilq, *ilz, *n, _ilo, _ihi, *nshifts, *nblock_desired, alpha, beta, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, QC, *ldqc, ZC, *ldzc, work, *lwork, info);
}

void zlar1v_(INT* n, INT* b1, INT* bn, f64* lambda, f64* D, f64* L, f64* LD, f64* LLD, f64* pivmin, f64* gaptol, c128* Z, INT* wantnc, INT* negcnt, f64* ztz, f64* mingma, INT* r, INT* isuppz, f64* nrminv, f64* resid, f64* rqcorr, f64* work) {
    zlar1v(*n, *b1, *bn, *lambda, D, L, LD, LLD, *pivmin, *gaptol, Z, *wantnc, negcnt, ztz, mingma, r, isuppz, nrminv, resid, rqcorr, work);
}

void zlar2v_(INT* n, c128* X, c128* Y, c128* Z, INT* incx, f64* C, c128* S, INT* incc) {
    zlar2v(*n, X, Y, Z, *incx, C, S, *incc);
}

void zlarcm_(INT* m, INT* n, f64* A, INT* lda, c128* B, INT* ldb, c128* C, INT* ldc, f64* rwork) {
    zlarcm(*m, *n, A, *lda, B, *ldb, C, *ldc, rwork);
}

void zlarf_(char* side, INT* m, INT* n, c128* v, INT* incv, c128* tau, c128* C, INT* ldc, c128* work) {
    zlarf(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void zlarf1f_(char* side, INT* m, INT* n, c128* v, INT* incv, c128* tau, c128* C, INT* ldc, c128* work) {
    zlarf1f(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void zlarf1l_(char* side, INT* m, INT* n, c128* v, INT* incv, c128* tau, c128* C, INT* ldc, c128* work) {
    zlarf1l(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void zlarfb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, c128* V, INT* ldv, c128* T, INT* ldt, c128* C, INT* ldc, c128* work, INT* ldwork) {
    zlarfb(side, trans, direct, storev, *m, *n, *k, V, *ldv, T, *ldt, C, *ldc, work, *ldwork);
}

void zlarfb_gett_(char* ident, INT* m, INT* n, INT* k, c128* T, INT* ldt, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* ldwork) {
    zlarfb_gett(ident, *m, *n, *k, T, *ldt, A, *lda, B, *ldb, work, *ldwork);
}

void zlarfg_(INT* n, c128* alpha, c128* x, INT* incx, c128* tau) {
    zlarfg(*n, alpha, x, *incx, tau);
}

void zlarfgp_(INT* n, c128* alpha, c128* x, INT* incx, c128* tau) {
    zlarfgp(*n, alpha, x, *incx, tau);
}

void zlarft_(char* direct, char* storev, INT* n, INT* k, c128* V, INT* ldv, c128* tau, c128* T, INT* ldt) {
    zlarft(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void zlarft_lvl2_(char* direct, char* storev, INT* n, INT* k, c128* V, INT* ldv, c128* tau, c128* T, INT* ldt) {
    zlarft_lvl2(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void zlarfx_(char* side, INT* m, INT* n, c128* v, c128* tau, c128* C, INT* ldc, c128* work) {
    zlarfx(side, *m, *n, v, *tau, C, *ldc, work);
}

void zlarfy_(char* uplo, INT* n, c128* V, INT* incv, c128* tau, c128* C, INT* ldc, c128* work) {
    zlarfy(uplo, *n, V, *incv, *tau, C, *ldc, work);
}

void zlargv_(INT* n, c128* X, INT* incx, c128* Y, INT* incy, f64* C, INT* incc) {
    zlargv(*n, X, *incx, Y, *incy, C, *incc);
}

void zlarnv_(INT* idist, INT* iseed, INT* n, c128* X) {
    zlarnv(*idist, iseed, *n, X);
}

void zlarrv_(INT* n, f64* vl, f64* vu, f64* D, f64* L, f64* pivmin, INT* isplit, INT* m, INT* dol, INT* dou, f64* minrgp, f64* rtol1, f64* rtol2, f64* W, f64* werr, f64* wgap, INT* iblock, INT* indexw, f64* gers, c128* Z, INT* ldz, INT* isuppz, f64* work, INT* iwork, INT* info) {
    INT* _isplit0 = NULL;
    INT* _iblock0 = NULL;
    INT* _indexw0 = NULL;
    if (isplit) {
        INT _isplit_sz = *n;
        _isplit0 = (INT*)malloc(_isplit_sz * sizeof(INT));
        for (INT _i = 0; _i < _isplit_sz; _i++) _isplit0[_i] = (isplit[_i] > 0) ? isplit[_i] - 1 : isplit[_i];
    }
    if (iblock) {
        INT _iblock_sz = *m;
        _iblock0 = (INT*)malloc(_iblock_sz * sizeof(INT));
        for (INT _i = 0; _i < _iblock_sz; _i++) _iblock0[_i] = (iblock[_i] > 0) ? iblock[_i] - 1 : iblock[_i];
    }
    if (indexw) {
        INT _indexw_sz = *m;
        _indexw0 = (INT*)malloc(_indexw_sz * sizeof(INT));
        for (INT _i = 0; _i < _indexw_sz; _i++) _indexw0[_i] = (indexw[_i] > 0) ? indexw[_i] - 1 : indexw[_i];
    }
    zlarrv(*n, *vl, *vu, D, L, *pivmin, _isplit0, *m, *dol, *dou, *minrgp, *rtol1, *rtol2, W, werr, wgap, _iblock0, _indexw0, gers, Z, *ldz, isuppz, work, iwork, info);
    if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    free(_isplit0);
    free(_iblock0);
    free(_indexw0);
}

void zlarscl2_(INT* m, INT* n, f64* D, c128* X, INT* ldx) {
    zlarscl2(*m, *n, D, X, *ldx);
}

void zlartg_(c128* f, c128* g, f64* c, c128* s, c128* r) {
    zlartg(*f, *g, c, s, r);
}

void zlartv_(INT* n, c128* X, INT* incx, c128* Y, INT* incy, f64* C, c128* S, INT* incc) {
    zlartv(*n, X, *incx, Y, *incy, C, S, *incc);
}

void zlarz_(char* side, INT* m, INT* n, INT* l, c128* v, INT* incv, c128* tau, c128* C, INT* ldc, c128* work) {
    zlarz(side, *m, *n, *l, v, *incv, *tau, C, *ldc, work);
}

void zlarzb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, INT* l, c128* V, INT* ldv, c128* T, INT* ldt, c128* C, INT* ldc, c128* work, INT* ldwork) {
    zlarzb(side, trans, direct, storev, *m, *n, *k, *l, V, *ldv, T, *ldt, C, *ldc, work, *ldwork);
}

void zlarzt_(char* direct, char* storev, INT* n, INT* k, c128* V, INT* ldv, c128* tau, c128* T, INT* ldt) {
    zlarzt(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void zlascl_(char* type, INT* kl, INT* ku, f64* cfrom, f64* cto, INT* m, INT* n, c128* A, INT* lda, INT* info) {
    zlascl(type, *kl, *ku, *cfrom, *cto, *m, *n, A, *lda, info);
}

void zlascl2_(INT* m, INT* n, f64* D, c128* X, INT* ldx) {
    zlascl2(*m, *n, D, X, *ldx);
}

void zlaset_(char* uplo, INT* m, INT* n, c128* alpha, c128* beta, c128* A, INT* lda) {
    zlaset(uplo, *m, *n, *alpha, *beta, A, *lda);
}

void zlasr_(char* side, char* pivot, char* direct, INT* m, INT* n, f64* C_rot, f64* S_rot, c128* A, INT* lda) {
    zlasr(side, pivot, direct, *m, *n, C_rot, S_rot, A, *lda);
}

void zlassq_(INT* n, c128* X, INT* incx, f64* scale, f64* sumsq) {
    zlassq(*n, X, *incx, scale, sumsq);
}

void zlaswlq_(INT* m, INT* n, INT* mb, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* lwork, INT* info) {
    zlaswlq(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void zlaswp_(INT* n, c128* A, INT* lda, INT* k1, INT* k2, INT* ipiv, INT* incx) {
    INT _k1 = *k1 - 1;
    INT _k2 = *k2 - 1;
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *k2;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zlaswp(*n, A, *lda, _k1, _k2, _ipiv0, *incx);
    free(_ipiv0);
}

void zlahef_(char* uplo, INT* n, INT* nb, INT* kb, c128* A, INT* lda, INT* ipiv, c128* W, INT* ldw, INT* info) {
    zlahef(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void zlahef_rk_(char* uplo, INT* n, INT* nb, INT* kb, c128* A, INT* lda, c128* E, INT* ipiv, c128* W, INT* ldw, INT* info) {
    zlahef_rk(uplo, *n, *nb, kb, A, *lda, E, ipiv, W, *ldw, info);
}

void zlahef_rook_(char* uplo, INT* n, INT* nb, INT* kb, c128* A, INT* lda, INT* ipiv, c128* W, INT* ldw, INT* info) {
    zlahef_rook(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void zlahef_aa_(char* uplo, INT* j1, INT* m, INT* nb, c128* A, INT* lda, INT* ipiv, c128* H, INT* ldh, c128* work) {
    zlahef_aa(uplo, *j1, *m, *nb, A, *lda, ipiv, H, *ldh, work);
}

void zlasyf_(char* uplo, INT* n, INT* nb, INT* kb, c128* A, INT* lda, INT* ipiv, c128* W, INT* ldw, INT* info) {
    zlasyf(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void zlasyf_aa_(char* uplo, INT* j1, INT* m, INT* nb, c128* A, INT* lda, INT* ipiv, c128* H, INT* ldh, c128* work) {
    zlasyf_aa(uplo, *j1, *m, *nb, A, *lda, ipiv, H, *ldh, work);
}

void zlasyf_rk_(char* uplo, INT* n, INT* nb, INT* kb, c128* A, INT* lda, c128* E, INT* ipiv, c128* W, INT* ldw, INT* info) {
    zlasyf_rk(uplo, *n, *nb, kb, A, *lda, E, ipiv, W, *ldw, info);
}

void zlasyf_rook_(char* uplo, INT* n, INT* nb, INT* kb, c128* A, INT* lda, INT* ipiv, c128* W, INT* ldw, INT* info) {
    zlasyf_rook(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void zlat2c_(char* uplo, INT* n, c128* A, INT* lda, c64* SA, INT* ldsa, INT* info) {
    zlat2c(uplo, *n, A, *lda, SA, *ldsa, info);
}

void zlatbs_(char* uplo, char* trans, char* diag, char* normin, INT* n, INT* kd, c128* AB, INT* ldab, c128* X, f64* scale, f64* cnorm, INT* info) {
    zlatbs(uplo, trans, diag, normin, *n, *kd, AB, *ldab, X, scale, cnorm, info);
}

void zlatdf_(INT* ijob, INT* n, c128* Z, INT* ldz, c128* rhs, f64* rdsum, f64* rdscal, INT* ipiv, INT* jpiv) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zlatdf(*ijob, *n, Z, *ldz, rhs, rdsum, rdscal, _ipiv0, jpiv);
    free(_ipiv0);
}

void zlatps_(char* uplo, char* trans, char* diag, char* normin, INT* n, c128* AP, c128* X, f64* scale, f64* cnorm, INT* info) {
    zlatps(uplo, trans, diag, normin, *n, AP, X, scale, cnorm, info);
}

void zlatrd_(char* uplo, INT* n, INT* nb, c128* A, INT* lda, f64* E, c128* tau, c128* W, INT* ldw) {
    zlatrd(uplo, *n, *nb, A, *lda, E, tau, W, *ldw);
}

void zlatrs_(char* uplo, char* trans, char* diag, char* normin, INT* n, c128* A, INT* lda, c128* X, f64* scale, f64* cnorm, INT* info) {
    zlatrs(uplo, trans, diag, normin, *n, A, *lda, X, scale, cnorm, info);
}

void zlatrs3_(char* uplo, char* trans, char* diag, char* normin, INT* n, INT* nrhs, c128* A, INT* lda, c128* X, INT* ldx, f64* scale, f64* cnorm, f64* work, INT* lwork, INT* info) {
    zlatrs3(uplo, trans, diag, normin, *n, *nrhs, A, *lda, X, *ldx, scale, cnorm, work, *lwork, info);
}

void zlatrz_(INT* m, INT* n, INT* l, c128* A, INT* lda, c128* tau, c128* work) {
    zlatrz(*m, *n, *l, A, *lda, tau, work);
}

void zlatsqr_(INT* m, INT* n, INT* mb, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* lwork, INT* info) {
    zlatsqr(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void zlaunhr_col_getrfnp_(INT* m, INT* n, c128* A, INT* lda, c128* D, INT* info) {
    zlaunhr_col_getrfnp(*m, *n, A, *lda, D, info);
}

void zlaunhr_col_getrfnp2_(INT* m, INT* n, c128* A, INT* lda, c128* D, INT* info) {
    zlaunhr_col_getrfnp2(*m, *n, A, *lda, D, info);
}

void zlauu2_(char* uplo, INT* n, c128* A, INT* lda, INT* info) {
    zlauu2(uplo, *n, A, *lda, info);
}

void zlauum_(char* uplo, INT* n, c128* A, INT* lda, INT* info) {
    zlauum(uplo, *n, A, *lda, info);
}

void zpbcon_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* anorm, f64* rcond, c128* work, f64* rwork, INT* info) {
    zpbcon(uplo, *n, *kd, AB, *ldab, *anorm, rcond, work, rwork, info);
}

void zpbequ_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, f64* S, f64* scond, f64* amax, INT* info) {
    zpbequ(uplo, *n, *kd, AB, *ldab, S, scond, amax, info);
}

void zpbrfs_(char* uplo, INT* n, INT* kd, INT* nrhs, c128* AB, INT* ldab, c128* AFB, INT* ldafb, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zpbrfs(uplo, *n, *kd, *nrhs, AB, *ldab, AFB, *ldafb, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void zpbstf_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, INT* info) {
    zpbstf(uplo, *n, *kd, AB, *ldab, info);
}

void zpbsv_(char* uplo, INT* n, INT* kd, INT* nrhs, c128* AB, INT* ldab, c128* B, INT* ldb, INT* info) {
    zpbsv(uplo, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void zpbsvx_(char* fact, char* uplo, INT* n, INT* kd, INT* nrhs, c128* AB, INT* ldab, c128* AFB, INT* ldafb, char* equed, f64* S, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zpbsvx(fact, uplo, *n, *kd, *nrhs, AB, *ldab, AFB, *ldafb, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
}

void zpbtf2_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, INT* info) {
    zpbtf2(uplo, *n, *kd, AB, *ldab, info);
}

void zpbtrf_(char* uplo, INT* n, INT* kd, c128* AB, INT* ldab, INT* info) {
    zpbtrf(uplo, *n, *kd, AB, *ldab, info);
}

void zpbtrs_(char* uplo, INT* n, INT* kd, INT* nrhs, c128* AB, INT* ldab, c128* B, INT* ldb, INT* info) {
    zpbtrs(uplo, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void zpftrf_(char* transr, char* uplo, INT* n, c128* A, INT* info) {
    zpftrf(transr, uplo, *n, A, info);
}

void zpftri_(char* transr, char* uplo, INT* n, c128* A, INT* info) {
    zpftri(transr, uplo, *n, A, info);
}

void zpftrs_(char* transr, char* uplo, INT* n, INT* nrhs, c128* A, c128* B, INT* ldb, INT* info) {
    zpftrs(transr, uplo, *n, *nrhs, A, B, *ldb, info);
}

void zpocon_(char* uplo, INT* n, c128* A, INT* lda, f64* anorm, f64* rcond, c128* work, f64* rwork, INT* info) {
    zpocon(uplo, *n, A, *lda, *anorm, rcond, work, rwork, info);
}

void zpoequ_(INT* n, c128* A, INT* lda, f64* S, f64* scond, f64* amax, INT* info) {
    zpoequ(*n, A, *lda, S, scond, amax, info);
}

void zpoequb_(INT* n, c128* A, INT* lda, f64* S, f64* scond, f64* amax, INT* info) {
    zpoequb(*n, A, *lda, S, scond, amax, info);
}

void zporfs_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zporfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void zposv_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, INT* info) {
    zposv(uplo, *n, *nrhs, A, *lda, B, *ldb, info);
}

void zposvx_(char* fact, char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, char* equed, f64* S, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zposvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
}

void zpotf2_(char* uplo, INT* n, c128* A, INT* lda, INT* info) {
    zpotf2(uplo, *n, A, *lda, info);
}

void zpotrf_(char* uplo, INT* n, c128* A, INT* lda, INT* info) {
    zpotrf(uplo, *n, A, *lda, info);
}

void zpotrf2_(char* uplo, INT* n, c128* A, INT* lda, INT* info) {
    zpotrf2(uplo, *n, A, *lda, info);
}

void zpotri_(char* uplo, INT* n, c128* A, INT* lda, INT* info) {
    zpotri(uplo, *n, A, *lda, info);
}

void zpotrs_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, INT* info) {
    zpotrs(uplo, *n, *nrhs, A, *lda, B, *ldb, info);
}

void zppcon_(char* uplo, INT* n, c128* AP, f64* anorm, f64* rcond, c128* work, f64* rwork, INT* info) {
    zppcon(uplo, *n, AP, *anorm, rcond, work, rwork, info);
}

void zppequ_(char* uplo, INT* n, c128* AP, f64* S, f64* scond, f64* amax, INT* info) {
    zppequ(uplo, *n, AP, S, scond, amax, info);
}

void zpprfs_(char* uplo, INT* n, INT* nrhs, c128* AP, c128* AFP, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zpprfs(uplo, *n, *nrhs, AP, AFP, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void zppsv_(char* uplo, INT* n, INT* nrhs, c128* AP, c128* B, INT* ldb, INT* info) {
    zppsv(uplo, *n, *nrhs, AP, B, *ldb, info);
}

void zppsvx_(char* fact, char* uplo, INT* n, INT* nrhs, c128* AP, c128* AFP, char* equed, f64* S, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zppsvx(fact, uplo, *n, *nrhs, AP, AFP, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
}

void zpptrf_(char* uplo, INT* n, c128* AP, INT* info) {
    zpptrf(uplo, *n, AP, info);
}

void zpptri_(char* uplo, INT* n, c128* AP, INT* info) {
    zpptri(uplo, *n, AP, info);
}

void zpptrs_(char* uplo, INT* n, INT* nrhs, c128* AP, c128* B, INT* ldb, INT* info) {
    zpptrs(uplo, *n, *nrhs, AP, B, *ldb, info);
}

void zpstf2_(char* uplo, INT* n, c128* A, INT* lda, INT* piv, INT* rank, f64* tol, f64* work, INT* info) {
    zpstf2(uplo, *n, A, *lda, piv, rank, *tol, work, info);
    if (piv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (piv[_i] >= 0) piv[_i]++; } }
}

void zpstrf_(char* uplo, INT* n, c128* A, INT* lda, INT* piv, INT* rank, f64* tol, f64* work, INT* info) {
    zpstrf(uplo, *n, A, *lda, piv, rank, *tol, work, info);
    if (piv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (piv[_i] >= 0) piv[_i]++; } }
}

void zptcon_(INT* n, f64* D, c128* E, f64* anorm, f64* rcond, f64* rwork, INT* info) {
    zptcon(*n, D, E, *anorm, rcond, rwork, info);
}

void zpteqr_(char* compz, INT* n, f64* D, f64* E, c128* Z, INT* ldz, f64* work, INT* info) {
    zpteqr(compz, *n, D, E, Z, *ldz, work, info);
}

void zptrfs_(char* uplo, INT* n, INT* nrhs, f64* D, c128* E, f64* DF, c128* EF, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zptrfs(uplo, *n, *nrhs, D, E, DF, EF, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void zptsv_(INT* n, INT* nrhs, f64* D, c128* E, c128* B, INT* ldb, INT* info) {
    zptsv(*n, *nrhs, D, E, B, *ldb, info);
}

void zptsvx_(char* fact, INT* n, INT* nrhs, f64* D, c128* E, f64* DF, c128* EF, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    zptsvx(fact, *n, *nrhs, D, E, DF, EF, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
}

void zpttrf_(INT* n, f64* D, c128* E, INT* info) {
    zpttrf(*n, D, E, info);
}

void zpttrs_(char* uplo, INT* n, INT* nrhs, f64* D, c128* E, c128* B, INT* ldb, INT* info) {
    zpttrs(uplo, *n, *nrhs, D, E, B, *ldb, info);
}

void zptts2_(INT* iuplo, INT* n, INT* nrhs, f64* D, c128* E, c128* B, INT* ldb) {
    zptts2(*iuplo, *n, *nrhs, D, E, B, *ldb);
}

void zrot_(INT* n, c128* CX, INT* incx, c128* CY, INT* incy, f64* c, c128* s) {
    zrot(*n, CX, *incx, CY, *incy, *c, *s);
}

void zrscl_(INT* n, c128* a, c128* x, INT* incx) {
    zrscl(*n, *a, x, *incx);
}

void zspcon_(char* uplo, INT* n, c128* AP, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zspcon(uplo, *n, AP, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zspmv_(char* uplo, INT* n, c128* alpha, c128* AP, c128* X, INT* incx, c128* beta, c128* Y, INT* incy) {
    zspmv(uplo, *n, *alpha, AP, X, *incx, *beta, Y, *incy);
}

void zspr_(char* uplo, INT* n, c128* alpha, c128* X, INT* incx, c128* AP) {
    zspr(uplo, *n, *alpha, X, *incx, AP);
}

void zsprfs_(char* uplo, INT* n, INT* nrhs, c128* AP, c128* AFP, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsprfs(uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zspsv_(char* uplo, INT* n, INT* nrhs, c128* AP, INT* ipiv, c128* B, INT* ldb, INT* info) {
    zspsv(uplo, *n, *nrhs, AP, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zspsvx_(char* fact, char* uplo, INT* n, INT* nrhs, c128* AP, c128* AFP, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zspsvx(fact, uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        free(_ipiv0);
    } else {
        zspsvx(fact, uplo, *n, *nrhs, AP, AFP, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsptrf_(char* uplo, INT* n, c128* AP, INT* ipiv, INT* info) {
    zsptrf(uplo, *n, AP, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zsptri_(char* uplo, INT* n, c128* AP, INT* ipiv, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsptri(uplo, *n, AP, _ipiv0, work, info);
    free(_ipiv0);
}

void zsptrs_(char* uplo, INT* n, INT* nrhs, c128* AP, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsptrs(uplo, *n, *nrhs, AP, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zstedc_(char* compz, INT* n, f64* D, f64* E, c128* Z, INT* ldz, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* liwork, INT* info) {
    zstedc(compz, *n, D, E, Z, *ldz, work, *lwork, rwork, *lrwork, iwork, *liwork, info);
}

void zstegr_(char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, f64* abstol, INT* m, f64* W, c128* Z, INT* ldz, INT* isuppz, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zstegr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void zstein_(INT* n, f64* D, f64* E, INT* m, f64* W, INT* iblock, INT* isplit, c128* Z, INT* ldz, f64* work, INT* iwork, INT* ifail, INT* info) {
    INT* _iblock0 = NULL;
    INT* _isplit0 = NULL;
    if (iblock) {
        INT _iblock_sz = *m;
        _iblock0 = (INT*)malloc(_iblock_sz * sizeof(INT));
        for (INT _i = 0; _i < _iblock_sz; _i++) _iblock0[_i] = (iblock[_i] > 0) ? iblock[_i] - 1 : iblock[_i];
    }
    if (isplit) {
        INT _isplit_sz = *n;
        _isplit0 = (INT*)malloc(_isplit_sz * sizeof(INT));
        for (INT _i = 0; _i < _isplit_sz; _i++) _isplit0[_i] = (isplit[_i] > 0) ? isplit[_i] - 1 : isplit[_i];
    }
    zstein(*n, D, E, *m, W, _iblock0, _isplit0, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    free(_iblock0);
    free(_isplit0);
}

void zstemr_(char* jobz, char* range, INT* n, f64* D, f64* E, f64* vl, f64* vu, INT* il, INT* iu, INT* m, f64* W, c128* Z, INT* ldz, INT* nzc, INT* isuppz, INT* tryrac, f64* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    zstemr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, m, W, Z, *ldz, *nzc, isuppz, tryrac, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void zsteqr_(char* compz, INT* n, f64* D, f64* E, c128* Z, INT* ldz, f64* work, INT* info) {
    zsteqr(compz, *n, D, E, Z, *ldz, work, info);
}

void zsycon_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsycon(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zsycon_3_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsycon_3(uplo, *n, A, *lda, E, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zsycon_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, f64* anorm, f64* rcond, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsycon_rook(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, info);
    free(_ipiv0);
}

void zsyconv_(char* uplo, char* way, INT* n, c128* A, INT* lda, INT* ipiv, c128* E, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsyconv(uplo, way, *n, A, *lda, _ipiv0, E, info);
    free(_ipiv0);
}

void zsyconvf_(char* uplo, char* way, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsyconvf(uplo, way, *n, A, *lda, E, _ipiv0, info);
    free(_ipiv0);
}

void zsyconvf_rook_(char* uplo, char* way, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsyconvf_rook(uplo, way, *n, A, *lda, E, _ipiv0, info);
    free(_ipiv0);
}

void zsyequb_(char* uplo, INT* n, c128* A, INT* lda, f64* S, f64* scond, f64* amax, c128* work, INT* info) {
    zsyequb(uplo, *n, A, *lda, S, scond, amax, work, info);
}

void zsymv_(char* uplo, INT* n, c128* alpha, c128* A, INT* lda, c128* X, INT* incx, c128* beta, c128* Y, INT* incy) {
    zsymv(uplo, *n, *alpha, A, *lda, X, *incx, *beta, Y, *incy);
}

void zsyr_(char* uplo, INT* n, c128* alpha, c128* X, INT* incx, c128* A, INT* lda) {
    zsyr(uplo, *n, *alpha, X, *incx, A, *lda);
}

void zsyrfs_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsyrfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
    free(_ipiv0);
}

void zsysv_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zsysv(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsysv_aa_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zsysv_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsysv_aa_2stage_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* TB, INT* ltb, INT* ipiv, INT* ipiv2, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zsysv_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, ipiv, ipiv2, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void zsysv_rk_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* E, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zsysv_rk(uplo, *n, *nrhs, A, *lda, E, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsysv_rook_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    zsysv_rook(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsysvx_(char* fact, char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* AF, INT* ldaf, INT* ipiv, c128* B, INT* ldb, c128* X, INT* ldx, f64* rcond, f64* ferr, f64* berr, c128* work, INT* lwork, f64* rwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        zsysvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, rwork, info);
        free(_ipiv0);
    } else {
        zsysvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, rwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsyswapr_(char* uplo, INT* n, c128* A, INT* lda, INT* i1, INT* i2) {
    zsyswapr(uplo, *n, A, *lda, *i1, *i2);
}

void zsytf2_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zsytf2(uplo, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void zsytf2_rk_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, INT* info) {
    zsytf2_rk(uplo, *n, A, *lda, E, ipiv, info);
}

void zsytf2_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, INT* info) {
    zsytf2_rook(uplo, *n, A, *lda, ipiv, info);
}

void zsytrf_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zsytrf(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsytrf_aa_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zsytrf_aa(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsytrf_aa_2stage_(char* uplo, INT* n, c128* A, INT* lda, c128* TB, INT* ltb, INT* ipiv, INT* ipiv2, c128* work, INT* lwork, INT* info) {
    zsytrf_aa_2stage(uplo, *n, A, *lda, TB, *ltb, ipiv, ipiv2, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void zsytrf_rk_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zsytrf_rk(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsytrf_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    zsytrf_rook(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void zsytri_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytri(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void zsytri2_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zsytri2(uplo, *n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        zsytri2(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zsytri2x_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytri2x(uplo, *n, A, *lda, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void zsytri_3_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zsytri_3(uplo, *n, A, *lda, E, _ipiv0, work, *lwork, info);
    } else {
        zsytri_3(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zsytri_3x_(char* uplo, INT* n, c128* A, INT* lda, c128* E, INT* ipiv, c128* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytri_3x(uplo, *n, A, *lda, E, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void zsytri_rook_(char* uplo, INT* n, c128* A, INT* lda, INT* ipiv, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytri_rook(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void zsytrs_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytrs(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zsytrs2_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytrs2(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, info);
    free(_ipiv0);
}

void zsytrs_3_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* E, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytrs_3(uplo, *n, *nrhs, A, *lda, E, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void zsytrs_aa_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, c128* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        zsytrs_aa(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, *lwork, info);
    } else {
        zsytrs_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void zsytrs_aa_2stage_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, c128* TB, INT* ltb, INT* ipiv, INT* ipiv2, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    INT* _ipiv20 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (ipiv2) {
        INT _ipiv2_sz = *n;
        _ipiv20 = (INT*)malloc(_ipiv2_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv2_sz; _i++) _ipiv20[_i] = (ipiv2[_i] > 0) ? ipiv2[_i] - 1 : ipiv2[_i];
    }
    zsytrs_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, _ipiv0, _ipiv20, B, *ldb, info);
    free(_ipiv0);
    free(_ipiv20);
}

void zsytrs_rook_(char* uplo, INT* n, INT* nrhs, c128* A, INT* lda, INT* ipiv, c128* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    zsytrs_rook(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void ztbcon_(char* norm, char* uplo, char* diag, INT* n, INT* kd, c128* AB, INT* ldab, f64* rcond, c128* work, f64* rwork, INT* info) {
    ztbcon(norm, uplo, diag, *n, *kd, AB, *ldab, rcond, work, rwork, info);
}

void ztbrfs_(char* uplo, char* trans, char* diag, INT* n, INT* kd, INT* nrhs, c128* AB, INT* ldab, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    ztbrfs(uplo, trans, diag, *n, *kd, *nrhs, AB, *ldab, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void ztbtrs_(char* uplo, char* trans, char* diag, INT* n, INT* kd, INT* nrhs, c128* AB, INT* ldab, c128* B, INT* ldb, INT* info) {
    ztbtrs(uplo, trans, diag, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void ztfsm_(char* transr, char* side, char* uplo, char* trans, char* diag, INT* m, INT* n, c128* alpha, c128* A, c128* B, INT* ldb) {
    ztfsm(transr, side, uplo, trans, diag, *m, *n, *alpha, A, B, *ldb);
}

void ztftri_(char* transr, char* uplo, char* diag, INT* n, c128* A, INT* info) {
    ztftri(transr, uplo, diag, *n, A, info);
}

void ztfttp_(char* transr, char* uplo, INT* n, c128* ARF, c128* AP, INT* info) {
    ztfttp(transr, uplo, *n, ARF, AP, info);
}

void ztfttr_(char* transr, char* uplo, INT* n, c128* ARF, c128* A, INT* lda, INT* info) {
    ztfttr(transr, uplo, *n, ARF, A, *lda, info);
}

void ztgex2_(INT* wantq, INT* wantz, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* Q, INT* ldq, c128* Z, INT* ldz, INT* j1, INT* info) {
    ztgex2(*wantq, *wantz, *n, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, *j1, info);
}

void ztgexc_(INT* wantq, INT* wantz, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* Q, INT* ldq, c128* Z, INT* ldz, INT* ifst, INT* ilst, INT* info) {
    INT _ifst = *ifst - 1;
    INT _ilst = *ilst - 1;
    ztgexc(*wantq, *wantz, *n, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, _ifst, &_ilst, info);
    *ilst = _ilst + 1;
}

void ztgevc_(char* side, char* howmny, INT* select, INT* n, c128* S, INT* lds, c128* P, INT* ldp, c128* VL, INT* ldvl, c128* VR, INT* ldvr, INT* mm, INT* m, c128* work, f64* rwork, INT* info) {
    ztgevc(side, howmny, select, *n, S, *lds, P, *ldp, VL, *ldvl, VR, *ldvr, *mm, m, work, rwork, info);
}

void ztgsen_(INT* ijob, INT* wantq, INT* wantz, INT* select, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* alpha, c128* beta, c128* Q, INT* ldq, c128* Z, INT* ldz, INT* m, f64* pl, f64* pr, f64* dif, c128* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ztgsen(*ijob, *wantq, *wantz, select, *n, A, *lda, B, *ldb, alpha, beta, Q, *ldq, Z, *ldz, m, pl, pr, dif, work, *lwork, iwork, *liwork, info);
}

void ztgsna_(char* job, char* howmny, INT* select, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* VL, INT* ldvl, c128* VR, INT* ldvr, f64* S, f64* dif, INT* mm, INT* m, c128* work, INT* lwork, INT* iwork, INT* info) {
    ztgsna(job, howmny, select, *n, A, *lda, B, *ldb, VL, *ldvl, VR, *ldvr, S, dif, *mm, m, work, *lwork, iwork, info);
}

void ztgsyl_(char* trans, INT* ijob, INT* m, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* C, INT* ldc, c128* D, INT* ldd, c128* E, INT* lde, c128* F, INT* ldf, f64* scale, f64* dif, c128* work, INT* lwork, INT* iwork, INT* info) {
    ztgsyl(trans, *ijob, *m, *n, A, *lda, B, *ldb, C, *ldc, D, *ldd, E, *lde, F, *ldf, scale, dif, work, *lwork, iwork, info);
}

void ztgsja_(char* jobu, char* jobv, char* jobq, INT* m, INT* p, INT* n, INT* k, INT* l, c128* A, INT* lda, c128* B, INT* ldb, f64* tola, f64* tolb, f64* alpha, f64* beta, c128* U, INT* ldu, c128* V, INT* ldv, c128* Q, INT* ldq, c128* work, INT* ncycle, INT* info) {
    ztgsja(jobu, jobv, jobq, *m, *p, *n, *k, *l, A, *lda, B, *ldb, *tola, *tolb, alpha, beta, U, *ldu, V, *ldv, Q, *ldq, work, ncycle, info);
}

void ztgsy2_(char* trans, INT* ijob, INT* m, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* C, INT* ldc, c128* D, INT* ldd, c128* E, INT* lde, c128* F, INT* ldf, f64* scale, f64* rdsum, f64* rdscal, INT* info) {
    ztgsy2(trans, *ijob, *m, *n, A, *lda, B, *ldb, C, *ldc, D, *ldd, E, *lde, F, *ldf, scale, rdsum, rdscal, info);
}

void ztpcon_(char* norm, char* uplo, char* diag, INT* n, c128* AP, f64* rcond, c128* work, f64* rwork, INT* info) {
    ztpcon(norm, uplo, diag, *n, AP, rcond, work, rwork, info);
}

void ztplqt_(INT* m, INT* n, INT* l, INT* mb, c128* A, INT* lda, c128* B, INT* ldb, c128* T, INT* ldt, c128* work, INT* info) {
    ztplqt(*m, *n, *l, *mb, A, *lda, B, *ldb, T, *ldt, work, info);
}

void ztplqt2_(INT* m, INT* n, INT* l, c128* A, INT* lda, c128* B, INT* ldb, c128* T, INT* ldt, INT* info) {
    ztplqt2(*m, *n, *l, A, *lda, B, *ldb, T, *ldt, info);
}

void ztpmlqt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, INT* mb, c128* V, INT* ldv, c128* T, INT* ldt, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* info) {
    ztpmlqt(side, trans, *m, *n, *k, *l, *mb, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, info);
}

void ztpmqrt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, INT* nb, c128* V, INT* ldv, c128* T, INT* ldt, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* info) {
    ztpmqrt(side, trans, *m, *n, *k, *l, *nb, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, info);
}

void ztpqrt_(INT* m, INT* n, INT* l, INT* nb, c128* A, INT* lda, c128* B, INT* ldb, c128* T, INT* ldt, c128* work, INT* info) {
    ztpqrt(*m, *n, *l, *nb, A, *lda, B, *ldb, T, *ldt, work, info);
}

void ztpqrt2_(INT* m, INT* n, INT* l, c128* A, INT* lda, c128* B, INT* ldb, c128* T, INT* ldt, INT* info) {
    ztpqrt2(*m, *n, *l, A, *lda, B, *ldb, T, *ldt, info);
}

void ztprfb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, INT* l, c128* V, INT* ldv, c128* T, INT* ldt, c128* A, INT* lda, c128* B, INT* ldb, c128* work, INT* ldwork) {
    ztprfb(side, trans, direct, storev, *m, *n, *k, *l, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, *ldwork);
}

void ztprfs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, c128* AP, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    ztprfs(uplo, trans, diag, *n, *nrhs, AP, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void ztptri_(char* uplo, char* diag, INT* n, c128* AP, INT* info) {
    ztptri(uplo, diag, *n, AP, info);
}

void ztptrs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, c128* AP, c128* B, INT* ldb, INT* info) {
    ztptrs(uplo, trans, diag, *n, *nrhs, AP, B, *ldb, info);
}

void ztpttf_(char* transr, char* uplo, INT* n, c128* AP, c128* ARF, INT* info) {
    ztpttf(transr, uplo, *n, AP, ARF, info);
}

void ztpttr_(char* uplo, INT* n, c128* AP, c128* A, INT* lda, INT* info) {
    ztpttr(uplo, *n, AP, A, *lda, info);
}

void ztrcon_(char* norm, char* uplo, char* diag, INT* n, c128* A, INT* lda, f64* rcond, c128* work, f64* rwork, INT* info) {
    ztrcon(norm, uplo, diag, *n, A, *lda, rcond, work, rwork, info);
}

void ztrevc_(char* side, char* howmny, INT* select, INT* n, c128* T, INT* ldt, c128* VL, INT* ldvl, c128* VR, INT* ldvr, INT* mm, INT* m, c128* work, f64* rwork, INT* info) {
    ztrevc(side, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, *mm, m, work, rwork, info);
}

void ztrevc3_(char* side, char* howmny, INT* select, INT* n, c128* T, INT* ldt, c128* VL, INT* ldvl, c128* VR, INT* ldvr, INT* mm, INT* m, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* info) {
    ztrevc3(side, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, *mm, m, work, *lwork, rwork, *lrwork, info);
}

void ztrexc_(char* compq, INT* n, c128* T, INT* ldt, c128* Q, INT* ldq, INT* ifst, INT* ilst, INT* info) {
    INT _ifst = *ifst - 1;
    INT _ilst = *ilst - 1;
    ztrexc(compq, *n, T, *ldt, Q, *ldq, _ifst, _ilst, info);
}

void ztrrfs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, c128* X, INT* ldx, f64* ferr, f64* berr, c128* work, f64* rwork, INT* info) {
    ztrrfs(uplo, trans, diag, *n, *nrhs, A, *lda, B, *ldb, X, *ldx, ferr, berr, work, rwork, info);
}

void ztrsen_(char* job, char* compq, INT* select, INT* n, c128* T, INT* ldt, c128* Q, INT* ldq, c128* W, INT* m, f64* s, f64* sep, c128* work, INT* lwork, INT* info) {
    ztrsen(job, compq, select, *n, T, *ldt, Q, *ldq, W, m, s, sep, work, *lwork, info);
}

void ztrsna_(char* job, char* howmny, INT* select, INT* n, c128* T, INT* ldt, c128* VL, INT* ldvl, c128* VR, INT* ldvr, f64* S, f64* sep, INT* mm, INT* m, c128* work, INT* ldwork, f64* rwork, INT* info) {
    ztrsna(job, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, S, sep, *mm, m, work, *ldwork, rwork, info);
}

void ztrsyl_(char* trana, char* tranb, INT* isgn, INT* m, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* C, INT* ldc, f64* scale, INT* info) {
    ztrsyl(trana, tranb, *isgn, *m, *n, A, *lda, B, *ldb, C, *ldc, scale, info);
}

void ztrsyl3_(char* trana, char* tranb, INT* isgn, INT* m, INT* n, c128* A, INT* lda, c128* B, INT* ldb, c128* C, INT* ldc, f64* scale, f64* swork, INT* ldswork, INT* info) {
    ztrsyl3(trana, tranb, *isgn, *m, *n, A, *lda, B, *ldb, C, *ldc, scale, swork, *ldswork, info);
}

void ztrti2_(char* uplo, char* diag, INT* n, c128* A, INT* lda, INT* info) {
    ztrti2(uplo, diag, *n, A, *lda, info);
}

void ztrtri_(char* uplo, char* diag, INT* n, c128* A, INT* lda, INT* info) {
    ztrtri(uplo, diag, *n, A, *lda, info);
}

void ztrtrs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, c128* A, INT* lda, c128* B, INT* ldb, INT* info) {
    ztrtrs(uplo, trans, diag, *n, *nrhs, A, *lda, B, *ldb, info);
}

void ztrttf_(char* transr, char* uplo, INT* n, c128* A, INT* lda, c128* ARF, INT* info) {
    ztrttf(transr, uplo, *n, A, *lda, ARF, info);
}

void ztrttp_(char* uplo, INT* n, c128* A, INT* lda, c128* AP, INT* info) {
    ztrttp(uplo, *n, A, *lda, AP, info);
}

void ztzrzf_(INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    ztzrzf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void zunbdb_(char* trans, char* signs, INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X12, INT* ldx12, c128* X21, INT* ldx21, c128* X22, INT* ldx22, f64* theta, f64* phi, c128* taup1, c128* taup2, c128* tauq1, c128* tauq2, c128* work, INT* lwork, INT* info) {
    zunbdb(trans, signs, *m, *p, *q, X11, *ldx11, X12, *ldx12, X21, *ldx21, X22, *ldx22, theta, phi, taup1, taup2, tauq1, tauq2, work, *lwork, info);
}

void zunbdb1_(INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X21, INT* ldx21, f64* theta, f64* phi, c128* taup1, c128* taup2, c128* tauq1, c128* work, INT* lwork, INT* info) {
    zunbdb1(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void zunbdb2_(INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X21, INT* ldx21, f64* theta, f64* phi, c128* taup1, c128* taup2, c128* tauq1, c128* work, INT* lwork, INT* info) {
    zunbdb2(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void zunbdb3_(INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X21, INT* ldx21, f64* theta, f64* phi, c128* taup1, c128* taup2, c128* tauq1, c128* work, INT* lwork, INT* info) {
    zunbdb3(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void zunbdb4_(INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X21, INT* ldx21, f64* theta, f64* phi, c128* taup1, c128* taup2, c128* tauq1, c128* phantom, c128* work, INT* lwork, INT* info) {
    zunbdb4(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, phantom, work, *lwork, info);
}

void zunbdb5_(INT* m1, INT* m2, INT* n, c128* X1, INT* incx1, c128* X2, INT* incx2, c128* Q1, INT* ldq1, c128* Q2, INT* ldq2, c128* work, INT* lwork, INT* info) {
    zunbdb5(*m1, *m2, *n, X1, *incx1, X2, *incx2, Q1, *ldq1, Q2, *ldq2, work, *lwork, info);
}

void zunbdb6_(INT* m1, INT* m2, INT* n, c128* X1, INT* incx1, c128* X2, INT* incx2, c128* Q1, INT* ldq1, c128* Q2, INT* ldq2, c128* work, INT* lwork, INT* info) {
    zunbdb6(*m1, *m2, *n, X1, *incx1, X2, *incx2, Q1, *ldq1, Q2, *ldq2, work, *lwork, info);
}

void zuncsd_(char* jobu1, char* jobu2, char* jobv1t, char* jobv2t, char* trans, char* signs, INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X12, INT* ldx12, c128* X21, INT* ldx21, c128* X22, INT* ldx22, f64* theta, c128* U1, INT* ldu1, c128* U2, INT* ldu2, c128* V1T, INT* ldv1t, c128* V2T, INT* ldv2t, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* info) {
    zuncsd(jobu1, jobu2, jobv1t, jobv2t, trans, signs, *m, *p, *q, X11, *ldx11, X12, *ldx12, X21, *ldx21, X22, *ldx22, theta, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, V2T, *ldv2t, work, *lwork, rwork, *lrwork, iwork, info);
}

void zuncsd2by1_(char* jobu1, char* jobu2, char* jobv1t, INT* m, INT* p, INT* q, c128* X11, INT* ldx11, c128* X21, INT* ldx21, f64* theta, c128* U1, INT* ldu1, c128* U2, INT* ldu2, c128* V1T, INT* ldv1t, c128* work, INT* lwork, f64* rwork, INT* lrwork, INT* iwork, INT* info) {
    zuncsd2by1(jobu1, jobu2, jobv1t, *m, *p, *q, X11, *ldx11, X21, *ldx21, theta, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, work, *lwork, rwork, *lrwork, iwork, info);
}

void zung2l_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zung2l(*m, *n, *k, A, *lda, tau, work, info);
}

void zung2r_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zung2r(*m, *n, *k, A, *lda, tau, work, info);
}

void zungbr_(char* vect, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zungbr(vect, *m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void zunghr_(INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zunghr(*n, _ilo, _ihi, A, *lda, tau, work, *lwork, info);
}

void zungl2_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zungl2(*m, *n, *k, A, *lda, tau, work, info);
}

void zunglq_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zunglq(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void zungql_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zungql(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void zungqr_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zungqr(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void zungr2_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* info) {
    zungr2(*m, *n, *k, A, *lda, tau, work, info);
}

void zungrq_(INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zungrq(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void zungtr_(char* uplo, INT* n, c128* A, INT* lda, c128* tau, c128* work, INT* lwork, INT* info) {
    zungtr(uplo, *n, A, *lda, tau, work, *lwork, info);
}

void zungtsqr_(INT* m, INT* n, INT* mb, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* lwork, INT* info) {
    zungtsqr(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void zungtsqr_row_(INT* m, INT* n, INT* mb, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* work, INT* lwork, INT* info) {
    zungtsqr_row(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void zunhr_col_(INT* m, INT* n, INT* nb, c128* A, INT* lda, c128* T, INT* ldt, c128* D, INT* info) {
    zunhr_col(*m, *n, *nb, A, *lda, T, *ldt, D, info);
}

void zunm22_(char* side, char* trans, INT* m, INT* n, INT* n1, INT* n2, c128* Q, INT* ldq, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunm22(side, trans, *m, *n, *n1, *n2, Q, *ldq, C, *ldc, work, *lwork, info);
}

void zunm2l_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* info) {
    zunm2l(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void zunm2r_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* info) {
    zunm2r(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void zunmbr_(char* vect, char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmbr(vect, side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunmhr_(char* side, char* trans, INT* m, INT* n, INT* ilo, INT* ihi, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    zunmhr(side, trans, *m, *n, _ilo, _ihi, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunml2_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* info) {
    zunml2(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void zunmlq_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmlq(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunmql_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmql(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunmqr_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmqr(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunmr2_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* info) {
    zunmr2(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void zunmr3_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* info) {
    zunmr3(side, trans, *m, *n, *k, *l, A, *lda, tau, C, *ldc, work, info);
}

void zunmrq_(char* side, char* trans, INT* m, INT* n, INT* k, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmrq(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunmrz_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmrz(side, trans, *m, *n, *k, *l, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zunmtr_(char* side, char* uplo, char* trans, INT* m, INT* n, c128* A, INT* lda, c128* tau, c128* C, INT* ldc, c128* work, INT* lwork, INT* info) {
    zunmtr(side, uplo, trans, *m, *n, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void zupgtr_(char* uplo, INT* n, c128* AP, c128* tau, c128* Q, INT* ldq, c128* work, INT* info) {
    zupgtr(uplo, *n, AP, tau, Q, *ldq, work, info);
}

void zupmtr_(char* side, char* uplo, char* trans, INT* m, INT* n, c128* AP, c128* tau, c128* C, INT* ldc, c128* work, INT* info) {
    zupmtr(side, uplo, trans, *m, *n, AP, tau, C, *ldc, work, info);
}
