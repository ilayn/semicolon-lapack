/* AUTO-GENERATED FILE — DO NOT EDIT
 * Fortran-ABI compatibility shim for semicolon-lapack
 * Precision: single
 * Source header: semicolon_lapack_single.h
 * Generated by scripts/generate_fortran_shim.py
 */

#include <stdlib.h>
#include "semicolon_lapack_single.h"


f32 sladiv2_(f32* a, f32* b, f32* c, f32* d, f32* r, f32* t) {
    f32 _ret = sladiv2(*a, *b, *c, *d, *r, *t);
    return _ret;
}

f32 slamch_(char* cmach) {
    f32 _ret = slamch(cmach);
    return _ret;
}

f32 slangb_(char* norm, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, f32* work) {
    f32 _ret = slangb(norm, *n, *kl, *ku, AB, *ldab, work);
    return _ret;
}

f32 slange_(char* norm, INT* m, INT* n, f32* A, INT* lda, f32* work) {
    f32 _ret = slange(norm, *m, *n, A, *lda, work);
    return _ret;
}

f32 slangt_(char* norm, INT* n, f32* DL, f32* D, f32* DU) {
    f32 _ret = slangt(norm, *n, DL, D, DU);
    return _ret;
}

f32 slanhs_(char* norm, INT* n, f32* A, INT* lda, f32* work) {
    f32 _ret = slanhs(norm, *n, A, *lda, work);
    return _ret;
}

f32 slansb_(char* norm, char* uplo, INT* n, INT* k, f32* AB, INT* ldab, f32* work) {
    f32 _ret = slansb(norm, uplo, *n, *k, AB, *ldab, work);
    return _ret;
}

f32 slansf_(char* norm, char* transr, char* uplo, INT* n, f32* A, f32* work) {
    f32 _ret = slansf(norm, transr, uplo, *n, A, work);
    return _ret;
}

f32 slansp_(char* norm, char* uplo, INT* n, f32* AP, f32* work) {
    f32 _ret = slansp(norm, uplo, *n, AP, work);
    return _ret;
}

f32 slanst_(char* norm, INT* n, f32* D, f32* E) {
    f32 _ret = slanst(norm, *n, D, E);
    return _ret;
}

f32 slansy_(char* norm, char* uplo, INT* n, f32* A, INT* lda, f32* work) {
    f32 _ret = slansy(norm, uplo, *n, A, *lda, work);
    return _ret;
}

f32 slantb_(char* norm, char* uplo, char* diag, INT* n, INT* k, f32* AB, INT* ldab, f32* work) {
    f32 _ret = slantb(norm, uplo, diag, *n, *k, AB, *ldab, work);
    return _ret;
}

f32 slantp_(char* norm, char* uplo, char* diag, INT* n, f32* AP, f32* work) {
    f32 _ret = slantp(norm, uplo, diag, *n, AP, work);
    return _ret;
}

f32 slantr_(char* norm, char* uplo, char* diag, INT* m, INT* n, f32* A, INT* lda, f32* work) {
    f32 _ret = slantr(norm, uplo, diag, *m, *n, A, *lda, work);
    return _ret;
}

f32 slapy2_(f32* x, f32* y) {
    f32 _ret = slapy2(*x, *y);
    return _ret;
}

f32 slapy3_(f32* x, f32* y, f32* z) {
    f32 _ret = slapy3(*x, *y, *z);
    return _ret;
}

f32 slarmm_(f32* anorm, f32* bnorm, f32* cnorm) {
    f32 _ret = slarmm(*anorm, *bnorm, *cnorm);
    return _ret;
}

INT sisnan_(f32* din) {
    INT _ret = sisnan(*din);
    return _ret;
}

INT slaisnan_(f32* din1, f32* din2) {
    INT _ret = slaisnan(*din1, *din2);
    return _ret;
}

INT slaneg_(INT* n, f32* D, f32* lld, f32* sigma, f32* pivmin, INT* r) {
    INT _ret = slaneg(*n, D, lld, *sigma, *pivmin, *r);
    return _ret;
}

INT ilaslc_(INT* m, INT* n, f32* A, INT* lda) {
    INT _ret = ilaslc(*m, *n, A, *lda);
    return _ret;
}

INT ilaslr_(INT* m, INT* n, f32* A, INT* lda) {
    INT _ret = ilaslr(*m, *n, A, *lda);
    return _ret;
}

void slag2d_(INT* m, INT* n, f32* SA, INT* ldsa, double* A, INT* lda, INT* info) {
    slag2d(*m, *n, SA, *ldsa, A, *lda, info);
}

void sbbcsd_(char* jobu1, char* jobu2, char* jobv1t, char* jobv2t, char* trans, INT* m, INT* p, INT* q, f32* theta, f32* phi, f32* U1, INT* ldu1, f32* U2, INT* ldu2, f32* V1T, INT* ldv1t, f32* V2T, INT* ldv2t, f32* B11D, f32* B11E, f32* B12D, f32* B12E, f32* B21D, f32* B21E, f32* B22D, f32* B22E, f32* work, INT* lwork, INT* info) {
    sbbcsd(jobu1, jobu2, jobv1t, jobv2t, trans, *m, *p, *q, theta, phi, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, V2T, *ldv2t, B11D, B11E, B12D, B12E, B21D, B21E, B22D, B22E, work, *lwork, info);
}

void sbdsdc_(char* uplo, char* compq, INT* n, f32* D, f32* E, f32* U, INT* ldu, f32* VT, INT* ldvt, f32* Q, INT* IQ, f32* work, INT* IWORK, INT* info) {
    sbdsdc(uplo, compq, *n, D, E, U, *ldu, VT, *ldvt, Q, IQ, work, IWORK, info);
}

void sbdsqr_(char* uplo, INT* n, INT* ncvt, INT* nru, INT* ncc, f32* D, f32* E, f32* VT, INT* ldvt, f32* U, INT* ldu, f32* C, INT* ldc, f32* work, INT* info) {
    sbdsqr(uplo, *n, *ncvt, *nru, *ncc, D, E, VT, *ldvt, U, *ldu, C, *ldc, work, info);
}

void sbdsvdx_(char* uplo, char* jobz, char* range, INT* n, f32* D, f32* E, f32* vl, f32* vu, INT* il, INT* iu, INT* ns, f32* S, f32* Z, INT* ldz, f32* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sbdsvdx(uplo, jobz, range, *n, D, E, *vl, *vu, _il, _iu, ns, S, Z, *ldz, work, iwork, info);
}

void sdisna_(char* job, INT* m, INT* n, f32* D, f32* SEP, INT* info) {
    sdisna(job, *m, *n, D, SEP, info);
}

void sgbbrd_(char* vect, INT* m, INT* n, INT* ncc, INT* kl, INT* ku, f32* AB, INT* ldab, f32* D, f32* E, f32* Q, INT* ldq, f32* PT, INT* ldpt, f32* C, INT* ldc, f32* work, INT* info) {
    sgbbrd(vect, *m, *n, *ncc, *kl, *ku, AB, *ldab, D, E, Q, *ldq, PT, *ldpt, C, *ldc, work, info);
}

void sgbcon_(char* norm, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, INT* ipiv, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgbcon(norm, *n, *kl, *ku, AB, *ldab, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void sgbequ_(INT* m, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, f32* R, f32* C, f32* rowcnd, f32* colcnd, f32* amax, INT* info) {
    sgbequ(*m, *n, *kl, *ku, AB, *ldab, R, C, rowcnd, colcnd, amax, info);
}

void sgbequb_(INT* m, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, f32* R, f32* C, f32* rowcnd, f32* colcnd, f32* amax, INT* info) {
    sgbequb(*m, *n, *kl, *ku, AB, *ldab, R, C, rowcnd, colcnd, amax, info);
}

void sgbrfs_(char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, f32* AB, INT* ldab, f32* AFB, INT* ldafb, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgbrfs(trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void sgbsv_(INT* n, INT* kl, INT* ku, INT* nrhs, f32* AB, INT* ldab, INT* ipiv, f32* B, INT* ldb, INT* info) {
    sgbsv(*n, *kl, *ku, *nrhs, AB, *ldab, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgbsvx_(char* fact, char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, f32* AB, INT* ldab, f32* AFB, INT* ldafb, INT* ipiv, char* equed, f32* R, f32* C, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        sgbsvx(fact, trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, _ipiv0, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        sgbsvx(fact, trans, *n, *kl, *ku, *nrhs, AB, *ldab, AFB, *ldafb, ipiv, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void sgbtf2_(INT* m, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, INT* ipiv, INT* info) {
    sgbtf2(*m, *n, *kl, *ku, AB, *ldab, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgbtrf_(INT* m, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, INT* ipiv, INT* info) {
    sgbtrf(*m, *n, *kl, *ku, AB, *ldab, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgbtrs_(char* trans, INT* n, INT* kl, INT* ku, INT* nrhs, f32* AB, INT* ldab, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgbtrs(trans, *n, *kl, *ku, *nrhs, AB, *ldab, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void sgebak_(char* job, char* side, INT* n, INT* ilo, INT* ihi, f32* scale, INT* m, f32* V, INT* ldv, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    if (job[0] == 'B' || job[0] == 'b' || job[0] == 'P' || job[0] == 'p') {
        for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] -= 1.0;
        for (INT _i = *ihi; _i < *n; _i++) scale[_i] -= 1.0;
    }
    sgebak(job, side, *n, _ilo, _ihi, scale, *m, V, *ldv, info);
    if (job[0] == 'B' || job[0] == 'b' || job[0] == 'P' || job[0] == 'p') {
        for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] += 1.0;
        for (INT _i = *ihi; _i < *n; _i++) scale[_i] += 1.0;
    }
}

void sgebal_(char* job, INT* n, f32* A, INT* lda, INT* ilo, INT* ihi, f32* scale, INT* info) {
    sgebal(job, *n, A, *lda, ilo, ihi, scale, info);
    (*ilo)++;
    (*ihi)++;
    for (INT _i = 0; _i < *ilo - 1; _i++) scale[_i] += 1.0;
    for (INT _i = *ihi; _i < *n; _i++) scale[_i] += 1.0;
}

void sgebd2_(INT* m, INT* n, f32* A, INT* lda, f32* D, f32* E, f32* tauq, f32* taup, f32* work, INT* info) {
    sgebd2(*m, *n, A, *lda, D, E, tauq, taup, work, info);
}

void sgebrd_(INT* m, INT* n, f32* A, INT* lda, f32* D, f32* E, f32* tauq, f32* taup, f32* work, INT* lwork, INT* info) {
    sgebrd(*m, *n, A, *lda, D, E, tauq, taup, work, *lwork, info);
}

void sgecon_(char* norm, INT* n, f32* A, INT* lda, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    sgecon(norm, *n, A, *lda, *anorm, rcond, work, iwork, info);
}

void sgeequ_(INT* m, INT* n, f32* A, INT* lda, f32* R, f32* C, f32* rowcnd, f32* colcnd, f32* amax, INT* info) {
    sgeequ(*m, *n, A, *lda, R, C, rowcnd, colcnd, amax, info);
}

void sgeequb_(INT* m, INT* n, f32* A, INT* lda, f32* R, f32* C, f32* rowcnd, f32* colcnd, f32* amax, INT* info) {
    sgeequb(*m, *n, A, *lda, R, C, rowcnd, colcnd, amax, info);
}

void sgees_(char* jobvs, char* sort, sselect2_t select, INT* n, f32* A, INT* lda, INT* sdim, f32* wr, f32* wi, f32* VS, INT* ldvs, f32* work, INT* lwork, INT* bwork, INT* info) {
    sgees(jobvs, sort, select, *n, A, *lda, sdim, wr, wi, VS, *ldvs, work, *lwork, bwork, info);
}

void sgeesx_(char* jobvs, char* sort, sselect2_t select, char* sense, INT* n, f32* A, INT* lda, INT* sdim, f32* wr, f32* wi, f32* VS, INT* ldvs, f32* rconde, f32* rcondv, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* bwork, INT* info) {
    sgeesx(jobvs, sort, select, sense, *n, A, *lda, sdim, wr, wi, VS, *ldvs, rconde, rcondv, work, *lwork, iwork, *liwork, bwork, info);
}

void sgeev_(char* jobvl, char* jobvr, INT* n, f32* A, INT* lda, f32* wr, f32* wi, f32* VL, INT* ldvl, f32* VR, INT* ldvr, f32* work, INT* lwork, INT* info) {
    sgeev(jobvl, jobvr, *n, A, *lda, wr, wi, VL, *ldvl, VR, *ldvr, work, *lwork, info);
}

void sgeevx_(char* balanc, char* jobvl, char* jobvr, char* sense, INT* n, f32* A, INT* lda, f32* wr, f32* wi, f32* VL, INT* ldvl, f32* VR, INT* ldvr, INT* ilo, INT* ihi, f32* scale, f32* abnrm, f32* rconde, f32* rcondv, f32* work, INT* lwork, INT* iwork, INT* info) {
    sgeevx(balanc, jobvl, jobvr, sense, *n, A, *lda, wr, wi, VL, *ldvl, VR, *ldvr, ilo, ihi, scale, abnrm, rconde, rcondv, work, *lwork, iwork, info);
    (*ilo)++;
    (*ihi)++;
}

void sgehd2_(INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sgehd2(*n, _ilo, _ihi, A, *lda, tau, work, info);
}

void sgehrd_(INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sgehrd(*n, _ilo, _ihi, A, *lda, tau, work, *lwork, info);
}

void sgejsv_(char* joba, char* jobu, char* jobv, char* jobr, char* jobt, char* jobp, INT* m, INT* n, f32* A, INT* lda, f32* SVA, f32* U, INT* ldu, f32* V, INT* ldv, f32* work, INT* lwork, INT* iwork, INT* info) {
    sgejsv(joba, jobu, jobv, jobr, jobt, jobp, *m, *n, A, *lda, SVA, U, *ldu, V, *ldv, work, *lwork, iwork, info);
}

void sgelq_(INT* m, INT* n, f32* A, INT* lda, f32* T, INT* tsize, f32* work, INT* lwork, INT* info) {
    sgelq(*m, *n, A, *lda, T, *tsize, work, *lwork, info);
}

void sgelq2_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sgelq2(*m, *n, A, *lda, tau, work, info);
}

void sgelqf_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sgelqf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void sgelqt_(INT* m, INT* n, INT* mb, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* info) {
    sgelqt(*m, *n, *mb, A, *lda, T, *ldt, work, info);
}

void sgelqt3_(INT* m, INT* n, f32* A, INT* lda, f32* T, INT* ldt, INT* info) {
    sgelqt3(*m, *n, A, *lda, T, *ldt, info);
}

void sgels_(char* trans, INT* m, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    sgels(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void sgelsd_(INT* m, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, f32* S, f32* rcond, INT* rank, f32* work, INT* lwork, INT* iwork, INT* info) {
    sgelsd(*m, *n, *nrhs, A, *lda, B, *ldb, S, *rcond, rank, work, *lwork, iwork, info);
}

void sgelss_(INT* m, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, f32* S, f32* rcond, INT* rank, f32* work, INT* lwork, INT* info) {
    sgelss(*m, *n, *nrhs, A, *lda, B, *ldb, S, *rcond, rank, work, *lwork, info);
}

void sgelst_(char* trans, INT* m, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    sgelst(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void sgelsy_(INT* m, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, INT* jpvt, f32* rcond, INT* rank, f32* work, INT* lwork, INT* info) {
    INT* _jpvt0 = NULL;
    if (*lwork != -1) {
        if (jpvt) {
            INT _jpvt_sz = *n;
            _jpvt0 = (INT*)malloc(_jpvt_sz * sizeof(INT));
            for (INT _i = 0; _i < _jpvt_sz; _i++) _jpvt0[_i] = (jpvt[_i] > 0) ? jpvt[_i] - 1 : jpvt[_i];
        }
    }
    if (*lwork != -1) {
        sgelsy(*m, *n, *nrhs, A, *lda, B, *ldb, _jpvt0, *rcond, rank, work, *lwork, info);
    } else {
        sgelsy(*m, *n, *nrhs, A, *lda, B, *ldb, jpvt, *rcond, rank, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_jpvt0);
    }
}

void sgemlq_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* T, INT* tsize, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sgemlq(side, trans, *m, *n, *k, A, *lda, T, *tsize, C, *ldc, work, *lwork, info);
}

void sgemlqt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, f32* V, INT* ldv, f32* T, INT* ldt, f32* C, INT* ldc, f32* work, INT* info) {
    sgemlqt(side, trans, *m, *n, *k, *mb, V, *ldv, T, *ldt, C, *ldc, work, info);
}

void sgemqr_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* T, INT* tsize, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sgemqr(side, trans, *m, *n, *k, A, *lda, T, *tsize, C, *ldc, work, *lwork, info);
}

void sgemqrt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* nb, f32* V, INT* ldv, f32* T, INT* ldt, f32* C, INT* ldc, f32* work, INT* info) {
    sgemqrt(side, trans, *m, *n, *k, *nb, V, *ldv, T, *ldt, C, *ldc, work, info);
}

void sgeql2_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sgeql2(*m, *n, A, *lda, tau, work, info);
}

void sgeqlf_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sgeqlf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void sgeqp3_(INT* m, INT* n, f32* A, INT* lda, INT* jpvt, f32* tau, f32* work, INT* lwork, INT* info) {
    sgeqp3(*m, *n, A, *lda, jpvt, tau, work, *lwork, info);
    if (*lwork != -1) {
        if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
    }
}

void sgeqp3rk_(INT* m, INT* n, INT* nrhs, INT* kmax, f32* abstol, f32* reltol, f32* A, INT* lda, INT* K, f32* maxc2nrmk, f32* relmaxc2nrmk, INT* jpiv, f32* tau, f32* work, INT* lwork, INT* iwork, INT* info) {
    sgeqp3rk(*m, *n, *nrhs, *kmax, *abstol, *reltol, A, *lda, K, maxc2nrmk, relmaxc2nrmk, jpiv, tau, work, *lwork, iwork, info);
    if (*lwork != -1) {
        if (jpiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpiv[_i] >= 0) jpiv[_i]++; } }
    }
}

void sgeqr_(INT* m, INT* n, f32* A, INT* lda, f32* T, INT* tsize, f32* work, INT* lwork, INT* info) {
    sgeqr(*m, *n, A, *lda, T, *tsize, work, *lwork, info);
}

void sgeqr2_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sgeqr2(*m, *n, A, *lda, tau, work, info);
}

void sgeqr2p_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sgeqr2p(*m, *n, A, *lda, tau, work, info);
}

void sgeqrf_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sgeqrf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void sgeqrfp_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sgeqrfp(*m, *n, A, *lda, tau, work, *lwork, info);
}

void sgeqrt_(INT* m, INT* n, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* info) {
    sgeqrt(*m, *n, *nb, A, *lda, T, *ldt, work, info);
}

void sgeqrt2_(INT* m, INT* n, f32* A, INT* lda, f32* T, INT* ldt, INT* info) {
    sgeqrt2(*m, *n, A, *lda, T, *ldt, info);
}

void sgeqrt3_(INT* m, INT* n, f32* A, INT* lda, f32* T, INT* ldt, INT* info) {
    sgeqrt3(*m, *n, A, *lda, T, *ldt, info);
}

void sgerfs_(char* trans, INT* n, INT* nrhs, f32* A, INT* lda, f32* AF, INT* ldaf, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgerfs(trans, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void sgerq2_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sgerq2(*m, *n, A, *lda, tau, work, info);
}

void sgerqf_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sgerqf(*m, *n, A, *lda, tau, work, *lwork, info);
}

void sgesc2_(INT* n, f32* A, INT* lda, f32* rhs, INT* ipiv, INT* jpiv, f32* scale) {
    INT* _ipiv0 = NULL;
    INT* _jpiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (jpiv) {
        INT _jpiv_sz = *n;
        _jpiv0 = (INT*)malloc(_jpiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _jpiv_sz; _i++) _jpiv0[_i] = (jpiv[_i] > 0) ? jpiv[_i] - 1 : jpiv[_i];
    }
    sgesc2(*n, A, *lda, rhs, _ipiv0, _jpiv0, scale);
    free(_ipiv0);
    free(_jpiv0);
}

void sgesdd_(char* jobz, INT* m, INT* n, f32* A, INT* lda, f32* S, f32* U, INT* ldu, f32* VT, INT* ldvt, f32* work, INT* lwork, INT* IWORK, INT* info) {
    sgesdd(jobz, *m, *n, A, *lda, S, U, *ldu, VT, *ldvt, work, *lwork, IWORK, info);
}

void sgesv_(INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, INT* info) {
    sgesv(*n, *nrhs, A, *lda, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgesvd_(char* jobu, char* jobvt, INT* m, INT* n, f32* A, INT* lda, f32* S, f32* U, INT* ldu, f32* VT, INT* ldvt, f32* work, INT* lwork, INT* info) {
    sgesvd(jobu, jobvt, *m, *n, A, *lda, S, U, *ldu, VT, *ldvt, work, *lwork, info);
}

void sgesvdq_(char* joba, char* jobp, char* jobr, char* jobu, char* jobv, INT* m, INT* n, f32* A, INT* lda, f32* S, f32* U, INT* ldu, f32* V, INT* ldv, INT* numrank, INT* iwork, INT* liwork, f32* work, INT* lwork, f32* rwork, INT* lrwork, INT* info) {
    sgesvdq(joba, jobp, jobr, jobu, jobv, *m, *n, A, *lda, S, U, *ldu, V, *ldv, numrank, iwork, *liwork, work, *lwork, rwork, *lrwork, info);
}

void sgesvdx_(char* jobu, char* jobvt, char* range, INT* m, INT* n, f32* A, INT* lda, f32* vl, f32* vu, INT* il, INT* iu, INT* ns, f32* S, f32* U, INT* ldu, f32* VT, INT* ldvt, f32* work, INT* lwork, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sgesvdx(jobu, jobvt, range, *m, *n, A, *lda, *vl, *vu, _il, _iu, ns, S, U, *ldu, VT, *ldvt, work, *lwork, iwork, info);
}

void sgesvj_(char* joba, char* jobu, char* jobv, INT* m, INT* n, f32* A, INT* lda, f32* SVA, INT* mv, f32* V, INT* ldv, f32* work, INT* lwork, INT* info) {
    sgesvj(joba, jobu, jobv, *m, *n, A, *lda, SVA, *mv, V, *ldv, work, *lwork, info);
}

void sgesvx_(char* fact, char* trans, INT* n, INT* nrhs, f32* A, INT* lda, f32* AF, INT* ldaf, INT* ipiv, char* equed, f32* R, f32* C, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        sgesvx(fact, trans, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        sgesvx(fact, trans, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, equed, R, C, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void sgetc2_(INT* n, f32* A, INT* lda, INT* ipiv, INT* jpiv, INT* info) {
    sgetc2(*n, A, *lda, ipiv, jpiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    if (jpiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpiv[_i] >= 0) jpiv[_i]++; } }
}

void sgetf2_(INT* m, INT* n, f32* A, INT* lda, INT* ipiv, INT* info) {
    sgetf2(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgetrf_(INT* m, INT* n, f32* A, INT* lda, INT* ipiv, INT* info) {
    sgetrf(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgetrf2_(INT* m, INT* n, f32* A, INT* lda, INT* ipiv, INT* info) {
    sgetrf2(*m, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = ((*m < *n) ? *m : *n); for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgetri_(INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        sgetri(*n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        sgetri(*n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void sgetrs_(char* trans, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgetrs(trans, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void sgetsls_(char* trans, INT* m, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    sgetsls(trans, *m, *n, *nrhs, A, *lda, B, *ldb, work, *lwork, info);
}

void sgetsqrhrt_(INT* m, INT* n, INT* mb1, INT* nb1, INT* nb2, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* lwork, INT* info) {
    sgetsqrhrt(*m, *n, *mb1, *nb1, *nb2, A, *lda, T, *ldt, work, *lwork, info);
}

void sggbak_(char* job, char* side, INT* n, INT* ilo, INT* ihi, f32* lscale, f32* rscale, INT* m, f32* V, INT* ldv, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sggbak(job, side, *n, _ilo, _ihi, lscale, rscale, *m, V, *ldv, info);
}

void sggbal_(char* job, INT* n, f32* A, INT* lda, f32* B, INT* ldb, INT* ilo, INT* ihi, f32* lscale, f32* rscale, f32* work, INT* info) {
    sggbal(job, *n, A, *lda, B, *ldb, ilo, ihi, lscale, rscale, work, info);
    (*ilo)++;
    (*ihi)++;
}

void sgges_(char* jobvsl, char* jobvsr, char* sort, sselect3_t selctg, INT* n, f32* A, INT* lda, f32* B, INT* ldb, INT* sdim, f32* alphar, f32* alphai, f32* beta, f32* VSL, INT* ldvsl, f32* VSR, INT* ldvsr, f32* work, INT* lwork, INT* bwork, INT* info) {
    sgges(jobvsl, jobvsr, sort, selctg, *n, A, *lda, B, *ldb, sdim, alphar, alphai, beta, VSL, *ldvsl, VSR, *ldvsr, work, *lwork, bwork, info);
}

void sgges3_(char* jobvsl, char* jobvsr, char* sort, sselect3_t selctg, INT* n, f32* A, INT* lda, f32* B, INT* ldb, INT* sdim, f32* alphar, f32* alphai, f32* beta, f32* VSL, INT* ldvsl, f32* VSR, INT* ldvsr, f32* work, INT* lwork, INT* bwork, INT* info) {
    sgges3(jobvsl, jobvsr, sort, selctg, *n, A, *lda, B, *ldb, sdim, alphar, alphai, beta, VSL, *ldvsl, VSR, *ldvsr, work, *lwork, bwork, info);
}

void sggesx_(char* jobvsl, char* jobvsr, char* sort, sselect3_t selctg, char* sense, INT* n, f32* A, INT* lda, f32* B, INT* ldb, INT* sdim, f32* alphar, f32* alphai, f32* beta, f32* VSL, INT* ldvsl, f32* VSR, INT* ldvsr, f32* rconde, f32* rcondv, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* bwork, INT* info) {
    sggesx(jobvsl, jobvsr, sort, selctg, sense, *n, A, *lda, B, *ldb, sdim, alphar, alphai, beta, VSL, *ldvsl, VSR, *ldvsr, rconde, rcondv, work, *lwork, iwork, *liwork, bwork, info);
}

void sggev_(char* jobvl, char* jobvr, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* alphar, f32* alphai, f32* beta, f32* VL, INT* ldvl, f32* VR, INT* ldvr, f32* work, INT* lwork, INT* info) {
    sggev(jobvl, jobvr, *n, A, *lda, B, *ldb, alphar, alphai, beta, VL, *ldvl, VR, *ldvr, work, *lwork, info);
}

void sggev3_(char* jobvl, char* jobvr, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* alphar, f32* alphai, f32* beta, f32* VL, INT* ldvl, f32* VR, INT* ldvr, f32* work, INT* lwork, INT* info) {
    sggev3(jobvl, jobvr, *n, A, *lda, B, *ldb, alphar, alphai, beta, VL, *ldvl, VR, *ldvr, work, *lwork, info);
}

void sggevx_(char* balanc, char* jobvl, char* jobvr, char* sense, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* alphar, f32* alphai, f32* beta, f32* VL, INT* ldvl, f32* VR, INT* ldvr, INT* ilo, INT* ihi, f32* lscale, f32* rscale, f32* abnrm, f32* bbnrm, f32* rconde, f32* rcondv, f32* work, INT* lwork, INT* iwork, INT* bwork, INT* info) {
    sggevx(balanc, jobvl, jobvr, sense, *n, A, *lda, B, *ldb, alphar, alphai, beta, VL, *ldvl, VR, *ldvr, ilo, ihi, lscale, rscale, abnrm, bbnrm, rconde, rcondv, work, *lwork, iwork, bwork, info);
    (*ilo)++;
    (*ihi)++;
}

void sggglm_(INT* n, INT* m, INT* p, f32* A, INT* lda, f32* B, INT* ldb, f32* D, f32* X, f32* Y, f32* work, INT* lwork, INT* info) {
    sggglm(*n, *m, *p, A, *lda, B, *ldb, D, X, Y, work, *lwork, info);
}

void sgghd3_(char* compq, char* compz, INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* B, INT* ldb, f32* Q, INT* ldq, f32* Z, INT* ldz, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sgghd3(compq, compz, *n, _ilo, _ihi, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, work, *lwork, info);
}

void sgghrd_(char* compq, char* compz, INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* B, INT* ldb, f32* Q, INT* ldq, f32* Z, INT* ldz, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sgghrd(compq, compz, *n, _ilo, _ihi, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, info);
}

void sgglse_(INT* m, INT* n, INT* p, f32* A, INT* lda, f32* B, INT* ldb, f32* C, f32* D, f32* X, f32* work, INT* lwork, INT* info) {
    sgglse(*m, *n, *p, A, *lda, B, *ldb, C, D, X, work, *lwork, info);
}

void sggqrf_(INT* n, INT* m, INT* p, f32* A, INT* lda, f32* taua, f32* B, INT* ldb, f32* taub, f32* work, INT* lwork, INT* info) {
    sggqrf(*n, *m, *p, A, *lda, taua, B, *ldb, taub, work, *lwork, info);
}

void sggrqf_(INT* m, INT* p, INT* n, f32* A, INT* lda, f32* taua, f32* B, INT* ldb, f32* taub, f32* work, INT* lwork, INT* info) {
    sggrqf(*m, *p, *n, A, *lda, taua, B, *ldb, taub, work, *lwork, info);
}

void sggsvd3_(char* jobu, char* jobv, char* jobq, INT* m, INT* n, INT* p, INT* k, INT* l, f32* A, INT* lda, f32* B, INT* ldb, f32* alpha, f32* beta, f32* U, INT* ldu, f32* V, INT* ldv, f32* Q, INT* ldq, f32* work, INT* lwork, INT* iwork, INT* info) {
    sggsvd3(jobu, jobv, jobq, *m, *n, *p, k, l, A, *lda, B, *ldb, alpha, beta, U, *ldu, V, *ldv, Q, *ldq, work, *lwork, iwork, info);
}

void sggsvp3_(char* jobu, char* jobv, char* jobq, INT* m, INT* p, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* tola, f32* tolb, INT* k, INT* l, f32* U, INT* ldu, f32* V, INT* ldv, f32* Q, INT* ldq, INT* iwork, f32* tau, f32* work, INT* lwork, INT* info) {
    sggsvp3(jobu, jobv, jobq, *m, *p, *n, A, *lda, B, *ldb, *tola, *tolb, k, l, U, *ldu, V, *ldv, Q, *ldq, iwork, tau, work, *lwork, info);
}

void sgsvj0_(char* jobv, INT* m, INT* n, f32* A, INT* lda, f32* D, f32* SVA, INT* mv, f32* V, INT* ldv, f32* eps, f32* sfmin, f32* tol, INT* nsweep, f32* work, INT* lwork, INT* info) {
    sgsvj0(jobv, *m, *n, A, *lda, D, SVA, *mv, V, *ldv, *eps, *sfmin, *tol, *nsweep, work, *lwork, info);
}

void sgsvj1_(char* jobv, INT* m, INT* n, INT* n1, f32* A, INT* lda, f32* D, f32* SVA, INT* mv, f32* V, INT* ldv, f32* eps, f32* sfmin, f32* tol, INT* nsweep, f32* work, INT* lwork, INT* info) {
    sgsvj1(jobv, *m, *n, *n1, A, *lda, D, SVA, *mv, V, *ldv, *eps, *sfmin, *tol, *nsweep, work, *lwork, info);
}

void sgtcon_(char* norm, INT* n, f32* DL, f32* D, f32* DU, f32* DU2, INT* ipiv, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgtcon(norm, *n, DL, D, DU, DU2, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void sgtrfs_(char* trans, INT* n, INT* nrhs, f32* DL, f32* D, f32* DU, f32* DLF, f32* DF, f32* DUF, f32* DU2, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgtrfs(trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void sgtsv_(INT* n, INT* nrhs, f32* DL, f32* D, f32* DU, f32* B, INT* ldb, INT* info) {
    sgtsv(*n, *nrhs, DL, D, DU, B, *ldb, info);
}

void sgtsvx_(char* fact, char* trans, INT* n, INT* nrhs, f32* DL, f32* D, f32* DU, f32* DLF, f32* DF, f32* DUF, f32* DU2, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        sgtsvx(fact, trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        sgtsvx(fact, trans, *n, *nrhs, DL, D, DU, DLF, DF, DUF, DU2, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void sgttrf_(INT* n, f32* DL, f32* D, f32* DU, f32* DU2, INT* ipiv, INT* info) {
    sgttrf(*n, DL, D, DU, DU2, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sgttrs_(char* trans, INT* n, INT* nrhs, f32* DL, f32* D, f32* DU, f32* DU2, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgttrs(trans, *n, *nrhs, DL, D, DU, DU2, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void sgtts2_(INT* itrans, INT* n, INT* nrhs, f32* DL, f32* D, f32* DU, f32* DU2, INT* ipiv, f32* B, INT* ldb) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sgtts2(*itrans, *n, *nrhs, DL, D, DU, DU2, _ipiv0, B, *ldb);
    free(_ipiv0);
}

void shgeqz_(char* job, char* compq, char* compz, INT* n, INT* ilo, INT* ihi, f32* H, INT* ldh, f32* T, INT* ldt, f32* alphar, f32* alphai, f32* beta, f32* Q, INT* ldq, f32* Z, INT* ldz, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    shgeqz(job, compq, compz, *n, _ilo, _ihi, H, *ldh, T, *ldt, alphar, alphai, beta, Q, *ldq, Z, *ldz, work, *lwork, info);
}

void shsein_(char* side, char* eigsrc, char* initv, INT* select, INT* n, f32* H, INT* ldh, f32* wr, f32* wi, f32* VL, INT* ldvl, f32* VR, INT* ldvr, INT* mm, INT* m, f32* work, INT* ifaill, INT* ifailr, INT* info) {
    shsein(side, eigsrc, initv, select, *n, H, *ldh, wr, wi, VL, *ldvl, VR, *ldvr, *mm, m, work, ifaill, ifailr, info);
    if (ifaill) { INT _sz = *mm; for (INT _i = 0; _i < _sz; _i++) ifaill[_i]++; }
    if (ifailr) { INT _sz = *mm; for (INT _i = 0; _i < _sz; _i++) ifailr[_i]++; }
}

void shseqr_(char* job, char* compz, INT* n, INT* ilo, INT* ihi, f32* H, INT* ldh, f32* wr, f32* wi, f32* Z, INT* ldz, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    shseqr(job, compz, *n, _ilo, _ihi, H, *ldh, wr, wi, Z, *ldz, work, *lwork, info);
}

void slabad_(f32* small, f32* large) {
    slabad(small, large);
}

void slabrd_(INT* m, INT* n, INT* nb, f32* A, INT* lda, f32* D, f32* E, f32* tauq, f32* taup, f32* X, INT* ldx, f32* Y, INT* ldy) {
    slabrd(*m, *n, *nb, A, *lda, D, E, tauq, taup, X, *ldx, Y, *ldy);
}

void slacn2_(INT* n, f32* V, f32* X, INT* isgn, f32* est, INT* kase, INT* isave) {
    slacn2(*n, V, X, isgn, est, kase, isave);
}

void slacon_(INT* n, f32* V, f32* X, INT* ISGN, f32* est, INT* kase) {
    slacon(*n, V, X, ISGN, est, kase);
}

void slacpy_(char* uplo, INT* m, INT* n, f32* A, INT* lda, f32* B, INT* ldb) {
    slacpy(uplo, *m, *n, A, *lda, B, *ldb);
}

void sladiv_(f32* a, f32* b, f32* c, f32* d, f32* p, f32* q) {
    sladiv(*a, *b, *c, *d, p, q);
}

void sladiv1_(f32* a, f32* b, f32* c, f32* d, f32* p, f32* q) {
    sladiv1(*a, *b, *c, *d, p, q);
}

void slae2_(f32* a, f32* b, f32* c, f32* rt1, f32* rt2) {
    slae2(*a, *b, *c, rt1, rt2);
}

void slaebz_(INT* ijob, INT* nitmax, INT* n, INT* mmax, INT* minp, INT* nbmin, f32* abstol, f32* reltol, f32* pivmin, f32* D, f32* E, f32* E2, INT* nval, f32* AB, f32* C, INT* mout, INT* NAB, f32* work, INT* iwork, INT* info) {
    slaebz(*ijob, *nitmax, *n, *mmax, *minp, *nbmin, *abstol, *reltol, *pivmin, D, E, E2, nval, AB, C, mout, NAB, work, iwork, info);
}

void slaed0_(INT* icompq, INT* qsiz, INT* n, f32* D, f32* E, f32* Q, INT* ldq, f32* qstore, INT* ldqs, f32* work, INT* iwork, INT* info) {
    slaed0(*icompq, *qsiz, *n, D, E, Q, *ldq, qstore, *ldqs, work, iwork, info);
}

void slaed1_(INT* n, f32* D, f32* Q, INT* ldq, INT* indxq, f32* rho, INT* cutpnt, f32* work, INT* iwork, INT* info) {
    slaed1(*n, D, Q, *ldq, indxq, *rho, *cutpnt, work, iwork, info);
}

void slaed2_(INT* K, INT* n, INT* n1, f32* D, f32* Q, INT* ldq, INT* indxq, f32* rho, f32* Z, f32* dlambda, f32* W, f32* Q2, INT* indx, INT* indxc, INT* indxp, INT* coltyp, INT* info) {
    slaed2(K, *n, *n1, D, Q, *ldq, indxq, rho, Z, dlambda, W, Q2, indx, indxc, indxp, coltyp, info);
}

void slaed3_(INT* k, INT* n, INT* n1, f32* D, f32* Q, INT* ldq, f32* rho, f32* dlambda, f32* Q2, INT* indx, INT* ctot, f32* W, f32* S, INT* info) {
    slaed3(*k, *n, *n1, D, Q, *ldq, *rho, dlambda, Q2, indx, ctot, W, S, info);
}

void slaed4_(INT* n, INT* i, f32* D, f32* Z, f32* delta, f32* rho, f32* dlam, INT* info) {
    slaed4(*n, *i, D, Z, delta, *rho, dlam, info);
}

void slaed5_(INT* i, f32* D, f32* Z, f32* delta, f32* rho, f32* dlam) {
    slaed5(*i, D, Z, delta, *rho, dlam);
}

void slaed6_(INT* kniter, INT* orgati, f32* rho, f32* D, f32* Z, f32* finit, f32* tau, INT* info) {
    slaed6(*kniter, *orgati, *rho, D, Z, *finit, tau, info);
}

void slaed7_(INT* icompq, INT* n, INT* qsiz, INT* tlvls, INT* curlvl, INT* curpbm, f32* D, f32* Q, INT* ldq, INT* indxq, f32* rho, INT* cutpnt, f32* qstore, INT* qptr, INT* prmptr, INT* perm, INT* givptr, INT* givcol, f32* givnum, f32* work, INT* iwork, INT* info) {
    slaed7(*icompq, *n, *qsiz, *tlvls, *curlvl, *curpbm, D, Q, *ldq, indxq, *rho, *cutpnt, qstore, qptr, prmptr, perm, givptr, givcol, givnum, work, iwork, info);
}

void slaed8_(INT* icompq, INT* K, INT* n, INT* qsiz, f32* D, f32* Q, INT* ldq, INT* indxq, f32* rho, INT* cutpnt, f32* Z, f32* dlambda, f32* Q2, INT* ldq2, f32* W, INT* perm, INT* givptr, INT* givcol, f32* givnum, INT* indxp, INT* indx, INT* info) {
    slaed8(*icompq, K, *n, *qsiz, D, Q, *ldq, indxq, rho, *cutpnt, Z, dlambda, Q2, *ldq2, W, perm, givptr, givcol, givnum, indxp, indx, info);
}

void slaed9_(INT* k, INT* kstart, INT* kstop, INT* n, f32* D, f32* Q, INT* ldq, f32* rho, f32* dlambda, f32* W, f32* S, INT* lds, INT* info) {
    slaed9(*k, *kstart, *kstop, *n, D, Q, *ldq, *rho, dlambda, W, S, *lds, info);
}

void slaeda_(INT* n, INT* tlvls, INT* curlvl, INT* curpbm, INT* prmptr, INT* perm, INT* givptr, INT* givcol, f32* givnum, f32* Q, INT* qptr, f32* Z, f32* ztemp, INT* info) {
    slaeda(*n, *tlvls, *curlvl, *curpbm, prmptr, perm, givptr, givcol, givnum, Q, qptr, Z, ztemp, info);
}

void slaein_(INT* rightv, INT* noinit, INT* n, f32* H, INT* ldh, f32* wr, f32* wi, f32* vr, f32* vi, f32* B, INT* ldb, f32* work, f32* eps3, f32* smlnum, f32* bignum, INT* info) {
    slaein(*rightv, *noinit, *n, H, *ldh, *wr, *wi, vr, vi, B, *ldb, work, *eps3, *smlnum, *bignum, info);
}

void slaev2_(f32* a, f32* b, f32* c, f32* rt1, f32* rt2, f32* cs1, f32* sn1) {
    slaev2(*a, *b, *c, rt1, rt2, cs1, sn1);
}

void slaexc_(INT* wantq, INT* n, f32* T, INT* ldt, f32* Q, INT* ldq, INT* j1, INT* n1, INT* n2, f32* work, INT* info) {
    slaexc(*wantq, *n, T, *ldt, Q, *ldq, *j1, *n1, *n2, work, info);
}

void slag2_(f32* A, INT* lda, f32* B, INT* ldb, f32* safmin, f32* scale1, f32* scale2, f32* wr1, f32* wr2, f32* wi) {
    slag2(A, *lda, B, *ldb, *safmin, scale1, scale2, wr1, wr2, wi);
}

void slags2_(INT* upper, f32* a1, f32* a2, f32* a3, f32* b1, f32* b2, f32* b3, f32* csu, f32* snu, f32* csv, f32* snv, f32* csq, f32* snq) {
    slags2(*upper, *a1, *a2, *a3, *b1, *b2, *b3, csu, snu, csv, snv, csq, snq);
}

void slagtf_(INT* n, f32* A, f32* lambda, f32* B, f32* C, f32* tol, f32* D, INT* in, INT* info) {
    slagtf(*n, A, *lambda, B, C, *tol, D, in, info);
}

void slagtm_(char* trans, INT* n, INT* nrhs, f32* alpha, f32* DL, f32* D, f32* DU, f32* X, INT* ldx, f32* beta, f32* B, INT* ldb) {
    slagtm(trans, *n, *nrhs, *alpha, DL, D, DU, X, *ldx, *beta, B, *ldb);
}

void slagts_(INT* job, INT* n, f32* A, f32* B, f32* C, f32* D, INT* in, f32* Y, f32* tol, INT* info) {
    slagts(*job, *n, A, B, C, D, in, Y, tol, info);
}

void slagv2_(f32* A, INT* lda, f32* B, INT* ldb, f32* alphar, f32* alphai, f32* beta, f32* csl, f32* snl, f32* csr, f32* snr) {
    slagv2(A, *lda, B, *ldb, alphar, alphai, beta, csl, snl, csr, snr);
}

void slahqr_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, f32* H, INT* ldh, f32* wr, f32* wi, INT* iloz, INT* ihiz, f32* Z, INT* ldz, INT* info) {
    slahqr(*wantt, *wantz, *n, *ilo, *ihi, H, *ldh, wr, wi, *iloz, *ihiz, Z, *ldz, info);
}

void slahr2_(INT* n, INT* k, INT* nb, f32* A, INT* lda, f32* tau, f32* T, INT* ldt, f32* Y, INT* ldy) {
    slahr2(*n, *k, *nb, A, *lda, tau, T, *ldt, Y, *ldy);
}

void slaic1_(INT* job, INT* j, f32* x, f32* sest, f32* w, f32* gamma, f32* sestpr, f32* s, f32* c) {
    slaic1(*job, *j, x, *sest, w, *gamma, sestpr, s, c);
}

void slaln2_(INT* ltrans, INT* na, INT* nw, f32* smin, f32* ca, f32* A, INT* lda, f32* d1, f32* d2, f32* B, INT* ldb, f32* wr, f32* wi, f32* X, INT* ldx, f32* scale, f32* xnorm, INT* info) {
    slaln2(*ltrans, *na, *nw, *smin, *ca, A, *lda, *d1, *d2, B, *ldb, *wr, *wi, X, *ldx, scale, xnorm, info);
}

void slals0_(INT* icompq, INT* nl, INT* nr, INT* sqre, INT* nrhs, f32* B, INT* ldb, f32* BX, INT* ldbx, INT* perm, INT* givptr, INT* givcol, INT* ldgcol, f32* givnum, INT* ldgnum, f32* poles, f32* difl, f32* difr, f32* Z, INT* k, f32* c, f32* s, f32* work, INT* info) {
    slals0(*icompq, *nl, *nr, *sqre, *nrhs, B, *ldb, BX, *ldbx, perm, *givptr, givcol, *ldgcol, givnum, *ldgnum, poles, difl, difr, Z, *k, *c, *s, work, info);
}

void slalsa_(INT* icompq, INT* smlsiz, INT* n, INT* nrhs, f32* B, INT* ldb, f32* BX, INT* ldbx, f32* U, INT* ldu, f32* VT, INT* K, f32* difl, f32* difr, f32* Z, f32* poles, INT* givptr, INT* givcol, INT* ldgcol, INT* perm, f32* givnum, f32* C, f32* S, f32* work, INT* iwork, INT* info) {
    slalsa(*icompq, *smlsiz, *n, *nrhs, B, *ldb, BX, *ldbx, U, *ldu, VT, K, difl, difr, Z, poles, givptr, givcol, *ldgcol, perm, givnum, C, S, work, iwork, info);
}

void slalsd_(char* uplo, INT* smlsiz, INT* n, INT* nrhs, f32* D, f32* E, f32* B, INT* ldb, f32* rcond, INT* rank, f32* work, INT* iwork, INT* info) {
    slalsd(uplo, *smlsiz, *n, *nrhs, D, E, B, *ldb, *rcond, rank, work, iwork, info);
}

void slamrg_(INT* n1, INT* n2, f32* A, INT* dtrd1, INT* dtrd2, INT* index) {
    slamrg(*n1, *n2, A, *dtrd1, *dtrd2, index);
}

void slamswlq_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    slamswlq(side, trans, *m, *n, *k, *mb, *nb, A, *lda, T, *ldt, C, *ldc, work, *lwork, info);
}

void slamtsqr_(char* side, char* trans, INT* m, INT* n, INT* k, INT* mb, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    slamtsqr(side, trans, *m, *n, *k, *mb, *nb, A, *lda, T, *ldt, C, *ldc, work, *lwork, info);
}

void slanv2_(f32* a, f32* b, f32* c, f32* d, f32* rt1r, f32* rt1i, f32* rt2r, f32* rt2i, f32* cs, f32* sn) {
    slanv2(a, b, c, d, rt1r, rt1i, rt2r, rt2i, cs, sn);
}

void slaorhr_col_getrfnp_(INT* m, INT* n, f32* A, INT* lda, f32* D, INT* info) {
    slaorhr_col_getrfnp(*m, *n, A, *lda, D, info);
}

void slaorhr_col_getrfnp2_(INT* m, INT* n, f32* A, INT* lda, f32* D, INT* info) {
    slaorhr_col_getrfnp2(*m, *n, A, *lda, D, info);
}

void slapll_(INT* n, f32* x, INT* incx, f32* y, INT* incy, f32* ssmin) {
    slapll(*n, x, *incx, y, *incy, ssmin);
}

void slapmr_(INT* forwrd, INT* m, INT* n, f32* X, INT* ldx, INT* K) {
    slapmr(*forwrd, *m, *n, X, *ldx, K);
}

void slapmt_(INT* forwrd, INT* m, INT* n, f32* X, INT* ldx, INT* K) {
    slapmt(*forwrd, *m, *n, X, *ldx, K);
}

void slaqgb_(INT* m, INT* n, INT* kl, INT* ku, f32* AB, INT* ldab, f32* R, f32* C, f32* rowcnd, f32* colcnd, f32* amax, char* equed) {
    slaqgb(*m, *n, *kl, *ku, AB, *ldab, R, C, *rowcnd, *colcnd, *amax, equed);
}

void slaqge_(INT* m, INT* n, f32* A, INT* lda, f32* R, f32* C, f32* rowcnd, f32* colcnd, f32* amax, char* equed) {
    slaqge(*m, *n, A, *lda, R, C, *rowcnd, *colcnd, *amax, equed);
}

void slaqp2_(INT* m, INT* n, INT* offset, f32* A, INT* lda, INT* jpvt, f32* tau, f32* vn1, f32* vn2, f32* work) {
    slaqp2(*m, *n, *offset, A, *lda, jpvt, tau, vn1, vn2, work);
    if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
}

void slaqp2rk_(INT* m, INT* n, INT* nrhs, INT* ioffset, INT* kmax, f32* abstol, f32* reltol, INT* kp1, f32* maxc2nrm, f32* A, INT* lda, INT* K, f32* maxc2nrmk, f32* relmaxc2nrmk, INT* jpiv, f32* tau, f32* vn1, f32* vn2, f32* work, INT* info) {
    slaqp2rk(*m, *n, *nrhs, *ioffset, *kmax, *abstol, *reltol, *kp1, *maxc2nrm, A, *lda, K, maxc2nrmk, relmaxc2nrmk, jpiv, tau, vn1, vn2, work, info);
}

void slaqp3rk_(INT* m, INT* n, INT* nrhs, INT* ioffset, INT* nb, f32* abstol, f32* reltol, INT* kp1, f32* maxc2nrm, f32* A, INT* lda, INT* done, INT* KB, f32* maxc2nrmk, f32* relmaxc2nrmk, INT* jpiv, f32* tau, f32* vn1, f32* vn2, f32* auxv, f32* F, INT* ldf, INT* iwork, INT* info) {
    slaqp3rk(*m, *n, *nrhs, *ioffset, nb, *abstol, *reltol, *kp1, *maxc2nrm, A, *lda, done, KB, maxc2nrmk, relmaxc2nrmk, jpiv, tau, vn1, vn2, auxv, F, *ldf, iwork, info);
}

void slaqps_(INT* m, INT* n, INT* offset, INT* nb, INT* kb, f32* A, INT* lda, INT* jpvt, f32* tau, f32* vn1, f32* vn2, f32* auxv, f32* F, INT* ldf) {
    slaqps(*m, *n, *offset, *nb, kb, A, *lda, jpvt, tau, vn1, vn2, auxv, F, *ldf);
    if (jpvt) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (jpvt[_i] >= 0) jpvt[_i]++; } }
}

void slaqr0_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, f32* H, INT* ldh, f32* wr, f32* wi, INT* iloz, INT* ihiz, f32* Z, INT* ldz, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    slaqr0(*wantt, *wantz, *n, _ilo, _ihi, H, *ldh, wr, wi, *iloz, *ihiz, Z, *ldz, work, *lwork, info);
}

void slaqr1_(INT* n, f32* H, INT* ldh, f32* sr1, f32* si1, f32* sr2, f32* si2, f32* v) {
    slaqr1(*n, H, *ldh, *sr1, *si1, *sr2, *si2, v);
}

void slaqr2_(INT* wantt, INT* wantz, INT* n, INT* ktop, INT* kbot, INT* nw, f32* H, INT* ldh, INT* iloz, INT* ihiz, f32* Z, INT* ldz, INT* ns, INT* nd, f32* sr, f32* si, f32* V, INT* ldv, INT* nh, f32* T, INT* ldt, INT* nv, f32* WV, INT* ldwv, f32* work, INT* lwork) {
    slaqr2(*wantt, *wantz, *n, *ktop, *kbot, *nw, H, *ldh, *iloz, *ihiz, Z, *ldz, ns, nd, sr, si, V, *ldv, *nh, T, *ldt, *nv, WV, *ldwv, work, *lwork);
}

void slaqr3_(INT* wantt, INT* wantz, INT* n, INT* ktop, INT* kbot, INT* nw, f32* H, INT* ldh, INT* iloz, INT* ihiz, f32* Z, INT* ldz, INT* ns, INT* nd, f32* sr, f32* si, f32* V, INT* ldv, INT* nh, f32* T, INT* ldt, INT* nv, f32* WV, INT* ldwv, f32* work, INT* lwork) {
    slaqr3(*wantt, *wantz, *n, *ktop, *kbot, *nw, H, *ldh, *iloz, *ihiz, Z, *ldz, ns, nd, sr, si, V, *ldv, *nh, T, *ldt, *nv, WV, *ldwv, work, *lwork);
}

void slaqr4_(INT* wantt, INT* wantz, INT* n, INT* ilo, INT* ihi, f32* H, INT* ldh, f32* wr, f32* wi, INT* iloz, INT* ihiz, f32* Z, INT* ldz, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    slaqr4(*wantt, *wantz, *n, _ilo, _ihi, H, *ldh, wr, wi, *iloz, *ihiz, Z, *ldz, work, *lwork, info);
}

void slaqr5_(INT* wantt, INT* wantz, INT* kacc22, INT* n, INT* ktop, INT* kbot, INT* nshfts, f32* sr, f32* si, f32* H, INT* ldh, INT* iloz, INT* ihiz, f32* Z, INT* ldz, f32* V, INT* ldv, f32* U, INT* ldu, INT* nv, f32* WV, INT* ldwv, INT* nh, f32* WH, INT* ldwh) {
    slaqr5(*wantt, *wantz, *kacc22, *n, *ktop, *kbot, *nshfts, sr, si, H, *ldh, *iloz, *ihiz, Z, *ldz, V, *ldv, U, *ldu, *nv, WV, *ldwv, *nh, WH, *ldwh);
}

void slaqsb_(char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* S, f32* scond, f32* amax, char* equed) {
    slaqsb(uplo, *n, *kd, AB, *ldab, S, *scond, *amax, equed);
}

void slaqsp_(char* uplo, INT* n, f32* AP, f32* S, f32* scond, f32* amax, char* equed) {
    slaqsp(uplo, *n, AP, S, *scond, *amax, equed);
}

void slaqsy_(char* uplo, INT* n, f32* A, INT* lda, f32* S, f32* scond, f32* amax, char* equed) {
    slaqsy(uplo, *n, A, *lda, S, *scond, *amax, equed);
}

void slaqtr_(INT* ltran, INT* lreal, INT* n, f32* T, INT* ldt, f32* B, f32* w, f32* scale, f32* X, f32* work, INT* info) {
    slaqtr(*ltran, *lreal, *n, T, *ldt, B, *w, scale, X, work, info);
}

void slaqz0_(char* wants, char* wantq, char* wantz, INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* B, INT* ldb, f32* alphar, f32* alphai, f32* beta, f32* Q, INT* ldq, f32* Z, INT* ldz, f32* work, INT* lwork, INT* rec, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    slaqz0(wants, wantq, wantz, *n, _ilo, _ihi, A, *lda, B, *ldb, alphar, alphai, beta, Q, *ldq, Z, *ldz, work, *lwork, *rec, info);
}

void slaqz1_(f32* A, INT* lda, f32* B, INT* ldb, f32* sr1, f32* sr2, f32* si, f32* beta1, f32* beta2, f32* v) {
    slaqz1(A, *lda, B, *ldb, *sr1, *sr2, *si, *beta1, *beta2, v);
}

void slaqz2_(INT* ilq, INT* ilz, INT* k, INT* istartm, INT* istopm, INT* ihi, f32* A, INT* lda, f32* B, INT* ldb, INT* nq, INT* qstart, f32* Q, INT* ldq, INT* nz, INT* zstart, f32* Z, INT* ldz) {
    slaqz2(*ilq, *ilz, *k, *istartm, *istopm, *ihi, A, *lda, B, *ldb, *nq, *qstart, Q, *ldq, *nz, *zstart, Z, *ldz);
}

void slaqz3_(INT* ilschur, INT* ilq, INT* ilz, INT* n, INT* ilo, INT* ihi, INT* nw, f32* A, INT* lda, f32* B, INT* ldb, f32* Q, INT* ldq, f32* Z, INT* ldz, INT* ns, INT* nd, f32* alphar, f32* alphai, f32* beta, f32* QC, INT* ldqc, f32* ZC, INT* ldzc, f32* work, INT* lwork, INT* rec, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    slaqz3(*ilschur, *ilq, *ilz, *n, _ilo, _ihi, *nw, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, ns, nd, alphar, alphai, beta, QC, *ldqc, ZC, *ldzc, work, *lwork, *rec, info);
}

void slaqz4_(INT* ilschur, INT* ilq, INT* ilz, INT* n, INT* ilo, INT* ihi, INT* nshifts, INT* nblock_desired, f32* sr, f32* si, f32* ss, f32* A, INT* lda, f32* B, INT* ldb, f32* Q, INT* ldq, f32* Z, INT* ldz, f32* QC, INT* ldqc, f32* ZC, INT* ldzc, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    slaqz4(*ilschur, *ilq, *ilz, *n, _ilo, _ihi, *nshifts, *nblock_desired, sr, si, ss, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, QC, *ldqc, ZC, *ldzc, work, *lwork, info);
}

void slar1v_(INT* n, INT* b1, INT* bn, f32* lambda, f32* D, f32* L, f32* LD, f32* LLD, f32* pivmin, f32* gaptol, f32* Z, INT* wantnc, INT* negcnt, f32* ztz, f32* mingma, INT* r, INT* isuppz, f32* nrminv, f32* resid, f32* rqcorr, f32* work) {
    slar1v(*n, *b1, *bn, *lambda, D, L, LD, LLD, *pivmin, *gaptol, Z, *wantnc, negcnt, ztz, mingma, r, isuppz, nrminv, resid, rqcorr, work);
}

void slar2v_(INT* n, f32* X, f32* Y, f32* Z, INT* incx, f32* C, f32* S, INT* incc) {
    slar2v(*n, X, Y, Z, *incx, C, S, *incc);
}

void slarf_(char* side, INT* m, INT* n, f32* v, INT* incv, f32* tau, f32* C, INT* ldc, f32* work) {
    slarf(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void slarf1f_(char* side, INT* m, INT* n, f32* v, INT* incv, f32* tau, f32* C, INT* ldc, f32* work) {
    slarf1f(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void slarf1l_(char* side, INT* m, INT* n, f32* v, INT* incv, f32* tau, f32* C, INT* ldc, f32* work) {
    slarf1l(side, *m, *n, v, *incv, *tau, C, *ldc, work);
}

void slarfb_gett_(char* ident, INT* m, INT* n, INT* k, f32* T, INT* ldt, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* ldwork) {
    slarfb_gett(ident, *m, *n, *k, T, *ldt, A, *lda, B, *ldb, work, *ldwork);
}

void slarfb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, f32* V, INT* ldv, f32* T, INT* ldt, f32* C, INT* ldc, f32* work, INT* ldwork) {
    slarfb(side, trans, direct, storev, *m, *n, *k, V, *ldv, T, *ldt, C, *ldc, work, *ldwork);
}

void slarfg_(INT* n, f32* alpha, f32* x, INT* incx, f32* tau) {
    slarfg(*n, alpha, x, *incx, tau);
}

void slarfgp_(INT* n, f32* alpha, f32* x, INT* incx, f32* tau) {
    slarfgp(*n, alpha, x, *incx, tau);
}

void slarft_(char* direct, char* storev, INT* n, INT* k, f32* V, INT* ldv, f32* tau, f32* T, INT* ldt) {
    slarft(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void slarfx_(char* side, INT* m, INT* n, f32* v, f32* tau, f32* C, INT* ldc, f32* work) {
    slarfx(side, *m, *n, v, *tau, C, *ldc, work);
}

void slarfy_(char* uplo, INT* n, f32* V, INT* incv, f32* tau, f32* C, INT* ldc, f32* work) {
    slarfy(uplo, *n, V, *incv, *tau, C, *ldc, work);
}

void slargv_(INT* n, f32* X, INT* incx, f32* Y, INT* incy, f32* C, INT* incc) {
    slargv(*n, X, *incx, Y, *incy, C, *incc);
}

void slarnv_(INT* idist, INT* iseed, INT* n, f32* X) {
    slarnv(*idist, iseed, *n, X);
}

void slarra_(INT* n, f32* D, f32* E, f32* E2, f32* spltol, f32* tnrm, INT* nsplit, INT* isplit, INT* info) {
    slarra(*n, D, E, E2, *spltol, *tnrm, nsplit, isplit, info);
}

void slarrb_(INT* n, f32* D, f32* lld, INT* ifirst, INT* ilast, f32* rtol1, f32* rtol2, INT* offset, f32* W, f32* wgap, f32* werr, f32* work, INT* iwork, f32* pivmin, f32* spdiam, INT* twist, INT* info) {
    slarrb(*n, D, lld, *ifirst, *ilast, *rtol1, *rtol2, *offset, W, wgap, werr, work, iwork, *pivmin, *spdiam, *twist, info);
}

void slarrc_(char* jobt, INT* n, f32* vl, f32* vu, f32* D, f32* E, f32* pivmin, INT* eigcnt, INT* lcnt, INT* rcnt, INT* info) {
    slarrc(jobt, *n, *vl, *vu, D, E, *pivmin, eigcnt, lcnt, rcnt, info);
}

void slarrd_(char* range, char* order, INT* n, f32* vl, f32* vu, INT* il, INT* iu, f32* gers, f32* reltol, f32* D, f32* E, f32* E2, f32* pivmin, INT* nsplit, INT* isplit, INT* m, f32* W, f32* werr, f32* wl, f32* wu, INT* iblock, INT* indexw, f32* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    slarrd(range, order, *n, *vl, *vu, _il, _iu, gers, *reltol, D, E, E2, *pivmin, *nsplit, isplit, m, W, werr, wl, wu, iblock, indexw, work, iwork, info);
    if (iblock) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) iblock[_i]++; }
    if (isplit) { INT _sz = *nsplit; for (INT _i = 0; _i < _sz; _i++) isplit[_i]++; }
    if (indexw) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) indexw[_i]++; }
}

void slarre_(char* range, INT* n, f32* vl, f32* vu, INT* il, INT* iu, f32* D, f32* E, f32* E2, f32* rtol1, f32* rtol2, f32* spltol, INT* nsplit, INT* isplit, INT* m, f32* W, f32* werr, f32* wgap, INT* iblock, INT* indexw, f32* gers, f32* pivmin, f32* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    slarre(range, *n, vl, vu, _il, _iu, D, E, E2, *rtol1, *rtol2, *spltol, nsplit, isplit, m, W, werr, wgap, iblock, indexw, gers, pivmin, work, iwork, info);
    if (iblock) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) iblock[_i]++; }
    if (isplit) { INT _sz = *nsplit; for (INT _i = 0; _i < _sz; _i++) isplit[_i]++; }
    if (indexw) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) indexw[_i]++; }
}

void slarrf_(INT* n, f32* D, f32* L, f32* LD, INT* clstrt, INT* clend, f32* W, f32* wgap, f32* werr, f32* spdiam, f32* clgapl, f32* clgapr, f32* pivmin, f32* sigma, f32* dplus, f32* lplus, f32* work, INT* info) {
    slarrf(*n, D, L, LD, *clstrt, *clend, W, wgap, werr, *spdiam, *clgapl, *clgapr, *pivmin, sigma, dplus, lplus, work, info);
}

void slarrj_(INT* n, f32* D, f32* E2, INT* ifirst, INT* ilast, f32* rtol, INT* offset, f32* W, f32* werr, f32* work, INT* iwork, f32* pivmin, f32* spdiam, INT* info) {
    slarrj(*n, D, E2, *ifirst, *ilast, *rtol, *offset, W, werr, work, iwork, *pivmin, *spdiam, info);
}

void slarrk_(INT* n, INT* iw, f32* gl, f32* gu, f32* D, f32* E2, f32* pivmin, f32* reltol, f32* w, f32* werr, INT* info) {
    slarrk(*n, *iw, *gl, *gu, D, E2, *pivmin, *reltol, w, werr, info);
}

void slarrr_(INT* n, f32* D, f32* E, INT* info) {
    slarrr(*n, D, E, info);
}

void slarrv_(INT* n, f32* vl, f32* vu, f32* D, f32* L, f32* pivmin, INT* isplit, INT* m, INT* dol, INT* dou, f32* minrgp, f32* rtol1, f32* rtol2, f32* W, f32* werr, f32* wgap, INT* iblock, INT* indexw, f32* gers, f32* Z, INT* ldz, INT* isuppz, f32* work, INT* iwork, INT* info) {
    INT* _isplit0 = NULL;
    INT* _iblock0 = NULL;
    INT* _indexw0 = NULL;
    if (isplit) {
        INT _isplit_sz = *n;
        _isplit0 = (INT*)malloc(_isplit_sz * sizeof(INT));
        for (INT _i = 0; _i < _isplit_sz; _i++) _isplit0[_i] = (isplit[_i] > 0) ? isplit[_i] - 1 : isplit[_i];
    }
    if (iblock) {
        INT _iblock_sz = *m;
        _iblock0 = (INT*)malloc(_iblock_sz * sizeof(INT));
        for (INT _i = 0; _i < _iblock_sz; _i++) _iblock0[_i] = (iblock[_i] > 0) ? iblock[_i] - 1 : iblock[_i];
    }
    if (indexw) {
        INT _indexw_sz = *m;
        _indexw0 = (INT*)malloc(_indexw_sz * sizeof(INT));
        for (INT _i = 0; _i < _indexw_sz; _i++) _indexw0[_i] = (indexw[_i] > 0) ? indexw[_i] - 1 : indexw[_i];
    }
    slarrv(*n, *vl, *vu, D, L, *pivmin, _isplit0, *m, *dol, *dou, *minrgp, *rtol1, *rtol2, W, werr, wgap, _iblock0, _indexw0, gers, Z, *ldz, isuppz, work, iwork, info);
    if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    free(_isplit0);
    free(_iblock0);
    free(_indexw0);
}

void slartg_(f32* f, f32* g, f32* c, f32* s, f32* r) {
    slartg(*f, *g, c, s, r);
}

void slartgp_(f32* f, f32* g, f32* cs, f32* sn, f32* r) {
    slartgp(*f, *g, cs, sn, r);
}

void slartgs_(f32* x, f32* y, f32* sigma, f32* cs, f32* sn) {
    slartgs(*x, *y, *sigma, cs, sn);
}

void slartv_(INT* n, f32* X, INT* incx, f32* Y, INT* incy, f32* C, f32* S, INT* incc) {
    slartv(*n, X, *incx, Y, *incy, C, S, *incc);
}

void slaruv_(INT* iseed, INT* n, f32* X) {
    slaruv(iseed, *n, X);
}

void slarz_(char* side, INT* m, INT* n, INT* l, f32* v, INT* incv, f32* tau, f32* C, INT* ldc, f32* work) {
    slarz(side, *m, *n, *l, v, *incv, *tau, C, *ldc, work);
}

void slarzb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, INT* l, f32* V, INT* ldv, f32* T, INT* ldt, f32* C, INT* ldc, f32* work, INT* ldwork) {
    slarzb(side, trans, direct, storev, *m, *n, *k, *l, V, *ldv, T, *ldt, C, *ldc, work, *ldwork);
}

void slarzt_(char* direct, char* storev, INT* n, INT* k, f32* V, INT* ldv, f32* tau, f32* T, INT* ldt) {
    slarzt(direct, storev, *n, *k, V, *ldv, tau, T, *ldt);
}

void slas2_(f32* f, f32* g, f32* h, f32* ssmin, f32* ssmax) {
    slas2(*f, *g, *h, ssmin, ssmax);
}

void slascl_(char* type, INT* kl, INT* ku, f32* cfrom, f32* cto, INT* m, INT* n, f32* A, INT* lda, INT* info) {
    slascl(type, *kl, *ku, *cfrom, *cto, *m, *n, A, *lda, info);
}

void slasd0_(INT* n, INT* sqre, f32* D, f32* E, f32* U, INT* ldu, f32* VT, INT* ldvt, INT* smlsiz, INT* IWORK, f32* work, INT* info) {
    slasd0(*n, *sqre, D, E, U, *ldu, VT, *ldvt, *smlsiz, IWORK, work, info);
}

void slasd1_(INT* nl, INT* nr, INT* sqre, f32* D, f32* alpha, f32* beta, f32* U, INT* ldu, f32* VT, INT* ldvt, INT* IDXQ, INT* IWORK, f32* work, INT* info) {
    slasd1(*nl, *nr, *sqre, D, alpha, beta, U, *ldu, VT, *ldvt, IDXQ, IWORK, work, info);
}

void slasd2_(INT* nl, INT* nr, INT* sqre, INT* k, f32* D, f32* Z, f32* alpha, f32* beta, f32* U, INT* ldu, f32* VT, INT* ldvt, f32* DSIGMA, f32* U2, INT* ldu2, f32* VT2, INT* ldvt2, INT* IDXP, INT* IDX, INT* IDXC, INT* IDXQ, INT* COLTYP, INT* info) {
    slasd2(*nl, *nr, *sqre, k, D, Z, *alpha, *beta, U, *ldu, VT, *ldvt, DSIGMA, U2, *ldu2, VT2, *ldvt2, IDXP, IDX, IDXC, IDXQ, COLTYP, info);
}

void slasd3_(INT* nl, INT* nr, INT* sqre, INT* k, f32* D, f32* Q, INT* ldq, f32* DSIGMA, f32* U, INT* ldu, f32* U2, INT* ldu2, f32* VT, INT* ldvt, f32* VT2, INT* ldvt2, INT* IDXC, INT* CTOT, f32* Z, INT* info) {
    slasd3(*nl, *nr, *sqre, *k, D, Q, *ldq, DSIGMA, U, *ldu, U2, *ldu2, VT, *ldvt, VT2, *ldvt2, IDXC, CTOT, Z, info);
}

void slasd4_(INT* n, INT* i, f32* D, f32* Z, f32* delta, f32* rho, f32* sigma, f32* work, INT* info) {
    INT _i = *i - 1;
    slasd4(*n, _i, D, Z, delta, *rho, sigma, work, info);
}

void slasd5_(INT* i, f32* D, f32* Z, f32* delta, f32* rho, f32* dsigma, f32* work) {
    INT _i = *i - 1;
    slasd5(_i, D, Z, delta, *rho, dsigma, work);
}

void slasd6_(INT* icompq, INT* nl, INT* nr, INT* sqre, f32* D, f32* VF, f32* VL, f32* alpha, f32* beta, INT* IDXQ, INT* PERM, INT* givptr, INT* GIVCOL, INT* ldgcol, f32* GIVNUM, INT* ldgnum, f32* POLES, f32* DIFL, f32* DIFR, f32* Z, INT* k, f32* c, f32* s, f32* work, INT* IWORK, INT* info) {
    slasd6(*icompq, *nl, *nr, *sqre, D, VF, VL, alpha, beta, IDXQ, PERM, givptr, GIVCOL, *ldgcol, GIVNUM, *ldgnum, POLES, DIFL, DIFR, Z, k, c, s, work, IWORK, info);
}

void slasd7_(INT* icompq, INT* nl, INT* nr, INT* sqre, INT* k, f32* D, f32* Z, f32* ZW, f32* VF, f32* VFW, f32* VL, f32* VLW, f32* alpha, f32* beta, f32* DSIGMA, INT* IDX, INT* IDXP, INT* IDXQ, INT* PERM, INT* givptr, INT* GIVCOL, INT* ldgcol, f32* GIVNUM, INT* ldgnum, f32* c, f32* s, INT* info) {
    slasd7(*icompq, *nl, *nr, *sqre, k, D, Z, ZW, VF, VFW, VL, VLW, *alpha, *beta, DSIGMA, IDX, IDXP, IDXQ, PERM, givptr, GIVCOL, *ldgcol, GIVNUM, *ldgnum, c, s, info);
}

void slasd8_(INT* icompq, INT* k, f32* D, f32* Z, f32* VF, f32* VL, f32* DIFL, f32* DIFR, INT* lddifr, f32* DSIGMA, f32* work, INT* info) {
    slasd8(*icompq, *k, D, Z, VF, VL, DIFL, DIFR, *lddifr, DSIGMA, work, info);
}

void slasda_(INT* icompq, INT* smlsiz, INT* n, INT* sqre, f32* D, f32* E, f32* U, INT* ldu, f32* VT, INT* K, f32* DIFL, f32* DIFR, f32* Z, f32* POLES, INT* GIVPTR, INT* GIVCOL, INT* ldgcol, INT* PERM, f32* GIVNUM, f32* C, f32* S, f32* work, INT* IWORK, INT* info) {
    slasda(*icompq, *smlsiz, *n, *sqre, D, E, U, *ldu, VT, K, DIFL, DIFR, Z, POLES, GIVPTR, GIVCOL, *ldgcol, PERM, GIVNUM, C, S, work, IWORK, info);
}

void slasdq_(char* uplo, INT* sqre, INT* n, INT* ncvt, INT* nru, INT* ncc, f32* D, f32* E, f32* VT, INT* ldvt, f32* U, INT* ldu, f32* C, INT* ldc, f32* work, INT* info) {
    slasdq(uplo, *sqre, *n, *ncvt, *nru, *ncc, D, E, VT, *ldvt, U, *ldu, C, *ldc, work, info);
}

void slasdt_(INT* n, INT* lvl, INT* nd, INT* inode, INT* ndiml, INT* ndimr, INT* msub) {
    slasdt(*n, lvl, nd, inode, ndiml, ndimr, *msub);
}

void slaset_(char* uplo, INT* m, INT* n, f32* alpha, f32* beta, f32* A, INT* lda) {
    slaset(uplo, *m, *n, *alpha, *beta, A, *lda);
}

void slasq1_(INT* n, f32* D, f32* E, f32* work, INT* info) {
    slasq1(*n, D, E, work, info);
}

void slasq2_(INT* n, f32* Z, INT* info) {
    slasq2(*n, Z, info);
}

void slasq3_(INT* i0, INT* n0, f32* Z, INT* pp, f32* dmin, f32* sigma, f32* desig, INT* nfail, INT* iter, INT* ndiv, INT* ieee, INT* ttype, f32* dmin1, f32* dmin2, f32* dn, f32* dn1, f32* dn2, f32* g, f32* tau) {
    slasq3(*i0, n0, Z, pp, dmin, sigma, desig, nfail, iter, ndiv, *ieee, ttype, dmin1, dmin2, dn, dn1, dn2, g, tau);
}

void slasq4_(INT* i0, INT* n0, f32* Z, INT* pp, INT* n0in, f32* dmin, f32* dmin1, f32* dmin2, f32* dn, f32* dn1, f32* dn2, f32* tau, INT* ttype, f32* g) {
    slasq4(*i0, *n0, Z, *pp, *n0in, *dmin, *dmin1, *dmin2, *dn, *dn1, *dn2, tau, ttype, g);
}

void slasq5_(INT* i0, INT* n0, f32* Z, INT* pp, f32* tau, f32* sigma, f32* dmin, f32* dmin1, f32* dmin2, f32* dn, f32* dnm1, f32* dnm2, INT* ieee, f32* eps) {
    slasq5(*i0, *n0, Z, *pp, *tau, *sigma, dmin, dmin1, dmin2, dn, dnm1, dnm2, *ieee, *eps);
}

void slasq6_(INT* i0, INT* n0, f32* Z, INT* pp, f32* dmin, f32* dmin1, f32* dmin2, f32* dn, f32* dnm1, f32* dnm2) {
    slasq6(*i0, *n0, Z, *pp, dmin, dmin1, dmin2, dn, dnm1, dnm2);
}

void slasr_(char* side, char* pivot, char* direct, INT* m, INT* n, f32* C_rot, f32* S_rot, f32* A, INT* lda) {
    slasr(side, pivot, direct, *m, *n, C_rot, S_rot, A, *lda);
}

void slasrt_(char* id, INT* n, f32* D, INT* info) {
    slasrt(id, *n, D, info);
}

void slassq_(INT* n, f32* X, INT* incx, f32* scale, f32* sumsq) {
    slassq(*n, X, *incx, scale, sumsq);
}

void slasv2_(f32* f, f32* g, f32* h, f32* ssmin, f32* ssmax, f32* snr, f32* csr, f32* snl, f32* csl) {
    slasv2(*f, *g, *h, ssmin, ssmax, snr, csr, snl, csl);
}

void slaswlq_(INT* m, INT* n, INT* mb, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* lwork, INT* info) {
    slaswlq(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void slaswp_(INT* n, f32* A, INT* lda, INT* k1, INT* k2, INT* ipiv, INT* incx) {
    INT _k1 = *k1 - 1;
    INT _k2 = *k2 - 1;
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *k2;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    slaswp(*n, A, *lda, _k1, _k2, _ipiv0, *incx);
    free(_ipiv0);
}

void slasy2_(INT* ltranl, INT* ltranr, INT* isgn, INT* n1, INT* n2, f32* TL, INT* ldtl, f32* TR, INT* ldtr, f32* B, INT* ldb, f32* scale, f32* X, INT* ldx, f32* xnorm, INT* info) {
    slasy2(*ltranl, *ltranr, *isgn, *n1, *n2, TL, *ldtl, TR, *ldtr, B, *ldb, scale, X, *ldx, xnorm, info);
}

void slasyf_aa_(char* uplo, INT* j1, INT* m, INT* nb, f32* A, INT* lda, INT* ipiv, f32* H, INT* ldh, f32* work) {
    slasyf_aa(uplo, *j1, *m, *nb, A, *lda, ipiv, H, *ldh, work);
}

void slasyf_rk_(char* uplo, INT* n, INT* nb, INT* kb, f32* A, INT* lda, f32* E, INT* ipiv, f32* W, INT* ldw, INT* info) {
    slasyf_rk(uplo, *n, *nb, kb, A, *lda, E, ipiv, W, *ldw, info);
}

void slasyf_rook_(char* uplo, INT* n, INT* nb, INT* kb, f32* A, INT* lda, INT* ipiv, f32* W, INT* ldw, INT* info) {
    slasyf_rook(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void slasyf_(char* uplo, INT* n, INT* nb, INT* kb, f32* A, INT* lda, INT* ipiv, f32* W, INT* ldw, INT* info) {
    slasyf(uplo, *n, *nb, kb, A, *lda, ipiv, W, *ldw, info);
}

void slatbs_(char* uplo, char* trans, char* diag, char* normin, INT* n, INT* kd, f32* AB, INT* ldab, f32* X, f32* scale, f32* cnorm, INT* info) {
    slatbs(uplo, trans, diag, normin, *n, *kd, AB, *ldab, X, scale, cnorm, info);
}

void slatdf_(INT* ijob, INT* n, f32* Z, INT* ldz, f32* rhs, f32* rdsum, f32* rdscal, INT* ipiv, INT* jpiv) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    slatdf(*ijob, *n, Z, *ldz, rhs, rdsum, rdscal, _ipiv0, jpiv);
    free(_ipiv0);
}

void slatps_(char* uplo, char* trans, char* diag, char* normin, INT* n, f32* AP, f32* X, f32* scale, f32* cnorm, INT* info) {
    slatps(uplo, trans, diag, normin, *n, AP, X, scale, cnorm, info);
}

void slatrd_(char* uplo, INT* n, INT* nb, f32* A, INT* lda, f32* E, f32* tau, f32* W, INT* ldw) {
    slatrd(uplo, *n, *nb, A, *lda, E, tau, W, *ldw);
}

void slatrs_(char* uplo, char* trans, char* diag, char* normin, INT* n, f32* A, INT* lda, f32* X, f32* scale, f32* cnorm, INT* info) {
    slatrs(uplo, trans, diag, normin, *n, A, *lda, X, scale, cnorm, info);
}

void slatrs3_(char* uplo, char* trans, char* diag, char* normin, INT* n, INT* nrhs, f32* A, INT* lda, f32* X, INT* ldx, f32* scale, f32* cnorm, f32* work, INT* lwork, INT* info) {
    slatrs3(uplo, trans, diag, normin, *n, *nrhs, A, *lda, X, *ldx, scale, cnorm, work, *lwork, info);
}

void slatrz_(INT* m, INT* n, INT* l, f32* A, INT* lda, f32* tau, f32* work) {
    slatrz(*m, *n, *l, A, *lda, tau, work);
}

void slatsqr_(INT* m, INT* n, INT* mb, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* lwork, INT* info) {
    slatsqr(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void slauu2_(char* uplo, INT* n, f32* A, INT* lda, INT* info) {
    slauu2(uplo, *n, A, *lda, info);
}

void slauum_(char* uplo, INT* n, f32* A, INT* lda, INT* info) {
    slauum(uplo, *n, A, *lda, info);
}

void sopgtr_(char* uplo, INT* n, f32* AP, f32* tau, f32* Q, INT* ldq, f32* work, INT* info) {
    sopgtr(uplo, *n, AP, tau, Q, *ldq, work, info);
}

void sopmtr_(char* side, char* uplo, char* trans, INT* m, INT* n, f32* AP, f32* tau, f32* C, INT* ldc, f32* work, INT* info) {
    sopmtr(side, uplo, trans, *m, *n, AP, tau, C, *ldc, work, info);
}

void sorbdb_(char* trans, char* signs, INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X12, INT* ldx12, f32* X21, INT* ldx21, f32* X22, INT* ldx22, f32* theta, f32* phi, f32* taup1, f32* taup2, f32* tauq1, f32* tauq2, f32* work, INT* lwork, INT* info) {
    sorbdb(trans, signs, *m, *p, *q, X11, *ldx11, X12, *ldx12, X21, *ldx21, X22, *ldx22, theta, phi, taup1, taup2, tauq1, tauq2, work, *lwork, info);
}

void sorbdb1_(INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X21, INT* ldx21, f32* theta, f32* phi, f32* taup1, f32* taup2, f32* tauq1, f32* work, INT* lwork, INT* info) {
    sorbdb1(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void sorbdb2_(INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X21, INT* ldx21, f32* theta, f32* phi, f32* taup1, f32* taup2, f32* tauq1, f32* work, INT* lwork, INT* info) {
    sorbdb2(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void sorbdb3_(INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X21, INT* ldx21, f32* theta, f32* phi, f32* taup1, f32* taup2, f32* tauq1, f32* work, INT* lwork, INT* info) {
    sorbdb3(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, work, *lwork, info);
}

void sorbdb4_(INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X21, INT* ldx21, f32* theta, f32* phi, f32* taup1, f32* taup2, f32* tauq1, f32* phantom, f32* work, INT* lwork, INT* info) {
    sorbdb4(*m, *p, *q, X11, *ldx11, X21, *ldx21, theta, phi, taup1, taup2, tauq1, phantom, work, *lwork, info);
}

void sorbdb5_(INT* m1, INT* m2, INT* n, f32* X1, INT* incx1, f32* X2, INT* incx2, f32* Q1, INT* ldq1, f32* Q2, INT* ldq2, f32* work, INT* lwork, INT* info) {
    sorbdb5(*m1, *m2, *n, X1, *incx1, X2, *incx2, Q1, *ldq1, Q2, *ldq2, work, *lwork, info);
}

void sorbdb6_(INT* m1, INT* m2, INT* n, f32* X1, INT* incx1, f32* X2, INT* incx2, f32* Q1, INT* ldq1, f32* Q2, INT* ldq2, f32* work, INT* lwork, INT* info) {
    sorbdb6(*m1, *m2, *n, X1, *incx1, X2, *incx2, Q1, *ldq1, Q2, *ldq2, work, *lwork, info);
}

void sorcsd_(char* jobu1, char* jobu2, char* jobv1t, char* jobv2t, char* trans, char* signs, INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X12, INT* ldx12, f32* X21, INT* ldx21, f32* X22, INT* ldx22, f32* theta, f32* U1, INT* ldu1, f32* U2, INT* ldu2, f32* V1T, INT* ldv1t, f32* V2T, INT* ldv2t, f32* work, INT* lwork, INT* iwork, INT* info) {
    sorcsd(jobu1, jobu2, jobv1t, jobv2t, trans, signs, *m, *p, *q, X11, *ldx11, X12, *ldx12, X21, *ldx21, X22, *ldx22, theta, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, V2T, *ldv2t, work, *lwork, iwork, info);
}

void sorcsd2by1_(char* jobu1, char* jobu2, char* jobv1t, INT* m, INT* p, INT* q, f32* X11, INT* ldx11, f32* X21, INT* ldx21, f32* theta, f32* U1, INT* ldu1, f32* U2, INT* ldu2, f32* V1T, INT* ldv1t, f32* work, INT* lwork, INT* iwork, INT* info) {
    sorcsd2by1(jobu1, jobu2, jobv1t, *m, *p, *q, X11, *ldx11, X21, *ldx21, theta, U1, *ldu1, U2, *ldu2, V1T, *ldv1t, work, *lwork, iwork, info);
}

void sorg2l_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sorg2l(*m, *n, *k, A, *lda, tau, work, info);
}

void sorg2r_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sorg2r(*m, *n, *k, A, *lda, tau, work, info);
}

void sorgbr_(char* vect, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sorgbr(vect, *m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void sorghr_(INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sorghr(*n, _ilo, _ihi, A, *lda, tau, work, *lwork, info);
}

void sorgl2_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sorgl2(*m, *n, *k, A, *lda, tau, work, info);
}

void sorglq_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sorglq(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void sorgql_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sorgql(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void sorgqr_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sorgqr(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void sorgr2_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* info) {
    sorgr2(*m, *n, *k, A, *lda, tau, work, info);
}

void sorgrq_(INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sorgrq(*m, *n, *k, A, *lda, tau, work, *lwork, info);
}

void sorgtr_(char* uplo, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    sorgtr(uplo, *n, A, *lda, tau, work, *lwork, info);
}

void sorgtsqr_row_(INT* m, INT* n, INT* mb, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* lwork, INT* info) {
    sorgtsqr_row(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void sorgtsqr_(INT* m, INT* n, INT* mb, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* work, INT* lwork, INT* info) {
    sorgtsqr(*m, *n, *mb, *nb, A, *lda, T, *ldt, work, *lwork, info);
}

void sorhr_col_(INT* m, INT* n, INT* nb, f32* A, INT* lda, f32* T, INT* ldt, f32* D, INT* info) {
    sorhr_col(*m, *n, *nb, A, *lda, T, *ldt, D, info);
}

void sorm22_(char* side, char* trans, INT* m, INT* n, INT* n1, INT* n2, f32* Q, INT* ldq, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sorm22(side, trans, *m, *n, *n1, *n2, Q, *ldq, C, *ldc, work, *lwork, info);
}

void sorm2l_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* info) {
    sorm2l(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void sorm2r_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* info) {
    sorm2r(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void sormbr_(char* vect, char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormbr(vect, side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sormhr_(char* side, char* trans, INT* m, INT* n, INT* ilo, INT* ihi, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    INT _ilo = *ilo - 1;
    INT _ihi = *ihi - 1;
    sormhr(side, trans, *m, *n, _ilo, _ihi, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sorml2_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* info) {
    sorml2(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void sormlq_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormlq(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sormql_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormql(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sormqr_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormqr(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sormr2_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* info) {
    sormr2(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, info);
}

void sormr3_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* info) {
    sormr3(side, trans, *m, *n, *k, *l, A, *lda, tau, C, *ldc, work, info);
}

void sormrq_(char* side, char* trans, INT* m, INT* n, INT* k, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormrq(side, trans, *m, *n, *k, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sormrz_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormrz(side, trans, *m, *n, *k, *l, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void sormtr_(char* side, char* uplo, char* trans, INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* C, INT* ldc, f32* work, INT* lwork, INT* info) {
    sormtr(side, uplo, trans, *m, *n, A, *lda, tau, C, *ldc, work, *lwork, info);
}

void spbcon_(char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    spbcon(uplo, *n, *kd, AB, *ldab, *anorm, rcond, work, iwork, info);
}

void spbequ_(char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* S, f32* scond, f32* amax, INT* info) {
    spbequ(uplo, *n, *kd, AB, *ldab, S, scond, amax, info);
}

void spbrfs_(char* uplo, INT* n, INT* kd, INT* nrhs, f32* AB, INT* ldab, f32* AFB, INT* ldafb, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    spbrfs(uplo, *n, *kd, *nrhs, AB, *ldab, AFB, *ldafb, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void spbstf_(char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, INT* info) {
    spbstf(uplo, *n, *kd, AB, *ldab, info);
}

void spbsv_(char* uplo, INT* n, INT* kd, INT* nrhs, f32* AB, INT* ldab, f32* B, INT* ldb, INT* info) {
    spbsv(uplo, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void spbsvx_(char* fact, char* uplo, INT* n, INT* kd, INT* nrhs, f32* AB, INT* ldab, f32* AFB, INT* ldafb, char* equed, f32* S, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    spbsvx(fact, uplo, *n, *kd, *nrhs, AB, *ldab, AFB, *ldafb, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
}

void spbtf2_(char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, INT* info) {
    spbtf2(uplo, *n, *kd, AB, *ldab, info);
}

void spbtrf_(char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, INT* info) {
    spbtrf(uplo, *n, *kd, AB, *ldab, info);
}

void spbtrs_(char* uplo, INT* n, INT* kd, INT* nrhs, f32* AB, INT* ldab, f32* B, INT* ldb, INT* info) {
    spbtrs(uplo, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void spftrf_(char* transr, char* uplo, INT* n, f32* A, INT* info) {
    spftrf(transr, uplo, *n, A, info);
}

void spftri_(char* transr, char* uplo, INT* n, f32* A, INT* info) {
    spftri(transr, uplo, *n, A, info);
}

void spftrs_(char* transr, char* uplo, INT* n, INT* nrhs, f32* A, f32* B, INT* ldb, INT* info) {
    spftrs(transr, uplo, *n, *nrhs, A, B, *ldb, info);
}

void spocon_(char* uplo, INT* n, f32* A, INT* lda, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    spocon(uplo, *n, A, *lda, *anorm, rcond, work, iwork, info);
}

void spoequ_(INT* n, f32* A, INT* lda, f32* S, f32* scond, f32* amax, INT* info) {
    spoequ(*n, A, *lda, S, scond, amax, info);
}

void spoequb_(INT* n, f32* A, INT* lda, f32* S, f32* scond, f32* amax, INT* info) {
    spoequb(*n, A, *lda, S, scond, amax, info);
}

void sporfs_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* AF, INT* ldaf, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    sporfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void sposv_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, INT* info) {
    sposv(uplo, *n, *nrhs, A, *lda, B, *ldb, info);
}

void sposvx_(char* fact, char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* AF, INT* ldaf, char* equed, f32* S, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    sposvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
}

void spotf2_(char* uplo, INT* n, f32* A, INT* lda, INT* info) {
    spotf2(uplo, *n, A, *lda, info);
}

void spotrf_(char* uplo, INT* n, f32* A, INT* lda, INT* info) {
    spotrf(uplo, *n, A, *lda, info);
}

void spotrf2_(char* uplo, INT* n, f32* A, INT* lda, INT* info) {
    spotrf2(uplo, *n, A, *lda, info);
}

void spotri_(char* uplo, INT* n, f32* A, INT* lda, INT* info) {
    spotri(uplo, *n, A, *lda, info);
}

void spotrs_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, INT* info) {
    spotrs(uplo, *n, *nrhs, A, *lda, B, *ldb, info);
}

void sppcon_(char* uplo, INT* n, f32* AP, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    sppcon(uplo, *n, AP, *anorm, rcond, work, iwork, info);
}

void sppequ_(char* uplo, INT* n, f32* AP, f32* S, f32* scond, f32* amax, INT* info) {
    sppequ(uplo, *n, AP, S, scond, amax, info);
}

void spprfs_(char* uplo, INT* n, INT* nrhs, f32* AP, f32* AFP, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    spprfs(uplo, *n, *nrhs, AP, AFP, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void sppsv_(char* uplo, INT* n, INT* nrhs, f32* AP, f32* B, INT* ldb, INT* info) {
    sppsv(uplo, *n, *nrhs, AP, B, *ldb, info);
}

void sppsvx_(char* fact, char* uplo, INT* n, INT* nrhs, f32* AP, f32* AFP, char* equed, f32* S, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    sppsvx(fact, uplo, *n, *nrhs, AP, AFP, equed, S, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
}

void spptrf_(char* uplo, INT* n, f32* AP, INT* info) {
    spptrf(uplo, *n, AP, info);
}

void spptri_(char* uplo, INT* n, f32* AP, INT* info) {
    spptri(uplo, *n, AP, info);
}

void spptrs_(char* uplo, INT* n, INT* nrhs, f32* AP, f32* B, INT* ldb, INT* info) {
    spptrs(uplo, *n, *nrhs, AP, B, *ldb, info);
}

void spstf2_(char* uplo, INT* n, f32* A, INT* lda, INT* piv, INT* rank, f32* tol, f32* work, INT* info) {
    spstf2(uplo, *n, A, *lda, piv, rank, *tol, work, info);
    if (piv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (piv[_i] >= 0) piv[_i]++; } }
}

void spstrf_(char* uplo, INT* n, f32* A, INT* lda, INT* piv, INT* rank, f32* tol, f32* work, INT* info) {
    spstrf(uplo, *n, A, *lda, piv, rank, *tol, work, info);
    if (piv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (piv[_i] >= 0) piv[_i]++; } }
}

void sptcon_(INT* n, f32* D, f32* E, f32* anorm, f32* rcond, f32* work, INT* info) {
    sptcon(*n, D, E, *anorm, rcond, work, info);
}

void spteqr_(char* compz, INT* n, f32* D, f32* E, f32* Z, INT* ldz, f32* work, INT* info) {
    spteqr(compz, *n, D, E, Z, *ldz, work, info);
}

void sptrfs_(INT* n, INT* nrhs, f32* D, f32* E, f32* DF, f32* EF, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* info) {
    sptrfs(*n, *nrhs, D, E, DF, EF, B, *ldb, X, *ldx, ferr, berr, work, info);
}

void sptsv_(INT* n, INT* nrhs, f32* D, f32* E, f32* B, INT* ldb, INT* info) {
    sptsv(*n, *nrhs, D, E, B, *ldb, info);
}

void sptsvx_(char* fact, INT* n, INT* nrhs, f32* D, f32* E, f32* DF, f32* EF, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* info) {
    sptsvx(fact, *n, *nrhs, D, E, DF, EF, B, *ldb, X, *ldx, rcond, ferr, berr, work, info);
}

void spttrf_(INT* n, f32* D, f32* E, INT* info) {
    spttrf(*n, D, E, info);
}

void spttrs_(INT* n, INT* nrhs, f32* D, f32* E, f32* B, INT* ldb, INT* info) {
    spttrs(*n, *nrhs, D, E, B, *ldb, info);
}

void sptts2_(INT* n, INT* nrhs, f32* D, f32* E, f32* B, INT* ldb) {
    sptts2(*n, *nrhs, D, E, B, *ldb);
}

void srscl_(INT* n, f32* sa, f32* sx, INT* incx) {
    srscl(*n, *sa, sx, *incx);
}

void ssb2st_kernels_(char* uplo, INT* wantz, INT* ttype, INT* st, INT* ed, INT* sweep, INT* n, INT* nb, INT* ib, f32* A, INT* lda, f32* V, f32* tau, INT* ldvt, f32* work) {
    ssb2st_kernels(uplo, *wantz, *ttype, *st, *ed, *sweep, *n, *nb, *ib, A, *lda, V, tau, *ldvt, work);
}

void ssbev_2stage_(char* jobz, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* info) {
    ssbev_2stage(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, info);
}

void ssbev_(char* jobz, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* W, f32* Z, INT* ldz, f32* work, INT* info) {
    ssbev(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, info);
}

void ssbevd_2stage_(char* jobz, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ssbevd_2stage(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void ssbevd_(char* jobz, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ssbevd(jobz, uplo, *n, *kd, AB, *ldab, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void ssbevx_2stage_(char* jobz, char* range, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* Q, INT* ldq, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssbevx_2stage(jobz, range, uplo, *n, *kd, AB, *ldab, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void ssbevx_(char* jobz, char* range, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* Q, INT* ldq, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssbevx(jobz, range, uplo, *n, *kd, AB, *ldab, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void ssbgst_(char* vect, char* uplo, INT* n, INT* ka, INT* kb, f32* AB, INT* ldab, f32* BB, INT* ldbb, f32* X, INT* ldx, f32* work, INT* info) {
    ssbgst(vect, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, X, *ldx, work, info);
}

void ssbgv_(char* jobz, char* uplo, INT* n, INT* ka, INT* kb, f32* AB, INT* ldab, f32* BB, INT* ldbb, f32* W, f32* Z, INT* ldz, f32* work, INT* info) {
    ssbgv(jobz, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, W, Z, *ldz, work, info);
}

void ssbgvd_(char* jobz, char* uplo, INT* n, INT* ka, INT* kb, f32* AB, INT* ldab, f32* BB, INT* ldbb, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ssbgvd(jobz, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void ssbgvx_(char* jobz, char* range, char* uplo, INT* n, INT* ka, INT* kb, f32* AB, INT* ldab, f32* BB, INT* ldbb, f32* Q, INT* ldq, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssbgvx(jobz, range, uplo, *n, *ka, *kb, AB, *ldab, BB, *ldbb, Q, *ldq, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void ssbtrd_(char* vect, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* D, f32* E, f32* Q, INT* ldq, f32* work, INT* info) {
    ssbtrd(vect, uplo, *n, *kd, AB, *ldab, D, E, Q, *ldq, work, info);
}

void ssfrk_(char* transr, char* uplo, char* trans, INT* n, INT* k, f32* alpha, f32* A, INT* lda, f32* beta, f32* C) {
    ssfrk(transr, uplo, trans, *n, *k, *alpha, A, *lda, *beta, C);
}

void sspcon_(char* uplo, INT* n, f32* AP, INT* ipiv, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    sspcon(uplo, *n, AP, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void sspev_(char* jobz, char* uplo, INT* n, f32* AP, f32* W, f32* Z, INT* ldz, f32* work, INT* info) {
    sspev(jobz, uplo, *n, AP, W, Z, *ldz, work, info);
}

void sspevd_(char* jobz, char* uplo, INT* n, f32* AP, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    sspevd(jobz, uplo, *n, AP, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void sspevx_(char* jobz, char* range, char* uplo, INT* n, f32* AP, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sspevx(jobz, range, uplo, *n, AP, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void sspgst_(INT* itype, char* uplo, INT* n, f32* AP, f32* BP, INT* info) {
    sspgst(*itype, uplo, *n, AP, BP, info);
}

void sspgv_(INT* itype, char* jobz, char* uplo, INT* n, f32* AP, f32* BP, f32* W, f32* Z, INT* ldz, f32* work, INT* info) {
    sspgv(*itype, jobz, uplo, *n, AP, BP, W, Z, *ldz, work, info);
}

void sspgvd_(INT* itype, char* jobz, char* uplo, INT* n, f32* AP, f32* BP, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    sspgvd(*itype, jobz, uplo, *n, AP, BP, W, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void sspgvx_(INT* itype, char* jobz, char* range, char* uplo, INT* n, f32* AP, f32* BP, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sspgvx(*itype, jobz, range, uplo, *n, AP, BP, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void ssprfs_(char* uplo, INT* n, INT* nrhs, f32* AP, f32* AFP, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssprfs(uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void sspsv_(char* uplo, INT* n, INT* nrhs, f32* AP, INT* ipiv, f32* B, INT* ldb, INT* info) {
    sspsv(uplo, *n, *nrhs, AP, ipiv, B, *ldb, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void sspsvx_(char* fact, char* uplo, INT* n, INT* nrhs, f32* AP, f32* AFP, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        sspsvx(fact, uplo, *n, *nrhs, AP, AFP, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        free(_ipiv0);
    } else {
        sspsvx(fact, uplo, *n, *nrhs, AP, AFP, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssptrd_(char* uplo, INT* n, f32* AP, f32* D, f32* E, f32* tau, INT* info) {
    ssptrd(uplo, *n, AP, D, E, tau, info);
}

void ssptrf_(char* uplo, INT* n, f32* AP, INT* ipiv, INT* info) {
    ssptrf(uplo, *n, AP, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void ssptri_(char* uplo, INT* n, f32* AP, INT* ipiv, f32* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssptri(uplo, *n, AP, _ipiv0, work, info);
    free(_ipiv0);
}

void ssptrs_(char* uplo, INT* n, INT* nrhs, f32* AP, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssptrs(uplo, *n, *nrhs, AP, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void sstebz_(char* range, char* order, INT* n, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, f32* D, f32* E, INT* m, INT* nsplit, f32* W, INT* iblock, INT* isplit, f32* work, INT* iwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sstebz(range, order, *n, *vl, *vu, _il, _iu, *abstol, D, E, m, nsplit, W, iblock, isplit, work, iwork, info);
    if (iblock) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) iblock[_i]++; }
    if (isplit) { INT _sz = *nsplit; for (INT _i = 0; _i < _sz; _i++) isplit[_i]++; }
}

void sstedc_(char* compz, INT* n, f32* D, f32* E, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    sstedc(compz, *n, D, E, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void sstegr_(char* jobz, char* range, INT* n, f32* D, f32* E, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, INT* isuppz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sstegr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void sstein_(INT* n, f32* D, f32* E, INT* m, f32* W, INT* iblock, INT* isplit, f32* Z, INT* ldz, f32* work, INT* iwork, INT* ifail, INT* info) {
    INT* _iblock0 = NULL;
    INT* _isplit0 = NULL;
    if (iblock) {
        INT _iblock_sz = *m;
        _iblock0 = (INT*)malloc(_iblock_sz * sizeof(INT));
        for (INT _i = 0; _i < _iblock_sz; _i++) _iblock0[_i] = (iblock[_i] > 0) ? iblock[_i] - 1 : iblock[_i];
    }
    if (isplit) {
        INT _isplit_sz = *n;
        _isplit0 = (INT*)malloc(_isplit_sz * sizeof(INT));
        for (INT _i = 0; _i < _isplit_sz; _i++) _isplit0[_i] = (isplit[_i] > 0) ? isplit[_i] - 1 : isplit[_i];
    }
    sstein(*n, D, E, *m, W, _iblock0, _isplit0, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    free(_iblock0);
    free(_isplit0);
}

void sstemr_(char* jobz, char* range, INT* n, f32* D, f32* E, f32* vl, f32* vu, INT* il, INT* iu, INT* m, f32* W, f32* Z, INT* ldz, INT* nzc, INT* isuppz, INT* tryrac, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sstemr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, m, W, Z, *ldz, *nzc, isuppz, tryrac, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void ssteqr_(char* compz, INT* n, f32* D, f32* E, f32* Z, INT* ldz, f32* work, INT* info) {
    ssteqr(compz, *n, D, E, Z, *ldz, work, info);
}

void ssterf_(INT* n, f32* D, f32* E, INT* info) {
    ssterf(*n, D, E, info);
}

void sstev_(char* jobz, INT* n, f32* D, f32* E, f32* Z, INT* ldz, f32* work, INT* info) {
    sstev(jobz, *n, D, E, Z, *ldz, work, info);
}

void sstevd_(char* jobz, INT* n, f32* D, f32* E, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    sstevd(jobz, *n, D, E, Z, *ldz, work, *lwork, iwork, *liwork, info);
}

void sstevr_(char* jobz, char* range, INT* n, f32* D, f32* E, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, INT* isuppz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sstevr(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void sstevx_(char* jobz, char* range, INT* n, f32* D, f32* E, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    sstevx(jobz, range, *n, D, E, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, iwork, ifail, info);
    if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
}

void ssycon_3_(char* uplo, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssycon_3(uplo, *n, A, *lda, E, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void ssycon_rook_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssycon_rook(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void ssycon_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* anorm, f32* rcond, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssycon(uplo, *n, A, *lda, _ipiv0, *anorm, rcond, work, iwork, info);
    free(_ipiv0);
}

void ssyconv_(char* uplo, char* way, INT* n, f32* A, INT* lda, INT* ipiv, f32* E, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssyconv(uplo, way, *n, A, *lda, _ipiv0, E, info);
    free(_ipiv0);
}

void ssyconvf_rook_(char* uplo, char* way, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssyconvf_rook(uplo, way, *n, A, *lda, E, _ipiv0, info);
    free(_ipiv0);
}

void ssyconvf_(char* uplo, char* way, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssyconvf(uplo, way, *n, A, *lda, E, _ipiv0, info);
    free(_ipiv0);
}

void ssyequb_(char* uplo, INT* n, f32* A, INT* lda, f32* S, f32* scond, f32* amax, f32* work, INT* info) {
    ssyequb(uplo, *n, A, *lda, S, scond, amax, work, info);
}

void ssyev_2stage_(char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* W, f32* work, INT* lwork, INT* info) {
    ssyev_2stage(jobz, uplo, *n, A, *lda, W, work, *lwork, info);
}

void ssyev_(char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* W, f32* work, INT* lwork, INT* info) {
    ssyev(jobz, uplo, *n, A, *lda, W, work, *lwork, info);
}

void ssyevd_2stage_(char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* W, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ssyevd_2stage(jobz, uplo, *n, A, *lda, W, work, *lwork, iwork, *liwork, info);
}

void ssyevd_(char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* W, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ssyevd(jobz, uplo, *n, A, *lda, W, work, *lwork, iwork, *liwork, info);
}

void ssyevr_2stage_(char* jobz, char* range, char* uplo, INT* n, f32* A, INT* lda, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, INT* isuppz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssyevr_2stage(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz && ((*jobz == 'V' || *jobz == 'v') && (*range == 'A' || *range == 'a'))) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void ssyevr_(char* jobz, char* range, char* uplo, INT* n, f32* A, INT* lda, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, INT* isuppz, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssyevr(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, isuppz, work, *lwork, iwork, *liwork, info);
    if (*lwork != -1) {
        if (isuppz && ((*jobz == 'V' || *jobz == 'v') && (*range == 'A' || *range == 'a'))) { INT _sz = 2 * (*m); for (INT _i = 0; _i < _sz; _i++) isuppz[_i]++; }
    }
}

void ssyevx_2stage_(char* jobz, char* range, char* uplo, INT* n, f32* A, INT* lda, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssyevx_2stage(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void ssyevx_(char* jobz, char* range, char* uplo, INT* n, f32* A, INT* lda, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssyevx(jobz, range, uplo, *n, A, *lda, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void ssygs2_(INT* itype, char* uplo, INT* n, f32* A, INT* lda, f32* B, INT* ldb, INT* info) {
    ssygs2(*itype, uplo, *n, A, *lda, B, *ldb, info);
}

void ssygst_(INT* itype, char* uplo, INT* n, f32* A, INT* lda, f32* B, INT* ldb, INT* info) {
    ssygst(*itype, uplo, *n, A, *lda, B, *ldb, info);
}

void ssygv_2stage_(INT* itype, char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* W, f32* work, INT* lwork, INT* info) {
    ssygv_2stage(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, info);
}

void ssygv_(INT* itype, char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* W, f32* work, INT* lwork, INT* info) {
    ssygv(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, info);
}

void ssygvd_(INT* itype, char* jobz, char* uplo, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* W, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    ssygvd(*itype, jobz, uplo, *n, A, *lda, B, *ldb, W, work, *lwork, iwork, *liwork, info);
}

void ssygvx_(INT* itype, char* jobz, char* range, char* uplo, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* vl, f32* vu, INT* il, INT* iu, f32* abstol, INT* m, f32* W, f32* Z, INT* ldz, f32* work, INT* lwork, INT* iwork, INT* ifail, INT* info) {
    INT _il = *il - 1;
    INT _iu = *iu - 1;
    ssygvx(*itype, jobz, range, uplo, *n, A, *lda, B, *ldb, *vl, *vu, _il, _iu, *abstol, m, W, Z, *ldz, work, *lwork, iwork, ifail, info);
    if (*lwork != -1) {
        if (ifail) { INT _sz = *m; for (INT _i = 0; _i < _sz; _i++) ifail[_i]++; }
    }
}

void ssyrfs_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* AF, INT* ldaf, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssyrfs(uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
    free(_ipiv0);
}

void ssysv_aa_2stage_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* TB, INT* ltb, INT* ipiv, INT* ipiv2, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    ssysv_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, ipiv, ipiv2, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void ssysv_aa_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    ssysv_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssysv_rk_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* E, INT* ipiv, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    ssysv_rk(uplo, *n, *nrhs, A, *lda, E, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssysv_rook_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    ssysv_rook(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssysv_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    ssysv(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssysvx_(char* fact, char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* AF, INT* ldaf, INT* ipiv, f32* B, INT* ldb, f32* X, INT* ldx, f32* rcond, f32* ferr, f32* berr, f32* work, INT* lwork, INT* iwork, INT* info) {
    if (fact[0] == 'F' || fact[0] == 'f') {
        INT* _ipiv0 = NULL;
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
        ssysvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, _ipiv0, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, iwork, info);
        free(_ipiv0);
    } else {
        ssysvx(fact, uplo, *n, *nrhs, A, *lda, AF, *ldaf, ipiv, B, *ldb, X, *ldx, rcond, ferr, berr, work, *lwork, iwork, info);
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssyswapr_(char* uplo, INT* n, f32* A, INT* lda, INT* i1, INT* i2) {
    ssyswapr(uplo, *n, A, *lda, *i1, *i2);
}

void ssytd2_(char* uplo, INT* n, f32* A, INT* lda, f32* D, f32* E, f32* tau, INT* info) {
    ssytd2(uplo, *n, A, *lda, D, E, tau, info);
}

void ssytf2_rk_(char* uplo, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, INT* info) {
    ssytf2_rk(uplo, *n, A, *lda, E, ipiv, info);
}

void ssytf2_rook_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, INT* info) {
    ssytf2_rook(uplo, *n, A, *lda, ipiv, info);
}

void ssytf2_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, INT* info) {
    ssytf2(uplo, *n, A, *lda, ipiv, info);
    if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
}

void ssytrd_2stage_(char* vect, char* uplo, INT* n, f32* A, INT* lda, f32* D, f32* E, f32* tau, f32* hous2, INT* lhous2, f32* work, INT* lwork, INT* info) {
    ssytrd_2stage(vect, uplo, *n, A, *lda, D, E, tau, hous2, *lhous2, work, *lwork, info);
}

void ssytrd_sb2st_(char* stage1, char* vect, char* uplo, INT* n, INT* kd, f32* AB, INT* ldab, f32* D, f32* E, f32* hous, INT* lhous, f32* work, INT* lwork, INT* info) {
    ssytrd_sb2st(stage1, vect, uplo, *n, *kd, AB, *ldab, D, E, hous, *lhous, work, *lwork, info);
}

void ssytrd_sy2sb_(char* uplo, INT* n, INT* kd, f32* A, INT* lda, f32* AB, INT* ldab, f32* tau, f32* work, INT* lwork, INT* info) {
    ssytrd_sy2sb(uplo, *n, *kd, A, *lda, AB, *ldab, tau, work, *lwork, info);
}

void ssytrd_(char* uplo, INT* n, f32* A, INT* lda, f32* D, f32* E, f32* tau, f32* work, INT* lwork, INT* info) {
    ssytrd(uplo, *n, A, *lda, D, E, tau, work, *lwork, info);
}

void ssytrf_aa_2stage_(char* uplo, INT* n, f32* A, INT* lda, f32* TB, INT* ltb, INT* ipiv, INT* ipiv2, f32* work, INT* lwork, INT* info) {
    ssytrf_aa_2stage(uplo, *n, A, *lda, TB, *ltb, ipiv, ipiv2, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
        if (ipiv2) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv2[_i] >= 0) ipiv2[_i]++; } }
    }
}

void ssytrf_aa_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* lwork, INT* info) {
    ssytrf_aa(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssytrf_rk_(char* uplo, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, f32* work, INT* lwork, INT* info) {
    ssytrf_rk(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssytrf_rook_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* lwork, INT* info) {
    ssytrf_rook(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssytrf_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* lwork, INT* info) {
    ssytrf(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    if (*lwork != -1) {
        if (ipiv) { INT _sz = *n; for (INT _i = 0; _i < _sz; _i++) { if (ipiv[_i] >= 0) ipiv[_i]++; } }
    }
}

void ssytri_3_(char* uplo, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, f32* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        ssytri_3(uplo, *n, A, *lda, E, _ipiv0, work, *lwork, info);
    } else {
        ssytri_3(uplo, *n, A, *lda, E, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void ssytri_3x_(char* uplo, INT* n, f32* A, INT* lda, f32* E, INT* ipiv, f32* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytri_3x(uplo, *n, A, *lda, E, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void ssytri_rook_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytri_rook(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void ssytri_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytri(uplo, *n, A, *lda, _ipiv0, work, info);
    free(_ipiv0);
}

void ssytri2_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        ssytri2(uplo, *n, A, *lda, _ipiv0, work, *lwork, info);
    } else {
        ssytri2(uplo, *n, A, *lda, ipiv, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void ssytri2x_(char* uplo, INT* n, f32* A, INT* lda, INT* ipiv, f32* work, INT* nb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytri2x(uplo, *n, A, *lda, _ipiv0, work, *nb, info);
    free(_ipiv0);
}

void ssytrs_3_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* E, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytrs_3(uplo, *n, *nrhs, A, *lda, E, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void ssytrs_aa_2stage_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, f32* TB, INT* ltb, INT* ipiv, INT* ipiv2, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    INT* _ipiv20 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    if (ipiv2) {
        INT _ipiv2_sz = *n;
        _ipiv20 = (INT*)malloc(_ipiv2_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv2_sz; _i++) _ipiv20[_i] = (ipiv2[_i] > 0) ? ipiv2[_i] - 1 : ipiv2[_i];
    }
    ssytrs_aa_2stage(uplo, *n, *nrhs, A, *lda, TB, *ltb, _ipiv0, _ipiv20, B, *ldb, info);
    free(_ipiv0);
    free(_ipiv20);
}

void ssytrs_aa_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, f32* work, INT* lwork, INT* info) {
    INT* _ipiv0 = NULL;
    if (*lwork != -1) {
        if (ipiv) {
            INT _ipiv_sz = *n;
            _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
            for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
        }
    }
    if (*lwork != -1) {
        ssytrs_aa(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, *lwork, info);
    } else {
        ssytrs_aa(uplo, *n, *nrhs, A, *lda, ipiv, B, *ldb, work, *lwork, info);
    }
    if (*lwork != -1) {
        free(_ipiv0);
    }
}

void ssytrs_rook_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytrs_rook(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void ssytrs_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytrs(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, info);
    free(_ipiv0);
}

void ssytrs2_(char* uplo, INT* n, INT* nrhs, f32* A, INT* lda, INT* ipiv, f32* B, INT* ldb, f32* work, INT* info) {
    INT* _ipiv0 = NULL;
    if (ipiv) {
        INT _ipiv_sz = *n;
        _ipiv0 = (INT*)malloc(_ipiv_sz * sizeof(INT));
        for (INT _i = 0; _i < _ipiv_sz; _i++) _ipiv0[_i] = (ipiv[_i] > 0) ? ipiv[_i] - 1 : ipiv[_i];
    }
    ssytrs2(uplo, *n, *nrhs, A, *lda, _ipiv0, B, *ldb, work, info);
    free(_ipiv0);
}

void stbcon_(char* norm, char* uplo, char* diag, INT* n, INT* kd, f32* AB, INT* ldab, f32* rcond, f32* work, INT* iwork, INT* info) {
    stbcon(norm, uplo, diag, *n, *kd, AB, *ldab, rcond, work, iwork, info);
}

void stbrfs_(char* uplo, char* trans, char* diag, INT* n, INT* kd, INT* nrhs, f32* AB, INT* ldab, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    stbrfs(uplo, trans, diag, *n, *kd, *nrhs, AB, *ldab, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void stbtrs_(char* uplo, char* trans, char* diag, INT* n, INT* kd, INT* nrhs, f32* AB, INT* ldab, f32* B, INT* ldb, INT* info) {
    stbtrs(uplo, trans, diag, *n, *kd, *nrhs, AB, *ldab, B, *ldb, info);
}

void stfsm_(char* transr, char* side, char* uplo, char* trans, char* diag, INT* m, INT* n, f32* alpha, f32* A, f32* B, INT* ldb) {
    stfsm(transr, side, uplo, trans, diag, *m, *n, *alpha, A, B, *ldb);
}

void stftri_(char* transr, char* uplo, char* diag, INT* n, f32* A, INT* info) {
    stftri(transr, uplo, diag, *n, A, info);
}

void stfttp_(char* transr, char* uplo, INT* n, f32* ARF, f32* AP, INT* info) {
    stfttp(transr, uplo, *n, ARF, AP, info);
}

void stfttr_(char* transr, char* uplo, INT* n, f32* ARF, f32* A, INT* lda, INT* info) {
    stfttr(transr, uplo, *n, ARF, A, *lda, info);
}

void stgevc_(char* side, char* howmny, INT* select, INT* n, f32* S, INT* lds, f32* P, INT* ldp, f32* VL, INT* ldvl, f32* VR, INT* ldvr, INT* mm, INT* m, f32* work, INT* info) {
    stgevc(side, howmny, select, *n, S, *lds, P, *ldp, VL, *ldvl, VR, *ldvr, *mm, m, work, info);
}

void stgex2_(INT* wantq, INT* wantz, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* Q, INT* ldq, f32* Z, INT* ldz, INT* j1, INT* n1, INT* n2, f32* work, INT* lwork, INT* info) {
    stgex2(*wantq, *wantz, *n, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, *j1, *n1, *n2, work, *lwork, info);
}

void stgexc_(INT* wantq, INT* wantz, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* Q, INT* ldq, f32* Z, INT* ldz, INT* ifst, INT* ilst, f32* work, INT* lwork, INT* info) {
    INT _ifst = *ifst - 1;
    INT _ilst = *ilst - 1;
    stgexc(*wantq, *wantz, *n, A, *lda, B, *ldb, Q, *ldq, Z, *ldz, &_ifst, &_ilst, work, *lwork, info);
    *ifst = _ifst + 1;
    *ilst = _ilst + 1;
}

void stgsen_(INT* ijob, INT* wantq, INT* wantz, INT* select, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* alphar, f32* alphai, f32* beta, f32* Q, INT* ldq, f32* Z, INT* ldz, INT* m, f32* pl, f32* pr, f32* dif, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    stgsen(*ijob, *wantq, *wantz, select, *n, A, *lda, B, *ldb, alphar, alphai, beta, Q, *ldq, Z, *ldz, m, pl, pr, dif, work, *lwork, iwork, *liwork, info);
}

void stgsja_(char* jobu, char* jobv, char* jobq, INT* m, INT* p, INT* n, INT* k, INT* l, f32* A, INT* lda, f32* B, INT* ldb, f32* tola, f32* tolb, f32* alpha, f32* beta, f32* U, INT* ldu, f32* V, INT* ldv, f32* Q, INT* ldq, f32* work, INT* ncycle, INT* info) {
    stgsja(jobu, jobv, jobq, *m, *p, *n, *k, *l, A, *lda, B, *ldb, *tola, *tolb, alpha, beta, U, *ldu, V, *ldv, Q, *ldq, work, ncycle, info);
}

void stgsna_(char* job, char* howmny, INT* select, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* VL, INT* ldvl, f32* VR, INT* ldvr, f32* S, f32* dif, INT* mm, INT* m, f32* work, INT* lwork, INT* iwork, INT* info) {
    stgsna(job, howmny, select, *n, A, *lda, B, *ldb, VL, *ldvl, VR, *ldvr, S, dif, *mm, m, work, *lwork, iwork, info);
}

void stgsy2_(char* trans, INT* ijob, INT* m, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* C, INT* ldc, f32* D, INT* ldd, f32* E, INT* lde, f32* F, INT* ldf, f32* scale, f32* rdsum, f32* rdscal, INT* iwork, INT* pq, INT* info) {
    stgsy2(trans, *ijob, *m, *n, A, *lda, B, *ldb, C, *ldc, D, *ldd, E, *lde, F, *ldf, scale, rdsum, rdscal, iwork, pq, info);
}

void stgsyl_(char* trans, INT* ijob, INT* m, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* C, INT* ldc, f32* D, INT* ldd, f32* E, INT* lde, f32* F, INT* ldf, f32* scale, f32* dif, f32* work, INT* lwork, INT* iwork, INT* info) {
    stgsyl(trans, *ijob, *m, *n, A, *lda, B, *ldb, C, *ldc, D, *ldd, E, *lde, F, *ldf, scale, dif, work, *lwork, iwork, info);
}

void stpcon_(char* norm, char* uplo, char* diag, INT* n, f32* AP, f32* rcond, f32* work, INT* iwork, INT* info) {
    stpcon(norm, uplo, diag, *n, AP, rcond, work, iwork, info);
}

void stplqt_(INT* m, INT* n, INT* l, INT* mb, f32* A, INT* lda, f32* B, INT* ldb, f32* T, INT* ldt, f32* work, INT* info) {
    stplqt(*m, *n, *l, *mb, A, *lda, B, *ldb, T, *ldt, work, info);
}

void stplqt2_(INT* m, INT* n, INT* l, f32* A, INT* lda, f32* B, INT* ldb, f32* T, INT* ldt, INT* info) {
    stplqt2(*m, *n, *l, A, *lda, B, *ldb, T, *ldt, info);
}

void stpmlqt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, INT* mb, f32* V, INT* ldv, f32* T, INT* ldt, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* info) {
    stpmlqt(side, trans, *m, *n, *k, *l, *mb, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, info);
}

void stpmqrt_(char* side, char* trans, INT* m, INT* n, INT* k, INT* l, INT* nb, f32* V, INT* ldv, f32* T, INT* ldt, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* info) {
    stpmqrt(side, trans, *m, *n, *k, *l, *nb, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, info);
}

void stpqrt_(INT* m, INT* n, INT* l, INT* nb, f32* A, INT* lda, f32* B, INT* ldb, f32* T, INT* ldt, f32* work, INT* info) {
    stpqrt(*m, *n, *l, *nb, A, *lda, B, *ldb, T, *ldt, work, info);
}

void stpqrt2_(INT* m, INT* n, INT* l, f32* A, INT* lda, f32* B, INT* ldb, f32* T, INT* ldt, INT* info) {
    stpqrt2(*m, *n, *l, A, *lda, B, *ldb, T, *ldt, info);
}

void stprfb_(char* side, char* trans, char* direct, char* storev, INT* m, INT* n, INT* k, INT* l, f32* V, INT* ldv, f32* T, INT* ldt, f32* A, INT* lda, f32* B, INT* ldb, f32* work, INT* ldwork) {
    stprfb(side, trans, direct, storev, *m, *n, *k, *l, V, *ldv, T, *ldt, A, *lda, B, *ldb, work, *ldwork);
}

void stprfs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f32* AP, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    stprfs(uplo, trans, diag, *n, *nrhs, AP, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void stptri_(char* uplo, char* diag, INT* n, f32* AP, INT* info) {
    stptri(uplo, diag, *n, AP, info);
}

void stptrs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f32* AP, f32* B, INT* ldb, INT* info) {
    stptrs(uplo, trans, diag, *n, *nrhs, AP, B, *ldb, info);
}

void stpttf_(char* transr, char* uplo, INT* n, f32* AP, f32* ARF, INT* info) {
    stpttf(transr, uplo, *n, AP, ARF, info);
}

void stpttr_(char* uplo, INT* n, f32* AP, f32* A, INT* lda, INT* info) {
    stpttr(uplo, *n, AP, A, *lda, info);
}

void strcon_(char* norm, char* uplo, char* diag, INT* n, f32* A, INT* lda, f32* rcond, f32* work, INT* iwork, INT* info) {
    strcon(norm, uplo, diag, *n, A, *lda, rcond, work, iwork, info);
}

void strevc_(char* side, char* howmny, INT* select, INT* n, f32* T, INT* ldt, f32* VL, INT* ldvl, f32* VR, INT* ldvr, INT* mm, INT* m, f32* work, INT* info) {
    strevc(side, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, *mm, m, work, info);
}

void strevc3_(char* side, char* howmny, INT* select, INT* n, f32* T, INT* ldt, f32* VL, INT* ldvl, f32* VR, INT* ldvr, INT* mm, INT* m, f32* work, INT* lwork, INT* info) {
    strevc3(side, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, *mm, m, work, *lwork, info);
}

void strexc_(char* compq, INT* n, f32* T, INT* ldt, f32* Q, INT* ldq, INT* ifst, INT* ilst, f32* work, INT* info) {
    INT _ifst = *ifst - 1;
    INT _ilst = *ilst - 1;
    strexc(compq, *n, T, *ldt, Q, *ldq, &_ifst, &_ilst, work, info);
    *ifst = _ifst + 1;
    *ilst = _ilst + 1;
}

void strrfs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, f32* X, INT* ldx, f32* ferr, f32* berr, f32* work, INT* iwork, INT* info) {
    strrfs(uplo, trans, diag, *n, *nrhs, A, *lda, B, *ldb, X, *ldx, ferr, berr, work, iwork, info);
}

void strsen_(char* job, char* compq, INT* select, INT* n, f32* T, INT* ldt, f32* Q, INT* ldq, f32* wr, f32* wi, INT* m, f32* s, f32* sep, f32* work, INT* lwork, INT* iwork, INT* liwork, INT* info) {
    strsen(job, compq, select, *n, T, *ldt, Q, *ldq, wr, wi, m, s, sep, work, *lwork, iwork, *liwork, info);
}

void strsna_(char* job, char* howmny, INT* select, INT* n, f32* T, INT* ldt, f32* VL, INT* ldvl, f32* VR, INT* ldvr, f32* S, f32* sep, INT* mm, INT* m, f32* work, INT* ldwork, INT* iwork, INT* info) {
    strsna(job, howmny, select, *n, T, *ldt, VL, *ldvl, VR, *ldvr, S, sep, *mm, m, work, *ldwork, iwork, info);
}

void strsyl_(char* trana, char* tranb, INT* isgn, INT* m, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* C, INT* ldc, f32* scale, INT* info) {
    strsyl(trana, tranb, *isgn, *m, *n, A, *lda, B, *ldb, C, *ldc, scale, info);
}

void strsyl3_(char* trana, char* tranb, INT* isgn, INT* m, INT* n, f32* A, INT* lda, f32* B, INT* ldb, f32* C, INT* ldc, f32* scale, INT* iwork, INT* liwork, f32* swork, INT* ldswork, INT* info) {
    strsyl3(trana, tranb, *isgn, *m, *n, A, *lda, B, *ldb, C, *ldc, scale, iwork, *liwork, swork, *ldswork, info);
}

void strti2_(char* uplo, char* diag, INT* n, f32* A, INT* lda, INT* info) {
    strti2(uplo, diag, *n, A, *lda, info);
}

void strtri_(char* uplo, char* diag, INT* n, f32* A, INT* lda, INT* info) {
    strtri(uplo, diag, *n, A, *lda, info);
}

void strtrs_(char* uplo, char* trans, char* diag, INT* n, INT* nrhs, f32* A, INT* lda, f32* B, INT* ldb, INT* info) {
    strtrs(uplo, trans, diag, *n, *nrhs, A, *lda, B, *ldb, info);
}

void strttf_(char* transr, char* uplo, INT* n, f32* A, INT* lda, f32* ARF, INT* info) {
    strttf(transr, uplo, *n, A, *lda, ARF, info);
}

void strttp_(char* uplo, INT* n, f32* A, INT* lda, f32* AP, INT* info) {
    strttp(uplo, *n, A, *lda, AP, info);
}

void stzrzf_(INT* m, INT* n, f32* A, INT* lda, f32* tau, f32* work, INT* lwork, INT* info) {
    stzrzf(*m, *n, A, *lda, tau, work, *lwork, info);
}
